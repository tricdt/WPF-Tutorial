<UserControl x:Class="syncfusion.ledsign.wpf.LedGroupPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:syncfusion.ledsign.wpf"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Border BorderThickness="1" BorderBrush="{DynamicResource PrimaryBackground}" CornerRadius="10" Padding="5" HorizontalAlignment="Left">
        <Grid>
            <Grid.OpacityMask>
                <VisualBrush Visual="{Binding ElementName=clipMask}" Stretch="None" />
            </Grid.OpacityMask>
            <Border x:Name="clipMask" Background="White" CornerRadius="10" />
            <ScrollViewer CanContentScroll="True"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto"
                          Background="{DynamicResource PrimaryForeground}">
                <local:SampleGrid LedCount="{Binding Path=Value, ElementName=updown}" x:Name="grid"/>
            </ScrollViewer>
            <syncfusion:UpDown HorizontalAlignment="Left" VerticalAlignment="Top" Height="25" Width="95"
                               TextAlignment="Center" MinValue="1" Value="4" NumberDecimalDigits="0" BorderThickness="0"
                               Focusable="False" Background="{DynamicResource PrimaryForeground}" x:Name="updown"/>
        </Grid>
    </Border>
</UserControl>
