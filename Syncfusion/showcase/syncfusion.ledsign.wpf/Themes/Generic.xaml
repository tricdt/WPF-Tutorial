<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:syncfusion.ledsign.wpf"
                    xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
                    xmlns:skin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
                    xmlns:shared="clr-namespace:Syncfusion.Windows.Tools.Controls;assembly=Syncfusion.Shared.Wpf"
                    xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Luna">
    <ImageSource x:Key="FillIcon">/syncfusion.ledsign.wpf;component/Assets/DropDownIcon.png</ImageSource>
    <Style x:Key="DropdownStyle" TargetType="local:FillDropdownItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:FillDropdownItem">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <shared:SplitButtonAdv x:Name="dropdownSplitter"
                                               BorderThickness="1"
                                               Label=""
                                               SmallIcon="{StaticResource FillIcon}">
                            <syncfusion:RibbonListBox x:Name="FillList"
                                     HorizontalAlignment="Left"
                                     VerticalAlignment="Center"
                                     HorizontalContentAlignment="Left"
                                     VerticalContentAlignment="Center"
                                     BorderBrush="White"
                                     BorderThickness="0"
                                     SelectionMode="Single"
                                     ItemsSource="{TemplateBinding ItemsSource}"
                                     SelectedItem="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=SelectedItem}">
                            </syncfusion:RibbonListBox>
                        </shared:SplitButtonAdv>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style BasedOn="{StaticResource DropdownStyle}" TargetType="local:FillDropdownItem"/>
</ResourceDictionary>