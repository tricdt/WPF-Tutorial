<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
                    xmlns:local="clr-namespace:DevExpress.Xpf.DemoCenterBase.Helpers">
    <Style TargetType="{x:Type local:ImageButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ImageButton}">
                    <DockPanel x:Name="panel" Background="Transparent">
                        <Image x:Name="image"
                               DockPanel.Dock="Top"
                               Source="{TemplateBinding Image}"
                               Width="{TemplateBinding ImageWidth}"
                               HorizontalAlignment="Center" />
                        <TextBlock x:Name="text"
                                   Visibility="{DXBinding @p.Text eq null ? `Collapsed` : `Visible`}"
                                   Text="{TemplateBinding Text}"
                                   Foreground="{TemplateBinding TextForeground}"
                                   VerticalAlignment="Bottom"
                                   HorizontalAlignment="Center"/>
                    </DockPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="image" Property="Source" Value="{DXBinding @p.ImageHover}" />
                            <Setter TargetName="text" Property="Foreground" Value="{DXBinding @p.TextForegroundHover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>