<UserControl x:Class="DevExpress.Xpf.DemoBase.FillDatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <GroupBox Header="Fill Database">
        <StackPanel>
            <TextBlock Margin="0,0,0,8">Current Number of Records in Database: <Bold><Run Text="{Binding CurrentNumberOfRecords, Mode=OneWay}"/></Bold></TextBlock>
            <TextBlock Margin="0,0,0,8">Add more records:</TextBlock>
            <Grid HorizontalAlignment="Stretch" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{DXBinding '@e(trackbarEdit).Minimum / 1000 +`K`'}" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,0,0" />
                <dxe:TrackBarEdit Margin="6,0,6,0" x:Name="trackbarEdit" Grid.Column="1" Minimum="{Binding MinAddRecordCount}" Maximum="{Binding MaxAddRecordCount}"
                                              Value="{Binding AddRecordCount, Mode=TwoWay}" LargeStep="50000" SmallStep="50000" TickFrequency="50000"/>
                <TextBlock Text="{DXBinding '@e(trackbarEdit).Maximum / 1000 +`K`'}" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,0,0" />
            </Grid>
            <Button Command="{Binding AddRecordsCommand}" Margin="0,8,0,4">
                <TextBlock>Add <Bold><Run Text="{DXBinding '(@e(trackbarEdit).Value / 1000)', Mode=OneWay}"/>K</Bold> Records</TextBlock>
            </Button>
            <Button Command="{Binding RegenerateDatabaseCommand}">
                <TextBlock>Recreate Database with <Bold><Run Text="{DXBinding '(@e(trackbarEdit).Value / 1000)', Mode=OneWay}"/>K</Bold> Records</TextBlock>
            </Button>
        </StackPanel>
    </GroupBox>
</UserControl>
