<ResourceDictionary mc:Ignorable="cs" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:cs="colorscheme_ignorable_uri" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking" xmlns:dxdop="http://schemas.devexpress.com/winfx/2008/xaml/docking/platform" xmlns:dxdot="http://schemas.devexpress.com/winfx/2008/xaml/docking/themekeys" xmlns:dxdove="http://schemas.devexpress.com/winfx/2008/xaml/docking/visualelements" xmlns:dxi="http://schemas.devexpress.com/winfx/2008/xaml/core/internal" xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:sys="clr-namespace:System;assembly=mscorlib" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ResourceDictionary.MergedDictionaries>
    <dxt:ResourceDictionaryEx Source="/DevExpress.Xpf.Themes.Win11Light.v24.1;component/Docking/Win11Light/UICore.xaml" />
  </ResourceDictionary.MergedDictionaries>
  <!-- General -->
  <ControlTemplate x:Key="{dxdot:DockPaneElementsThemeKey ResourceKey=Template}" TargetType="dxdove:DockPane">
    <Grid Background="Transparent" dxdove:DockPane.HitTestType="Bounds" Margin="{Binding Path=LayoutItem.ActualMargin, RelativeSource={RelativeSource TemplatedParent}}" cs:id="Grid_0000">
      <Border x:Name="DockPaneContentBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding ActualBorderThickness}" CornerRadius="{TemplateBinding CaptionCornerRadius}" Margin="{TemplateBinding ActualBorderMargin}" Padding="{TemplateBinding ActualBorderPadding}" SnapsToDevicePixels="True" cs:id="Border_0000">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
          </Grid.RowDefinitions>
          <dxdove:FloatingDragWidget Background="Transparent" cs:id="FloatingDragWidget_0000">
            <dxdove:DockPaneHeaderPresenter x:Name="PART_Header" Content="{TemplateBinding Header}" Margin="{DynamicResource {dxdot:DockPaneElementsThemeKey ResourceKey=HeaderMargin}}" />
          </dxdove:FloatingDragWidget>
          <dxdove:DockPaneContentPresenter x:Name="PART_Content" Content="{TemplateBinding Content}" dx:FrameworkElementHelper.ClipCornerRadius="7" Grid.Row="1" Margin="{Binding Path=LayoutItem.ActualPadding, RelativeSource={RelativeSource TemplatedParent}}" />
        </Grid>
      </Border>
    </Grid>
    <ControlTemplate.Triggers>
      <Trigger Property="ViewStyle" Value="Light">
        <Setter Property="Margin" TargetName="PART_Header" Value="{DynamicResource {dxdot:DockPaneElementsThemeKey ResourceKey=LightViewHeaderMargin}}" />
      </Trigger>
      <DataTrigger Binding="{Binding Path=LayoutItem.IsTabPage, RelativeSource={RelativeSource Mode=Self}}" Value="True">
        <Setter Property="BorderThickness" TargetName="DockPaneContentBorder" Value="{Binding Path=ActualBorderThickness, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToBottomThicknessConverter}}" />
      </DataTrigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <ControlTemplate x:Key="{dxdot:DockElementsThemeKey ResourceKey=SplitterTemplate}" TargetType="dxdove:Splitter">
    <Grid Background="Transparent" cs:id="Grid_0000">
      <dx:DXBorder x:Name="PART_SplitThumb" Background="{TemplateBinding Background}" EnableDPICorrection="False" Margin="{TemplateBinding Padding}" cs:id="DXBorder_0000" />
      <Border Background="{DynamicResource {dxdot:BrushElementsThemeKey ResourceKey=MoveTargetBackground}}" Opacity="0.15" Visibility="{Binding Path=IsDragDropOver, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
    </Grid>
  </ControlTemplate>
  <DataTemplate x:Key="{dxdot:DockPaneElementsThemeKey ResourceKey=Header}">
    <Grid DataContext="{Binding LayoutItem}">
      <Border x:Name="PART_CaptionBackground" CornerRadius="6,6,0,0" dxdove:DockPane.HitTestType="Header">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <dxdove:TemplatedCaptionControl x:Name="PART_CaptionControlPresenter" FontWeight="Bold" LayoutItem="{Binding}" Margin="8,6" />
          <dxdove:PanelControlBoxControl x:Name="PART_ControlBox" ButtonHeight="16" ButtonStyle="{DynamicResource {dxdot:DockPaneElementsThemeKey ResourceKey=DockPaneButtonStyle}}" ButtonWidth="22" CloseButtonTemplate="{DynamicResource {dxdot:DockPaneElementsThemeKey ResourceKey=CloseButton}}" CollapseButtonTemplate="{DynamicResource {dxdot:DockPaneElementsThemeKey ResourceKey=CollapseButton}}" ExpandButtonTemplate="{DynamicResource {dxdot:DockPaneElementsThemeKey ResourceKey=ExpandButton}}" Grid.Column="1" HideButtonTemplate="{DynamicResource {dxdot:DockPaneElementsThemeKey ResourceKey=HideButton}}" LayoutItem="{Binding}" Margin="6" MaximizeButtonTemplate="{DynamicResource {dxdot:DockPaneElementsThemeKey ResourceKey=MaximizeButton}}" MinimizeButtonTemplate="{DynamicResource {dxdot:DockPaneElementsThemeKey ResourceKey=MinimizeButton}}" PinButtonTemplate="{DynamicResource {dxdot:DockPaneElementsThemeKey ResourceKey=PinButton}}" RestoreButtonTemplate="{DynamicResource {dxdot:DockPaneElementsThemeKey ResourceKey=RestoreButton}}" UseLayoutRounding="True" />
        </Grid>
      </Border>
    </Grid>
    <DataTemplate.Triggers>
      <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
        <Setter Property="Height" TargetName="PART_CaptionBackground" Value="44" />
        <Setter Property="Margin" TargetName="PART_CaptionControlPresenter" Value="14" />
        <Setter Property="ButtonHeight" TargetName="PART_ControlBox" Value="44" />
        <Setter Property="ButtonWidth" TargetName="PART_ControlBox" Value="44" />
        <Setter Property="Margin" TargetName="PART_ControlBox" Value="0" />
      </Trigger>
    </DataTemplate.Triggers>
  </DataTemplate>
</ResourceDictionary>