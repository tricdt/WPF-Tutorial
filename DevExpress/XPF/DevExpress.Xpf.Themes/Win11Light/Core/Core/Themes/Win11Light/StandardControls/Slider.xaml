<ResourceDictionary mc:Ignorable="cs" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:cs="colorscheme_ignorable_uri" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxi="http://schemas.devexpress.com/winfx/2008/xaml/core/internal" xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:sys="clr-namespace:System;assembly=mscorlib" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ResourceDictionary.MergedDictionaries>
    <dxt:ResourceDictionaryEx Source="/DevExpress.Xpf.Themes.Win11Light.v24.1;component/Core/Core/Themes/UICore.xaml" />
  </ResourceDictionary.MergedDictionaries>
  <!-- General -->
  <Style x:Key="SliderRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
    <Setter Property="Background" Value="Transparent" cs:id="Setter_0000" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type RepeatButton}">
          <Border Background="{TemplateBinding Background}" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <!-- Thumb -->
  <Style x:Key="VerticalSliderThumbStyle" TargetType="{x:Type Thumb}">
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Grid x:Name="Root">
            <Border x:Name="outter" Background="#FFFFFFFF" BorderBrush="#FFD0D0D0" BorderThickness="1" CornerRadius="99" Height="22" HorizontalAlignment="Center" VerticalAlignment="Center" Width="22" cs:id="Border_0001" x:Uid="@Background=Button.SimpleButtonBackground;BorderBrush=Border" />
            <Border x:Name="Gradient" BorderThickness="1" CornerRadius="99" Height="22" HorizontalAlignment="Center" VerticalAlignment="Center" Width="22">
              <Border.BorderBrush cs:id="Border.BorderBrush_0000">
                <LinearGradientBrush EndPoint="0,1" Opacity="0.85" StartPoint="0,0" cs:id="LinearGradientBrush_0000">
                  <LinearGradientBrush.GradientStops>
                    <GradientStop Color="#FFD0D0D0" Offset="0" cs:id="GradientStop_0000" x:Uid="@Color=Border" />
                    <GradientStop Color="#FFD0D0D0" Offset="0.6" cs:id="GradientStop_0001" x:Uid="@Color=Border" />
                    <GradientStop Color="#88777777" Offset="1" cs:id="GradientStop_0003" />
                  </LinearGradientBrush.GradientStops>
                </LinearGradientBrush>
              </Border.BorderBrush>
            </Border>
            <Border x:Name="inner" CornerRadius="99" Height="14" HorizontalAlignment="Center" VerticalAlignment="Center" Width="14" cs:id="Border_0000">
              <Border.Background>
                <RadialGradientBrush cs:id="RadialGradientBrush_0000">
                  <GradientStop x:Name="GradientStop1" Color="#FF005FBA" Offset="0.8" cs:id="GradientStop_0004" x:Uid="@Color=Accent" />
                  <GradientStop x:Name="GradientStop2" Color="Transparent" Offset="0.8" cs:id="GradientStop_0005" />
                  <GradientStop x:Name="GradientStop3" Color="Transparent" Offset="1" cs:id="GradientStop_0002" />
                </RadialGradientBrush>
              </Border.Background>
            </Border>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Trigger.EnterActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation Duration="0:0:0.09" From="0.8" Storyboard.TargetName="GradientStop1" Storyboard.TargetProperty="Offset" To="1" />
                    <DoubleAnimation Duration="0:0:0.09" From="0.8" Storyboard.TargetName="GradientStop2" Storyboard.TargetProperty="Offset" To="1" />
                  </Storyboard>
                </BeginStoryboard>
              </Trigger.EnterActions>
              <Trigger.ExitActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation Duration="0:0:0.09" From="1" Storyboard.TargetName="GradientStop1" Storyboard.TargetProperty="Offset" To="0.8" />
                    <DoubleAnimation Duration="0:0:0.09" From="1" Storyboard.TargetName="GradientStop2" Storyboard.TargetProperty="Offset" To="0.8" />
                  </Storyboard>
                </BeginStoryboard>
              </Trigger.ExitActions>
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
              <Trigger.EnterActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation Duration="0:0:0.19" From="1" Storyboard.TargetName="GradientStop1" Storyboard.TargetProperty="Offset" To="0.7" />
                    <DoubleAnimation Duration="0:0:0.19" From="1" Storyboard.TargetName="GradientStop2" Storyboard.TargetProperty="Offset" To="0.7" />
                  </Storyboard>
                </BeginStoryboard>
              </Trigger.EnterActions>
              <Trigger.ExitActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation Duration="0:0:0.19" From="0.7" Storyboard.TargetName="GradientStop1" Storyboard.TargetProperty="Offset" To="0.8" />
                    <DoubleAnimation Duration="0:0:0.19" From="0.7" Storyboard.TargetName="GradientStop2" Storyboard.TargetProperty="Offset" To="0.8" />
                  </Storyboard>
                </BeginStoryboard>
              </Trigger.ExitActions>
            </Trigger>
            <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
              <Setter Property="Margin" TargetName="Root" Value="-22" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="HorizontalSliderThumbStyle" TargetType="{x:Type Thumb}">
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Grid x:Name="Root">
            <Border x:Name="outter" Background="#FFFFFFFF" BorderBrush="#FFD0D0D0" BorderThickness="1" CornerRadius="99" Height="22" HorizontalAlignment="Center" VerticalAlignment="Center" Width="22" cs:id="Border_0001" x:Uid="@Background=Button.SimpleButtonBackground;BorderBrush=Border" />
            <Border x:Name="Gradient" BorderThickness="1" CornerRadius="99" Height="22" HorizontalAlignment="Center" VerticalAlignment="Center" Width="22">
              <Border.BorderBrush cs:id="Border.BorderBrush_0000">
                <LinearGradientBrush EndPoint="0,1" Opacity="0.85" StartPoint="0,0" cs:id="LinearGradientBrush_0000">
                  <LinearGradientBrush.GradientStops>
                    <GradientStop Color="#FFD0D0D0" Offset="0" cs:id="GradientStop_0000" x:Uid="@Color=Border" />
                    <GradientStop Color="#FFD0D0D0" Offset="0.6" cs:id="GradientStop_0001" x:Uid="@Color=Border" />
                    <GradientStop Color="#88777777" Offset="1" cs:id="GradientStop_0003" />
                  </LinearGradientBrush.GradientStops>
                </LinearGradientBrush>
              </Border.BorderBrush>
            </Border>
            <Border x:Name="inner" CornerRadius="99" Height="14" HorizontalAlignment="Center" VerticalAlignment="Center" Width="14" cs:id="Border_0000">
              <Border.Background>
                <RadialGradientBrush cs:id="RadialGradientBrush_0000">
                  <GradientStop x:Name="GradientStop1" Color="#FF005FBA" Offset="0.8" cs:id="GradientStop_0004" x:Uid="@Color=Accent" />
                  <GradientStop x:Name="GradientStop2" Color="Transparent" Offset="0.8" cs:id="GradientStop_0005" />
                  <GradientStop x:Name="GradientStop3" Color="Transparent" Offset="1" cs:id="GradientStop_0002" />
                </RadialGradientBrush>
              </Border.Background>
            </Border>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Trigger.EnterActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation Duration="0:0:0.09" From="0.8" Storyboard.TargetName="GradientStop1" Storyboard.TargetProperty="Offset" To="1" />
                    <DoubleAnimation Duration="0:0:0.09" From="0.8" Storyboard.TargetName="GradientStop2" Storyboard.TargetProperty="Offset" To="1" />
                  </Storyboard>
                </BeginStoryboard>
              </Trigger.EnterActions>
              <Trigger.ExitActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation Duration="0:0:0.09" From="1" Storyboard.TargetName="GradientStop1" Storyboard.TargetProperty="Offset" To="0.8" />
                    <DoubleAnimation Duration="0:0:0.09" From="1" Storyboard.TargetName="GradientStop2" Storyboard.TargetProperty="Offset" To="0.8" />
                  </Storyboard>
                </BeginStoryboard>
              </Trigger.ExitActions>
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
              <Trigger.EnterActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation Duration="0:0:0.19" From="1" Storyboard.TargetName="GradientStop1" Storyboard.TargetProperty="Offset" To="0.7" />
                    <DoubleAnimation Duration="0:0:0.19" From="1" Storyboard.TargetName="GradientStop2" Storyboard.TargetProperty="Offset" To="0.7" />
                  </Storyboard>
                </BeginStoryboard>
              </Trigger.EnterActions>
              <Trigger.ExitActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation Duration="0:0:0.19" From="0.7" Storyboard.TargetName="GradientStop1" Storyboard.TargetProperty="Offset" To="0.8" />
                    <DoubleAnimation Duration="0:0:0.19" From="0.7" Storyboard.TargetName="GradientStop2" Storyboard.TargetProperty="Offset" To="0.8" />
                  </Storyboard>
                </BeginStoryboard>
              </Trigger.ExitActions>
            </Trigger>
            <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
              <Setter Property="Margin" TargetName="Root" Value="-22" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="HorizontalSliderUpThumbStyle" BasedOn="{StaticResource HorizontalSliderThumbStyle}" TargetType="{x:Type Thumb}" />
  <Style x:Key="HorizontalSliderDownThumbStyle" BasedOn="{StaticResource HorizontalSliderThumbStyle}" TargetType="{x:Type Thumb}" />
  <Style x:Key="VerticalSliderLeftThumbStyle" BasedOn="{StaticResource VerticalSliderThumbStyle}" TargetType="{x:Type Thumb}" />
  <Style x:Key="VerticalSliderRightThumbStyle" BasedOn="{StaticResource VerticalSliderThumbStyle}" TargetType="{x:Type Thumb}" />
  <!-- Slider -->
  <Style x:Key="FocusVisualBorder">
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate>
          <Border BorderBrush="Black" BorderThickness="2" CornerRadius="6" Margin="-3" cs:id="Border_0000" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxt:DefaultStyleThemeKey FullName=System.Windows.Controls.Slider}" TargetType="{x:Type Slider}">
    <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisualBorder}" />
    <Setter Property="Foreground" Value="#FF1A1A1A" cs:id="Setter_0000" x:Uid="@Value=Foreground.Primary" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Stylus.IsPressAndHoldEnabled" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Slider}">
          <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Width="{TemplateBinding Width}">
            <Grid x:Name="Root" MinHeight="22">
              <TickBar x:Name="TopTick" Fill="#FF1A1A1A" Height="4" Panel.ZIndex="1" Placement="Top" VerticalAlignment="Top" Visibility="Collapsed" cs:id="TickBar_0000" x:Uid="@Fill=Foreground.Primary" />
              <Grid Margin="1,0,1,0" VerticalAlignment="Center">
                <Border x:Name="TrackBackground" Background="#FFD0D0D0" Height="2" VerticalAlignment="Center" cs:id="Border_0000" x:Uid="@Background=Border" />
                <Border x:Name="FocusState" Background="#FF005FBA" Visibility="Collapsed" cs:id="Border_0001" x:Uid="@Background=Accent" />
                <Canvas>
                  <Rectangle x:Name="PART_SelectionRange" Fill="#FF1A1A1A" Height="2" Opacity="0" cs:id="Rectangle_0000" x:Uid="@Fill=Foreground.Primary" />
                </Canvas>
              </Grid>
              <Track x:Name="PART_Track" Panel.ZIndex="2" VerticalAlignment="Center">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}" />
                </Track.DecreaseRepeatButton>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}" />
                </Track.IncreaseRepeatButton>
                <Track.Thumb>
                  <Thumb x:Name="Thumb" Background="#FFD0D0D0" Height="{TemplateBinding Height}" Style="{StaticResource HorizontalSliderThumbStyle}" cs:id="Thumb_0000" x:Uid="@Background=Border" />
                </Track.Thumb>
              </Track>
              <TickBar x:Name="BottomTick" Fill="#FF1A1A1A" Height="4" Panel.ZIndex="1" Placement="Bottom" VerticalAlignment="Bottom" Visibility="Collapsed" cs:id="TickBar_0001" x:Uid="@Fill=Foreground.Primary" />
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="TickPlacement" Value="TopLeft">
              <Setter Property="Style" TargetName="Thumb" Value="{StaticResource HorizontalSliderUpThumbStyle}" />
              <Setter Property="Visibility" TargetName="TopTick" Value="Visible" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="BottomRight">
              <Setter Property="Visibility" TargetName="BottomTick" Value="Visible" />
              <Setter Property="Style" TargetName="Thumb" Value="{StaticResource HorizontalSliderDownThumbStyle}" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="Both">
              <Setter Property="Visibility" TargetName="BottomTick" Value="Visible" />
              <Setter Property="Visibility" TargetName="TopTick" Value="Visible" />
            </Trigger>
            <Trigger Property="IsSelectionRangeEnabled" Value="True">
              <Setter Property="Opacity" TargetName="PART_SelectionRange" Value="1" />
            </Trigger>
            <Trigger Property="IsFocused" Value="True">
              <Setter Property="Visibility" TargetName="FocusState" Value="Visible" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" TargetName="Root" Value="0.35" />
            </Trigger>
            <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
              <Setter Property="MinHeight" TargetName="Root" Value="44" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="Orientation" Value="Vertical">
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type Slider}">
              <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Height="{TemplateBinding Height}">
                <Grid x:Name="Root" MinWidth="22">
                  <TickBar x:Name="LeftTick" Fill="#FF1A1A1A" HorizontalAlignment="Left" Panel.ZIndex="1" Placement="Left" Visibility="Collapsed" Width="4" cs:id="TickBar_0002" x:Uid="@Fill=Foreground.Primary" />
                  <Grid HorizontalAlignment="Center" Margin="0,1,0,1">
                    <Border Name="TrackBackground" Background="#FFD0D0D0" Width="2" cs:id="Border_0002" x:Uid="@Background=Border" />
                    <Border x:Name="FocusState" Background="#FF005FBA" Visibility="Collapsed" cs:id="Border_0003" x:Uid="@Background=Accent" />
                    <Canvas>
                      <Rectangle x:Name="PART_SelectionRange" Fill="#FF1A1A1A" Opacity="0" Width="2" cs:id="Rectangle_0001" x:Uid="@Fill=Foreground.Primary" />
                    </Canvas>
                  </Grid>
                  <Track x:Name="PART_Track" Panel.ZIndex="2">
                    <Track.DecreaseRepeatButton>
                      <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}" />
                    </Track.DecreaseRepeatButton>
                    <Track.IncreaseRepeatButton>
                      <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}" />
                    </Track.IncreaseRepeatButton>
                    <Track.Thumb>
                      <Thumb x:Name="Thumb" Background="#FFD0D0D0" Style="{StaticResource VerticalSliderThumbStyle}" Width="{TemplateBinding Width}" cs:id="Thumb_0001" x:Uid="@Background=Border" />
                    </Track.Thumb>
                  </Track>
                  <TickBar x:Name="RightTick" Fill="#FF1A1A1A" HorizontalAlignment="Right" Panel.ZIndex="1" Placement="Right" Visibility="Collapsed" Width="4" cs:id="TickBar_0003" x:Uid="@Fill=Foreground.Primary" />
                </Grid>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="TickPlacement" Value="TopLeft">
                  <Setter Property="Visibility" TargetName="LeftTick" Value="Visible" />
                  <Setter Property="Style" TargetName="Thumb" Value="{StaticResource VerticalSliderLeftThumbStyle}" />
                </Trigger>
                <Trigger Property="TickPlacement" Value="BottomRight">
                  <Setter Property="Visibility" TargetName="RightTick" Value="Visible" />
                  <Setter Property="Style" TargetName="Thumb" Value="{StaticResource VerticalSliderRightThumbStyle}" />
                </Trigger>
                <Trigger Property="TickPlacement" Value="Both">
                  <Setter Property="Visibility" TargetName="LeftTick" Value="Visible" />
                  <Setter Property="Visibility" TargetName="RightTick" Value="Visible" />
                </Trigger>
                <Trigger Property="IsSelectionRangeEnabled" Value="True">
                  <Setter Property="Opacity" TargetName="PART_SelectionRange" Value="1" />
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                  <Setter Property="Visibility" TargetName="FocusState" Value="Visible" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter Property="Opacity" TargetName="Root" Value="0.35" />
                </Trigger>
                <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
                  <Setter Property="MinWidth" TargetName="Root" Value="44" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Trigger>
    </Style.Triggers>
  </Style>
</ResourceDictionary>