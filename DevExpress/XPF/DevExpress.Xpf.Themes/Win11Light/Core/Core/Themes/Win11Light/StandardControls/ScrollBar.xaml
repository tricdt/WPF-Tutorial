<ResourceDictionary mc:Ignorable="cs" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:cs="colorscheme_ignorable_uri" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxi="http://schemas.devexpress.com/winfx/2008/xaml/core/internal" xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" xmlns:dxmvvmi="http://schemas.devexpress.com/winfx/2008/xaml/mvvm/internal" xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:sys="clr-namespace:System;assembly=mscorlib" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ResourceDictionary.MergedDictionaries>
    <dxt:ResourceDictionaryEx Source="/DevExpress.Xpf.Themes.Win11Light.v24.1;component/Core/Core/Themes/UICore.xaml" />
    <dxt:ResourceDictionaryEx Source="/DevExpress.Xpf.Themes.Win11Light.v24.1;component/Core/Core/Themes/Win11Light/StandardControls/ScrollBarTouch.xaml" />
  </ResourceDictionary.MergedDictionaries>
  <!-- General -->
  <ControlTemplate x:Key="{dxt:CommonThemeKey ResourceKey=VerticalScrollBarPageButtonTemplate}" TargetType="{x:Type RepeatButton}">
    <Border Background="Transparent" cs:id="Border_0001">
      <Rectangle Fill="{TemplateBinding Background}" Margin="{DynamicResource {dxt:CommonThemeKey ResourceKey=ScrollBarVerticalThumbMargin}}" Opacity="0.38" />
    </Border>
  </ControlTemplate>
  <ControlTemplate x:Key="{dxt:CommonThemeKey ResourceKey=HorizontalScrollBarPageButtonTemplate}" TargetType="{x:Type RepeatButton}">
    <Border Background="Transparent" cs:id="Border_0001">
      <Rectangle Fill="{TemplateBinding Background}" Margin="{DynamicResource {dxt:CommonThemeKey ResourceKey=ScrollBarHorizontalThumbMargin}}" Opacity="0.38" />
    </Border>
  </ControlTemplate>
  <Style x:Key="ScrollBarPageButtonStyle" TargetType="{x:Type RepeatButton}">
    <Setter Property="Focusable" Value="False" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Style.Triggers>
      <Trigger Property="IsPressed" Value="True">
        <Setter Property="Background" Value="#0A000000" cs:id="Setter_0000" x:Uid="@Value=ListItem.SelectionBackground" />
      </Trigger>
    </Style.Triggers>
  </Style>
  <Style x:Key="VerticalScrollBarPageButtonStyle" BasedOn="{StaticResource ScrollBarPageButtonStyle}" TargetType="{x:Type RepeatButton}">
    <Setter Property="Template" Value="{DynamicResource {dxt:CommonThemeKey ResourceKey=VerticalScrollBarPageButtonTemplate}}" />
  </Style>
  <Style x:Key="HorizontalScrollBarPageButtonStyle" BasedOn="{StaticResource ScrollBarPageButtonStyle}" TargetType="{x:Type RepeatButton}">
    <Setter Property="Template" Value="{DynamicResource {dxt:CommonThemeKey ResourceKey=HorizontalScrollBarPageButtonTemplate}}" />
  </Style>
  <!-- Button -->
  <Style x:Key="ScrollBarButtonStyle" TargetType="{x:Type RepeatButton}">
    <Setter Property="Focusable" Value="False" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Background" Value="Transparent" cs:id="Setter_0001" />
    <Setter Property="BorderBrush" Value="Transparent" cs:id="Setter_0002" />
    <Setter Property="Foreground" Value="#FF646464" cs:id="Setter_0003" x:Uid="@Value=Foreground.Secondary" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type RepeatButton}">
          <Grid>
            <Rectangle x:Name="IsDefault" Fill="{TemplateBinding Background}" Stroke="{TemplateBinding BorderBrush}" StrokeThickness="{TemplateBinding BorderThickness}" />
            <ContentPresenter />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" TargetName="IsDefault" Value="0.38" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Foreground" Value="#FF1A1A1A" cs:id="Setter_0008" x:Uid="@Value=Foreground.Primary" />
      </Trigger>
      <Trigger Property="IsPressed" Value="True">
        <Setter Property="Foreground" Value="#FF626262" cs:id="Setter_0009" x:Uid="@Value=Foreground.Muted" />
      </Trigger>
      <Trigger Property="IsEnabled" Value="False">
        <Setter Property="Foreground" Value="#FFE3E3E3" cs:id="Setter_0000" x:Uid="@Value=Button.SimpleButtonBorder" />
      </Trigger>
    </Style.Triggers>
  </Style>
  <!-- Thumb -->
  <ControlTemplate x:Key="{dxt:CommonThemeKey ResourceKey=VerticalScrollBarThumbTemplate}" TargetType="{x:Type Thumb}">
    <Grid Background="Transparent" cs:id="Grid_0000">
      <Grid.RowDefinitions>
        <RowDefinition x:Name="Row1" Height="4" />
        <RowDefinition x:Name="Row2" Height="*" />
        <RowDefinition x:Name="Row3" Height="4" />
      </Grid.RowDefinitions>
      <Rectangle x:Name="CornerSide1" Fill="{TemplateBinding Background}" Grid.Row="0" Height="4" Margin="2,-1,2,0" RadiusX="3" RadiusY="3" Width="{TemplateBinding Height}" cs:id="Rectangle_0000" />
      <Rectangle x:Name="IsDefault" Fill="{TemplateBinding Background}" Grid.Row="1" Height="{TemplateBinding Height}" Margin="2,-3,2,-3" MinHeight="{TemplateBinding MinHeight}" MinWidth="{TemplateBinding MinWidth}" Width="{TemplateBinding Width}" />
      <Rectangle x:Name="CornerSide2" Fill="{TemplateBinding Background}" Grid.Row="2" Height="4" Margin="2,0,2,-2" RadiusX="3" RadiusY="3" Width="{TemplateBinding Height}" cs:id="Rectangle_0001" />
    </Grid>
    <ControlTemplate.Triggers>
      <Trigger Property="IsEnabled" Value="False">
        <Setter Property="Fill" TargetName="IsDefault" Value="#FFE3E3E3" cs:id="Setter_0000" x:Uid="@Value=Button.SimpleButtonBorder" />
        <Setter Property="Fill" TargetName="CornerSide1" Value="#FFE3E3E3" cs:id="Setter_0001" x:Uid="@Value=Button.SimpleButtonBorder" />
        <Setter Property="Fill" TargetName="CornerSide2" Value="#FFE3E3E3" cs:id="Setter_0002" x:Uid="@Value=Button.SimpleButtonBorder" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <ControlTemplate x:Key="{dxt:CommonThemeKey ResourceKey=HorizontalScrollBarThumbTemplate}" TargetType="{x:Type Thumb}">
    <Grid Background="Transparent" cs:id="Border_0000">
      <Grid.ColumnDefinitions>
        <ColumnDefinition x:Name="Column1" Width="4" />
        <ColumnDefinition x:Name="Column2" Width="*" />
        <ColumnDefinition x:Name="Column3" Width="4" />
      </Grid.ColumnDefinitions>
      <Rectangle x:Name="CornerSide1" Fill="{TemplateBinding Background}" Grid.Column="0" Height="{TemplateBinding Width}" Margin="-1,2,0,2" RadiusX="3" RadiusY="3" Width="4" cs:id="Rectangle_0000" />
      <Rectangle x:Name="IsDefault" Fill="{TemplateBinding Background}" Grid.Column="1" Height="{TemplateBinding Height}" Margin="-3,2,-3,2" MinHeight="{TemplateBinding MinHeight}" MinWidth="{TemplateBinding MinWidth}" Width="{TemplateBinding Width}" />
      <Rectangle x:Name="CornerSide2" Fill="{TemplateBinding Background}" Grid.Column="2" Height="{TemplateBinding Width}" Margin="0,2,-2,2" RadiusX="3" RadiusY="3 " Width="4" cs:id="Rectangle_0001" />
    </Grid>
    <ControlTemplate.Triggers>
      <Trigger Property="IsEnabled" Value="False">
        <Setter Property="Fill" TargetName="IsDefault" Value="#FFE3E3E3" cs:id="Setter_0000" x:Uid="@Value=Button.SimpleButtonBorder" />
        <Setter Property="Fill" TargetName="CornerSide1" Value="#FFE3E3E3" cs:id="Setter_0001" x:Uid="@Value=Button.SimpleButtonBorder" />
        <Setter Property="Fill" TargetName="CornerSide2" Value="#FFE3E3E3" cs:id="Setter_0002" x:Uid="@Value=Button.SimpleButtonBorder" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
    <Setter Property="MinHeight" Value="6" />
    <Setter Property="MinWidth" Value="6" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Background" Value="#FF626262" cs:id="Setter_0003" x:Uid="@Value=Foreground.Muted" />
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="#FF646464" cs:id="Setter_0000" x:Uid="@Value=Foreground.Secondary" />
        <Setter Property="BorderBrush" Value="#FF646464" cs:id="Setter_0004" x:Uid="@Value=Foreground.Secondary" />
      </Trigger>
      <Trigger Property="IsDragging" Value="True">
        <Setter Property="Background" Value="#FF626262" cs:id="Setter_0001" x:Uid="@Value=Foreground.Muted" />
        <Setter Property="BorderBrush" Value="#FF626262" cs:id="Setter_0005" x:Uid="@Value=Foreground.Muted" />
      </Trigger>
    </Style.Triggers>
  </Style>
  <Style x:Key="{dxt:CommonThemeKey ResourceKey=VerticalScrollBarThumbStyle}" BasedOn="{StaticResource ScrollBarThumb}" TargetType="{x:Type Thumb}">
    <Setter Property="Template" Value="{DynamicResource {dxt:CommonThemeKey ResourceKey=VerticalScrollBarThumbTemplate}}" />
    <Setter Property="Width" Value="10" />
  </Style>
  <Style x:Key="{dxt:CommonThemeKey ResourceKey=HorizontalScrollBarThumbStyle}" BasedOn="{StaticResource ScrollBarThumb}" TargetType="{x:Type Thumb}">
    <Setter Property="Template" Value="{DynamicResource {dxt:CommonThemeKey ResourceKey=HorizontalScrollBarThumbTemplate}}" />
    <Setter Property="Height" Value="10" />
  </Style>
  <!-- Thumb Size Keys -->
  <Thickness x:Key="{dxt:CommonThemeKey ResourceKey=ScrollBarVerticalThumbMargin}">3,0,3,0</Thickness>
  <Thickness x:Key="{dxt:CommonThemeKey ResourceKey=ScrollBarHorizontalThumbMargin}">0,3,0,3</Thickness>
  <!-- Corner -->
  <Style x:Key="{dxt:ScrollBarThemeKey ResourceKey=ScrollingCornerStyle}" TargetType="{x:Type Border}">
    <Setter Property="Background" Value="#FFFFFFFF" cs:id="Setter_0000" x:Uid="@Value=Button.SimpleButtonBackground" />
  </Style>
  <!-- ScrollBar -->
  <ControlTemplate x:Key="{dxt:CommonThemeKey ResourceKey=VerticalScrollBarTemplate}" TargetType="{x:Type ScrollBar}">
    <Grid UseLayoutRounding="False">
      <Border Background="{TemplateBinding Background}" Opacity="0.87" />
      <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Margin="0" />
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition x:Name="Row1" MaxHeight="12" />
          <RowDefinition x:Name="Row2" Height="*" />
          <RowDefinition x:Name="Row3" MaxHeight="12" />
        </Grid.RowDefinitions>
        <RepeatButton x:Name="PART_LineUpButton" Command="{x:Static ScrollBar.LineUpCommand}" Grid.Row="0" Style="{StaticResource ScrollBarButtonStyle}">
          <Path x:Name="LineUpButton_Arrow" Fill="{Binding ElementName=PART_LineUpButton, Path=Foreground}" Style="{StaticResource UIShape.Bold.Up}" Width="8" />
        </RepeatButton>
        <Track x:Name="PART_Track" Grid.Row="1" IsDirectionReversed="True" Margin="0,1,0,1">
          <Track.DecreaseRepeatButton>
            <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Style="{StaticResource VerticalScrollBarPageButtonStyle}" />
          </Track.DecreaseRepeatButton>
          <Track.IncreaseRepeatButton>
            <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Style="{StaticResource VerticalScrollBarPageButtonStyle}" />
          </Track.IncreaseRepeatButton>
          <Track.Thumb>
            <Thumb x:Name="Thumb" Style="{DynamicResource {dxt:CommonThemeKey ResourceKey=VerticalScrollBarThumbStyle}}" />
          </Track.Thumb>
        </Track>
        <RepeatButton x:Name="PART_LineDownButton" Command="{x:Static ScrollBar.LineDownCommand}" Grid.Row="2" Style="{StaticResource ScrollBarButtonStyle}">
          <Path x:Name="LineDownButton_Arrow" Fill="{Binding ElementName=PART_LineDownButton, Path=Foreground}" Style="{StaticResource UIShape.Bold.Down}" Width="8" />
        </RepeatButton>
      </Grid>
    </Grid>
    <ControlTemplate.Triggers>
      <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
        <Setter Property="MinHeight" TargetName="Row1" Value="22" />
        <Setter Property="MinHeight" TargetName="Row3" Value="22" />
        <Setter Property="Width" TargetName="LineUpButton_Arrow" Value="10" />
        <Setter Property="Height" TargetName="LineUpButton_Arrow" Value="5" />
        <Setter Property="Width" TargetName="LineDownButton_Arrow" Value="10" />
        <Setter Property="Height" TargetName="LineDownButton_Arrow" Value="5" />
      </Trigger>
      <DataTrigger Binding="{Binding ElementName=Thumb, Path=IsDragging}" Value="True">
        <Setter Property="Panel.ZIndex" TargetName="PART_Track" Value="1" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ElementName=PART_LineUpButton, Path=IsPressed}" Value="True">
        <Setter Property="Panel.ZIndex" TargetName="PART_LineUpButton" Value="1" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ElementName=PART_LineDownButton, Path=IsPressed}" Value="True">
        <Setter Property="Panel.ZIndex" TargetName="PART_LineDownButton" Value="1" />
      </DataTrigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <ControlTemplate x:Key="{dxt:CommonThemeKey ResourceKey=HorizontalScrollBarTemplate}" TargetType="{x:Type ScrollBar}">
    <Grid UseLayoutRounding="False">
      <Border Background="{TemplateBinding Background}" Opacity="0.87" />
      <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Margin="0" />
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition x:Name="Column1" MaxWidth="12" />
          <ColumnDefinition x:Name="Column2" Width="*" />
          <ColumnDefinition x:Name="Column3" MaxWidth="12" />
        </Grid.ColumnDefinitions>
        <RepeatButton x:Name="PART_LineLeftButton" Command="{x:Static ScrollBar.LineLeftCommand}" Grid.Column="0" Style="{StaticResource ScrollBarButtonStyle}">
          <Path x:Name="LineLeftButton_Arrow" Fill="{Binding ElementName=PART_LineLeftButton, Path=Foreground}" Height="8" Margin="0,1,0,0" Style="{StaticResource UIShape.Bold.Left}" />
        </RepeatButton>
        <Track x:Name="PART_Track" Grid.Column="1" Margin="1,0,1,0">
          <Track.DecreaseRepeatButton>
            <RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}" Style="{StaticResource HorizontalScrollBarPageButtonStyle}" />
          </Track.DecreaseRepeatButton>
          <Track.IncreaseRepeatButton>
            <RepeatButton Command="{x:Static ScrollBar.PageRightCommand}" Style="{StaticResource HorizontalScrollBarPageButtonStyle}" />
          </Track.IncreaseRepeatButton>
          <Track.Thumb>
            <Thumb x:Name="Thumb" Style="{DynamicResource {dxt:CommonThemeKey ResourceKey=HorizontalScrollBarThumbStyle}}" />
          </Track.Thumb>
        </Track>
        <RepeatButton x:Name="PART_LineRightButton" Command="{x:Static ScrollBar.LineRightCommand}" Grid.Column="2" Style="{StaticResource ScrollBarButtonStyle}">
          <Path x:Name="LineRightButton_Arrow" Fill="{Binding ElementName=PART_LineRightButton, Path=Foreground}" Height="8" Margin="0,1,0,0" Style="{StaticResource UIShape.Bold.Right}" />
        </RepeatButton>
      </Grid>
    </Grid>
    <ControlTemplate.Triggers>
      <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
        <Setter Property="MinWidth" TargetName="Column1" Value="22" />
        <Setter Property="MinWidth" TargetName="Column3" Value="22" />
        <Setter Property="Width" TargetName="LineLeftButton_Arrow" Value="5" />
        <Setter Property="Height" TargetName="LineLeftButton_Arrow" Value="10" />
        <Setter Property="Width" TargetName="LineRightButton_Arrow" Value="5" />
        <Setter Property="Height" TargetName="LineRightButton_Arrow" Value="10" />
      </Trigger>
      <DataTrigger Binding="{Binding ElementName=Thumb, Path=IsDragging}" Value="True">
        <Setter Property="Panel.ZIndex" TargetName="PART_Track" Value="1" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ElementName=PART_LineLeftButton, Path=IsPressed}" Value="True">
        <Setter Property="Panel.ZIndex" TargetName="PART_LineLeftButton" Value="1" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ElementName=PART_LineRightButton, Path=IsPressed}" Value="True">
        <Setter Property="Panel.ZIndex" TargetName="PART_LineRightButton" Value="1" />
      </DataTrigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <Style x:Key="{dxt:DefaultStyleThemeKey FullName=System.Windows.Controls.Primitives.ScrollBar}" TargetType="{x:Type ScrollBar}">
    <Setter Property="Background" Value="#FFFFFFFF" cs:id="Setter_0001" x:Uid="@Value=Button.SimpleButtonBackground" />
    <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
    <Setter Property="Stylus.IsPressAndHoldEnabled" Value="False" />
    <Setter Property="Width" Value="12" />
    <Setter Property="Height" Value="Auto" />
    <Setter Property="dx:ScrollBarExtensions.ListeningScrollBarThumbDragDelta" Value="True" />
    <Setter Property="Template" Value="{DynamicResource {dxt:CommonThemeKey ResourceKey=VerticalScrollBarTemplate}}" />
    <Style.Triggers>
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="Orientation" Value="Vertical" />
          <Condition Property="dx:ScrollBarExtensions.IsTouchScrollBarMode" Value="True" />
        </MultiTrigger.Conditions>
        <MultiTrigger.Setters>
          <Setter Property="Width" Value="17" />
          <Setter Property="MinWidth" Value="17" />
          <Setter Property="Template" Value="{DynamicResource {dxt:ScrollBarThemeKey ResourceKey=ScrollBarVertTouchTemplate}}" />
        </MultiTrigger.Setters>
      </MultiTrigger>
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="Orientation" Value="Horizontal" />
          <Condition Property="dx:ScrollBarExtensions.IsTouchScrollBarMode" Value="False" />
        </MultiTrigger.Conditions>
        <MultiTrigger.Setters>
          <Setter Property="Background" Value="#FFFFFFFF" cs:id="Setter_0000" x:Uid="@Value=Button.SimpleButtonBackground" />
          <Setter Property="Width" Value="Auto" />
          <Setter Property="Height" Value="12" />
          <Setter Property="BorderThickness" Value="0,1,0,0" />
          <Setter Property="Template" Value="{DynamicResource {dxt:CommonThemeKey ResourceKey=HorizontalScrollBarTemplate}}" />
        </MultiTrigger.Setters>
      </MultiTrigger>
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="Orientation" Value="Horizontal" />
          <Condition Property="dx:ScrollBarExtensions.IsTouchScrollBarMode" Value="True" />
        </MultiTrigger.Conditions>
        <MultiTrigger.Setters>
          <Setter Property="Background" Value="#FFFFFFFF" cs:id="Setter_0004" x:Uid="@Value=Button.SimpleButtonBackground" />
          <Setter Property="BorderBrush" Value="#FFD0D0D0" cs:id="Setter_0005" x:Uid="@Value=Border" />
          <Setter Property="Width" Value="Auto" />
          <Setter Property="Height" Value="17" />
          <Setter Property="Template" Value="{DynamicResource {dxt:ScrollBarThemeKey ResourceKey=ScrollBarHorzTouchTemplate}}" />
        </MultiTrigger.Setters>
      </MultiTrigger>
      <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
        <Setter Property="MinHeight" Value="24" />
        <Setter Property="MinWidth" Value="24" />
      </Trigger>
      <Trigger Property="dx:ScrollBarExtensions.ScrollBarMode" Value="TouchOverlap">
        <Setter Property="dx:ScrollBarExtensions.IsTouchScrollBarMode" Value="True" />
        <Setter Property="dx:ScrollBarExtensions.ScrollViewerMouseMoved" Value="{Binding Path=(dx:ScrollBarExtensions.ScrollViewerMouseMoved), RelativeSource={RelativeSource TemplatedParent}}" />
        <Setter Property="dx:ScrollBarExtensions.ScrollViewerSizeChanged" Value="{Binding Path=(dx:ScrollBarExtensions.ScrollViewerSizeChanged), RelativeSource={RelativeSource TemplatedParent}}" />
      </Trigger>
    </Style.Triggers>
  </Style>
</ResourceDictionary>