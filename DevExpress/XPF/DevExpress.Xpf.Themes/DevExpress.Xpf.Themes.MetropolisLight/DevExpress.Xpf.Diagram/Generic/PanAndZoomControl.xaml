<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" xmlns:dxdiag="http://schemas.devexpress.com/winfx/2008/xaml/diagram" xmlns:dxdiagn="http://schemas.devexpress.com/winfx/2008/xaml/diagram/internal" xmlns:dxdiagt="http://schemas.devexpress.com/winfx/2008/xaml/diagram/themekeys" xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars" xmlns:sys="clr-namespace:System;assembly=mscorlib" xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" xmlns:dxdst="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys">
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.Diagram.PanAndZoomClientFramePresenter}" TargetType="{x:Type dxdiag:PanAndZoomClientFramePresenter}">
    <Setter Property="BorderThickness" Value="2" />
    <Setter Property="BorderBrush" Value="{DynamicResource {dxdiagt:DiagramPanAndZoomControlThemeKeys ResourceKey=ClientFrameBorderBrush}}" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type dxdiag:PanAndZoomClientFramePresenter}">
          <Border BorderBrush="{TemplateBinding BorderBrush}" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" BorderThickness="{TemplateBinding BorderThickness}" Background="Transparent" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.Diagram.PanAndZoomPreviewFramePresenter}" TargetType="{x:Type dxdiag:PanAndZoomPreviewFramePresenter}">
    <Setter Property="BorderThickness" Value="2" />
    <Setter Property="BorderBrush" Value="{DynamicResource {dxdiagt:DiagramPanAndZoomControlThemeKeys ResourceKey=PreviewFrameBorderBrush}}" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type dxdiag:PanAndZoomPreviewFramePresenter}">
          <Border BorderBrush="{TemplateBinding BorderBrush}" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" BorderThickness="{TemplateBinding BorderThickness}" Background="Transparent" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.Diagram.PanAndZoomDiagramPresenter}" TargetType="{x:Type dxdiag:PanAndZoomDiagramPresenter}">
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="VerticalAlignment" Value="Stretch" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type dxdiag:PanAndZoomDiagramPresenter}">
          <Canvas x:Name="PART_Canvas" HorizontalAlignment="{TemplateBinding HorizontalAlignment}" VerticalAlignment="{TemplateBinding VerticalAlignment}" Background="{TemplateBinding Background}">
            <Canvas.Children>
              <dxdiag:PanAndZoomClientFramePresenter x:Name="PART_ClientFramePresenter" />
              <dxdiag:PanAndZoomPreviewFramePresenter x:Name="PART_PreviewFramePresenter" Visibility="Collapsed" />
            </Canvas.Children>
          </Canvas>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.Diagram.DiagramPanAndZoomControl}" TargetType="{x:Type dxdiag:DiagramPanAndZoomControl}">
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="VerticalAlignment" Value="Stretch" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type dxdiag:DiagramPanAndZoomControl}">
          <Grid Height="{TemplateBinding Height}" Width="{TemplateBinding Width}">
            <Grid.ColumnDefinitions>
              <ColumnDefinition />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Border Background="{DynamicResource {dxdiagt:DiagramPanAndZoomControlThemeKeys ResourceKey=PresenterBackground}}" Padding="3">
              <dxdiag:PanAndZoomDiagramPresenter x:Name="PART_DiagramPresenter" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
            </Border>
            <Border x:Name="PART_ZoomTrackBarContainer" Grid.Column="1" Background="{DynamicResource {dxdiagt:DiagramPanAndZoomControlThemeKeys ResourceKey=ZoomTrackBarBackground}}" Padding="3" Visibility="{Binding Diagram.AllowZoom, RelativeSource={RelativeSource TemplatedParent}, Converter={dxmvvm:BooleanToVisibilityConverter}}">
              <dxe:TrackBarEdit Orientation="Vertical" TickPlacement="None" Minimum="-3600" Maximum="3600" SmallStep="120" LargeStep="240" TickFrequency="3600" EditValue="{Binding Diagram.ZoomFactor, RelativeSource={RelativeSource TemplatedParent}, Converter={dxdiagn:NonlinearZoomConverter}, Mode=TwoWay}">
                <dxe:TrackBarEdit.StyleSettings>
                  <dxe:TrackBarZoomStyleSettings />
                </dxe:TrackBarEdit.StyleSettings>
              </dxe:TrackBarEdit>
            </Border>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>