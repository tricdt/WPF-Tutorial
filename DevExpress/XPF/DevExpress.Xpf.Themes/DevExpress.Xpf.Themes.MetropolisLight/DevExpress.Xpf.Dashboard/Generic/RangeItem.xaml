<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxdash="http://schemas.devexpress.com/winfx/2008/xaml/dashboard" xmlns:dxdashi="http://schemas.devexpress.com/winfx/2008/xaml/dashboard/internal" xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" xmlns:dxdasht="http://schemas.devexpress.com/winfx/2008/xaml/dashboard/themekeys" xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" xmlns:dxdst="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys">
  <dxdashi:EventToCommandConverter x:Key="EventToCommandConverter" />
  <Style x:Key="{dxdasht:RangeItemThemeKey ResourceKey=ViewStyle}" TargetType="{x:Type dxe:RangeControl}">
    <Setter Property="Focusable" Value="False" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="AllowZoom" Value="False" />
    <Setter Property="AllowImmediateRangeUpdate" Value="False" />
    <Setter Property="AllowSnapToInterval" Value="False" />
    <Setter Property="AllowScroll" Value="False" />
    <Setter Property="ShowRangeBar" Value="False" />
    <Setter Property="UpdateDelay" Value="100" />
    <Setter Property="AllowImmediateRangeUpdate" Value="False" />
    <Setter Property="SelectionRangeStart" Value="{Binding SelectionStart, Mode=TwoWay}" />
    <Setter Property="SelectionRangeEnd" Value="{Binding SelectionEnd, Mode=TwoWay}" />
    <Setter Property="dxmvvm:Interaction.BehaviorsTemplate">
      <Setter.Value>
        <DataTemplate>
          <ItemsControl>
            <dxdashi:RangeItemClientGenerator RangeControlClientTemplate="{DynamicResource {dxdasht:RangeItemThemeKey ResourceKey=RangeControlClientTemplate}}" />
            <dxmvvm:EventToCommand EventName="MouseUp" Command="{Binding OnMouseUpCommand}" EventArgsConverter="{StaticResource EventToCommandConverter}" PassEventArgsToCommand="True" />
            <dxmvvm:EventToCommand EventName="PreviewMouseDown" Command="{Binding OnMouseDownCommand}" EventArgsConverter="{StaticResource EventToCommandConverter}" PassEventArgsToCommand="True" />
            <dxmvvm:EventToCommand EventName="MouseDoubleClick" Command="{Binding OnMouseDoubleClickCommand}" EventArgsConverter="{StaticResource EventToCommandConverter}" PassEventArgsToCommand="True" />
            <dxmvvm:EventToCommand EventName="MouseMove" Command="{Binding OnMouseMoveCommand}" EventArgsConverter="{StaticResource EventToCommandConverter}" PassEventArgsToCommand="True" />
          </ItemsControl>
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxdasht:RangeItemThemeKey ResourceKey=RangeControlClientStyle}" TargetType="{x:Type dxdashi:RangeControlClient}">
    <Setter Property="ItemsSource" Value="{Binding Data}" />
    <Setter Property="ChangeToken" Value="{Binding ChangeToken}" />
  </Style>
  <DataTemplate x:Key="{dxdasht:RangeItemThemeKey ResourceKey=RangeControlClientTemplate}">
    <dxdashi:RangeControlClient Style="{Binding Path=(dxdash:RangeDashboardLayoutItem.RangeControlClientStyle), RelativeSource={RelativeSource Self}}" />
  </DataTemplate>
  <DataTemplate x:Key="{dxdasht:RangeItemThemeKey ResourceKey=Template}">
    <dxe:RangeControl Style="{Binding Path=(dxdash:RangeDashboardLayoutItem.ViewStyle), RelativeSource={RelativeSource TemplatedParent}}" />
  </DataTemplate>
  <DataTemplate x:Key="{dxdasht:RangeItemThemeKey ResourceKey=CustomPeriodDialogWindowTemplate}">
    <Grid Width="292" Height="175">
      <Grid.RowDefinitions>
        <RowDefinition />
        <RowDefinition />
      </Grid.RowDefinitions>
      <Grid Grid.Row="0" Width="190" Margin="0,20,0,0">
        <Grid.RowDefinitions>
          <RowDefinition />
          <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" VerticalAlignment="Bottom">
          <Label Content="{dxdashi:DashboardLocalizationString RangeFilterSelectRangeFromCaption}" Height="20" />
        </Grid>
        <Grid Grid.Row="1" VerticalAlignment="Top">
          <dxe:DateEdit EditValue="{Binding DialogStartDate}" Height="20" StyleSettings="{Binding DialogStyleSettings}" Mask="{Binding DialogMask}" MaskUseAsDisplayFormat="{Binding DialogMaskUseAsDisplayFormat}" />
        </Grid>
      </Grid>
      <Grid Grid.Row="1" Width="190" Margin="0,0,0,20">
        <Grid.RowDefinitions>
          <RowDefinition />
          <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" VerticalAlignment="Bottom">
          <Label Content="{dxdashi:DashboardLocalizationString RangeFilterSelectRangeToCaption}" Height="20" />
        </Grid>
        <Grid Grid.Row="1" VerticalAlignment="Top">
          <dxe:DateEdit EditValue="{Binding DialogEndDate}" Height="20" StyleSettings="{Binding DialogStyleSettings}" Mask="{Binding DialogMask}" MaskUseAsDisplayFormat="{Binding DialogMaskUseAsDisplayFormat}" />
        </Grid>
      </Grid>
    </Grid>
  </DataTemplate>
  <Style x:Key="{dxdasht:RangeItemThemeKey ResourceKey=CustomPeriodDialogWindowStyle}" TargetType="{x:Type dx:ThemedWindow}">
    <Setter Property="ResizeMode" Value="NoResize" />
    <Setter Property="SizeToContent" Value="WidthAndHeight" />
    <Setter Property="ShowIcon" Value="False" />
  </Style>
</ResourceDictionary>