<ResourceDictionary mc:Ignorable="cs" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:cs="colorscheme_ignorable_uri" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking" xmlns:dxdop="http://schemas.devexpress.com/winfx/2008/xaml/docking/platform" xmlns:dxdot="http://schemas.devexpress.com/winfx/2008/xaml/docking/themekeys" xmlns:dxdove="http://schemas.devexpress.com/winfx/2008/xaml/docking/visualelements" xmlns:dxi="http://schemas.devexpress.com/winfx/2008/xaml/core/internal" xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:sys="clr-namespace:System;assembly=mscorlib" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ResourceDictionary.MergedDictionaries>
    <dxt:ResourceDictionaryEx Source="/DevExpress.Xpf.Themes.Office2019Black.v24.1;component/Docking/Office2019Black/UICore.xaml" />
  </ResourceDictionary.MergedDictionaries>
  <!-- General -->
  <SolidColorBrush x:Key="{dxdot:DocumentSelectorElementsThemeKey ResourceKey=CutBrushVertical}" Color="Transparent" />
  <SolidColorBrush x:Key="{dxdot:DocumentSelectorElementsThemeKey ResourceKey=CutBrushHorizontal}" Color="Transparent" />
  <ControlTemplate x:Key="{dxdot:OtherResourcesThemeKey ResourceKey=HiddenItemTemplate}" TargetType="dxdove:HiddenItem">
    <Grid Height="Auto">
      <Border x:Name="Item" Background="{DynamicResource {dxdot:BrushElementsThemeKey ResourceKey=HiddenItem}}" BorderBrush="{DynamicResource {dxdot:BrushElementsThemeKey ResourceKey=HiddenItemBorder}}" BorderThickness="1" />
      <StackPanel Margin="4" Orientation="Horizontal" VerticalAlignment="Center">
        <Image x:Name="PART_Image" Margin="0,0,4,0" />
        <ContentPresenter />
      </StackPanel>
    </Grid>
    <ControlTemplate.Triggers>
      <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
        <Setter Property="Margin" TargetName="PART_Image" Value="14" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <!-- Cursor -->
  <ControlTemplate x:Key="ItemTemplate" TargetType="ContentControl">
    <Grid VerticalAlignment="Top">
      <Border x:Name="Item" Background="{DynamicResource {dxdot:BrushElementsThemeKey ResourceKey=HiddenItem}}" BorderBrush="{DynamicResource {dxdot:BrushElementsThemeKey ResourceKey=HiddenItemBorder}}" BorderThickness="1" />
      <Border x:Name="CaptionContainer" HorizontalAlignment="Center" MinHeight="28">
        <ContentPresenter x:Name="Content" Margin="12,3,12,3" />
      </Border>
    </Grid>
    <ControlTemplate.Triggers>
      <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
        <Setter Property="Margin" TargetName="Content" Value="14" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <ControlTemplate x:Key="PanelTemplate" TargetType="ContentControl">
    <Grid>
      <Border x:Name="Panel" Background="{DynamicResource {dxdot:BrushElementsThemeKey ResourceKey=HiddenItemBorder}}" Margin="0,8,2,2">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition x:Name="RowDefinition1" Height="16" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>
          <Border Background="{DynamicResource {dxdot:BrushElementsThemeKey ResourceKey=HiddenItemCaption}}" Grid.Row="0" Margin="1" />
          <Border Background="{DynamicResource {dxdot:BrushElementsThemeKey ResourceKey=HiddenItem}}" Grid.Row="1" Margin="1,0,1,1" />
        </Grid>
      </Border>
      <Border x:Name="CaptionContainer" HorizontalAlignment="Center" Margin="2" VerticalAlignment="Center">
        <ContentPresenter x:Name="Content" Margin="12,3,12,3" />
      </Border>
    </Grid>
    <ControlTemplate.Triggers>
      <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
        <Setter Property="Margin" TargetName="Content" Value="14" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <ControlTemplate x:Key="DragCursorTemplate" TargetType="ContentControl">
    <Border Background="#FF505050" BorderBrush="#FF6A6A6A" BorderThickness="1" Opacity="0.62" cs:id="Border_0001" x:Uid="@Background=HoverBackground;BorderBrush=SelectionBackground" />
  </ControlTemplate>
  <ControlTemplate x:Key="{dxdot:DockElementsThemeKey ResourceKey=DragCursor}" TargetType="dxdove:DragCursorControl">
    <ContentControl x:Name="Content" Template="{StaticResource PanelTemplate}">
      <dxdove:TemplatedCaptionControl x:Name="PART_CaptionControl" Foreground="#FFDADADA" LayoutItem="{Binding}" cs:id="TemplatedCaptionControl_0000" x:Uid="@Foreground=Foreground" />
    </ContentControl>
    <ControlTemplate.Triggers>
      <Trigger Property="CursorType" Value="Item">
        <Setter Property="Template" TargetName="Content" Value="{StaticResource ItemTemplate}" />
      </Trigger>
      <Trigger Property="CursorType" Value="Window">
        <Setter Property="Template" TargetName="Content" Value="{StaticResource DragCursorTemplate}" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
</ResourceDictionary>