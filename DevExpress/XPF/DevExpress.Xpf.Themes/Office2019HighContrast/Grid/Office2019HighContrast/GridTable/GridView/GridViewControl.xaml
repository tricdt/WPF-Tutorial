<ResourceDictionary mc:Ignorable="cs" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:cs="colorscheme_ignorable_uri" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" xmlns:dxei="http://schemas.devexpress.com/winfx/2008/xaml/editors/internal" xmlns:dxet="http://schemas.devexpress.com/winfx/2008/xaml/editors/themekeys" xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid" xmlns:dxgi="http://schemas.devexpress.com/winfx/2008/xaml/grid/internal" xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys" xmlns:dxi="http://schemas.devexpress.com/winfx/2008/xaml/core/internal" xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:sys="clr-namespace:System;assembly=mscorlib" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ResourceDictionary.MergedDictionaries>
    <dxt:ResourceDictionaryEx Source="/DevExpress.Xpf.Themes.Office2019HighContrast.v24.1;component/Grid/Generic/HeaderFooterItemsControlStyles.xaml" />
    <dxt:ResourceDictionaryEx Source="/DevExpress.Xpf.Themes.Office2019HighContrast.v24.1;component/Grid/Office2019HighContrast/UICore.xaml" />
  </ResourceDictionary.MergedDictionaries>
  <!-- General -->
  <ControlTemplate x:Key="{dxgt:TotalSummaryThemeKey ResourceKey=FootersContentTemplate}" TargetType="{x:Type ContentControl}">
    <Grid x:Name="footersContent">
      <Border x:Name="PART_Background" Background="{DynamicResource {dxgt:GridRowThemeKey ResourceKey=FixedSummaryFillBrush}}" BorderBrush="{DynamicResource {dxgt:GridRowThemeKey ResourceKey=TotalSummaryBorderBrush}}">
        <Border.BorderThickness>
          <MultiBinding>
            <MultiBinding.Bindings>
              <Binding Path="View.TotalSummaryPosition" />
              <Binding Path="View.IsRootView" />
            </MultiBinding.Bindings>
            <MultiBinding.Converter>
              <dxgi:TableViewTotalSummaryPanelBorderThicknessConverter />
            </MultiBinding.Converter>
          </MultiBinding>
        </Border.BorderThickness>
      </Border>
      <ContentPresenter />
    </Grid>
    <ControlTemplate.Triggers>
      <DataTrigger Binding="{Binding Path=(dxg:GridControl.CurrentView).DataControl.(dxi:AppearanceOptions.HasBackground), RelativeSource={RelativeSource Self}}" Value="True">
        <Setter Property="Background" TargetName="PART_Background" Value="{Binding Path=(dxg:GridControl.CurrentView).DataControl.(dxi:AppearanceOptions.Background), RelativeSource={RelativeSource TemplatedParent}}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding Path=(dxg:GridControl.CurrentView).DataControl.(dxi:GridAppearanceOptions.HasDataAreaBackground), RelativeSource={RelativeSource Self}}" Value="True">
        <Setter Property="Background" TargetName="PART_Background" Value="{Binding Path=(dxg:GridControl.CurrentView).DataControl.(dxi:GridAppearanceOptions.DataAreaBackground), RelativeSource={RelativeSource TemplatedParent}}" />
      </DataTrigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <SolidColorBrush x:Key="{dxgt:TableViewThemeKey ResourceKey=DataPresenterBackground}" Color="#FF000000" x:Uid="@Color=Editor.Background" />
  <ControlTemplate x:Key="{dxgt:TableViewThemeKey ResourceKey=OuterBorderElementTemplate}" TargetType="{x:Type dxg:OuterBorderElement}" />
  <!-- Cell Content -->
  <ControlTemplate x:Key="{dxgt:GridRowThemeKey ResourceKey=CellContentPresenterTemplate}" TargetType="{x:Type dxg:CellContentPresenter}">
    <Border x:Name="ContentBorder" BorderThickness="{TemplateBinding BorderState}" Padding="{TemplateBinding Padding}">
      <Border.Background>
        <Binding Path="Background" RelativeSource="{RelativeSource TemplatedParent}" />
      </Border.Background>
      <Border.BorderBrush>
        <Binding Path="BorderBrush" RelativeSource="{RelativeSource TemplatedParent}" />
      </Border.BorderBrush>
      <dxg:CellEditor x:Name="PART_CellEditor" />
    </Border>
  </ControlTemplate>
  <!-- ColumnChooser -->
  <Style x:Key="{dxgt:TableViewThemeKey ResourceKey=ColumnChooserDragTextStyle}" TargetType="TextBlock">
    <Setter Property="Foreground" Value="#FFFFFFFF" cs:id="Setter_0000" x:Uid="@Value=Foreground" />
    <Setter Property="HorizontalAlignment" Value="Center" />
    <Setter Property="Margin" Value="32" />
    <Setter Property="Opacity" Value="0.62" />
    <Setter Property="TextAlignment" Value="Center" />
    <Setter Property="TextWrapping" Value="Wrap" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Style.Triggers>
      <Trigger Property="dxg:GridBorderHelper.ShowBorder" Value="True">
        <Setter Property="Margin" Value="44" />
      </Trigger>
    </Style.Triggers>
  </Style>
  <!-- CompactPanel -->
  <DataTemplate x:Key="{dxgt:TableViewThemeKey ResourceKey=CompactPanelDownGlyph}">
    <Grid x:Name="Root" Margin="0,-1,0,0">
      <Path Fill="{TemplateBinding TextBlock.Foreground}" Style="{StaticResource UIShape.Down}" />
    </Grid>
  </DataTemplate>
  <DataTemplate x:Key="{dxgt:TableViewThemeKey ResourceKey=CompactPanelSortGlyph}">
    <Grid x:Name="Root">
      <Path Fill="{TemplateBinding TextBlock.Foreground}" Stretch="Fill" Style="{StaticResource UIShape.CompactView.Sort}" />
    </Grid>
  </DataTemplate>
</ResourceDictionary>