<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
    xmlns:dxi="http://schemas.devexpress.com/winfx/2008/xaml/core/internal">
    <ResourceDictionary.MergedDictionaries>
        <dxi:ThemeResourceDictionary Path="Common/{ThemeName}/Common.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <!--#region GroupBox-->
    <ControlTemplate x:Key="GroupBoxButtonTemplate" TargetType="dxlc:GroupBoxButton">
        <dx:Container Background="Transparent">
            <Grid
                x:Name="Root"
                Background="Transparent"
                Height="15"
                Width="15">
                <Path x:Name="MinimizeElement" Fill="{StaticResource Brush.Foreground}" Style="{StaticResource UIShape.Up}" />
                <Path x:Name="UnminimizeElement" Fill="{StaticResource Brush.Foreground}" Style="{StaticResource UIShape.Down}" />
                <Path x:Name="MaximizeElement" Fill="{StaticResource Brush.Foreground}" Style="{StaticResource UIShape.Layout.Restore}" />
                <Path x:Name="UnmaximizeElement" Fill="{StaticResource Brush.Foreground}" Style="{StaticResource UIShape.Layout.Maximize}" />
            </Grid>
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="MouseOver">
                        <Storyboard>
                            <DoubleAnimation Duration="0" Storyboard.TargetName="Root" Storyboard.TargetProperty="Opacity" To="1" />
                            <ColorAnimation Duration="0" Storyboard.TargetName="Root" Storyboard.TargetProperty="Background.(SolidColorBrush.Color)" To="{StaticResource Color.HoverBackground}" />
                            <ColorAnimation Duration="0" Storyboard.TargetName="MinimizeElement" Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)" To="{StaticResource Color.HoverForeground}" />
                            <ColorAnimation Duration="0" Storyboard.TargetName="UnminimizeElement" Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)" To="{StaticResource Color.HoverForeground}" />
                            <ColorAnimation Duration="0" Storyboard.TargetName="MaximizeElement" Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)" To="{StaticResource Color.HoverForeground}" />
                            <ColorAnimation Duration="0" Storyboard.TargetName="UnmaximizeElement" Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)" To="{StaticResource Color.HoverForeground}" />
                        </Storyboard>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </dx:Container>
    </ControlTemplate>
    <!--#endregion GroupBox-->
</ResourceDictionary>