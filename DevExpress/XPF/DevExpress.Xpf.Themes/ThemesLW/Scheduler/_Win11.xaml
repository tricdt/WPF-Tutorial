<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:dxi="http://schemas.devexpress.com/winfx/2008/xaml/core/internal"
    xmlns:dxschi="http://schemas.devexpress.com/winfx/2008/xaml/scheduler/internal"
    xmlns:dxscht="http://schemas.devexpress.com/winfx/2008/xaml/scheduler/themekeys"
    xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    xmlns:dxsch="http://schemas.devexpress.com/winfx/2008/xaml/scheduler"
    xmlns:dxet="http://schemas.devexpress.com/winfx/2008/xaml/editors/themekeys">
    <ResourceDictionary.MergedDictionaries>
        <dxi:ThemeResourceDictionary Path="Common/{ThemeName}/Common.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <SolidColorBrush x:Key="{dxscht:LWKey SchedulerView.HeaderForegroundColor}" Color="{StaticResource Color.Foreground.Primary}" />
    <SolidColorBrush x:Key="{dxscht:LWKey SchedulerView.AppointmentForegroundColor}" Color="{StaticResource Color.Foreground.Primary}" />
    <SolidColorBrush x:Key="{dxscht:LWKey DayView.AllDayAreaBottomBorderBackground}" Color="{StaticResource Color.Editor.Background}" />
    <ControlTemplate x:Key="NavigationButtonTemplate" TargetType="{x:Type dxschi:VisualNavigationButton}">
        <Grid x:Name="Root">
            <dx:DXBorder x:Name="IsDefault"
                    Background="{dx:LWBrush 'Brush.Button.SimpleButtonBackground'}"
                    BorderBrush="{dx:LWBrush 'Brush.Button.SimpleButtonBorder'}"
                    BorderThickness="1"
                    CornerRadius="32">
                <dx:DXBorder.Effect>
                    <DropShadowEffect BlurRadius="15"
                                      Color="{dx:LWColor '#05F0F0F0, Win11Dark=#0D000000'}"
                                      ShadowDepth="0">
                    </DropShadowEffect>
                </dx:DXBorder.Effect>
            </dx:DXBorder>
            <Path x:Name="Backward"
                  Fill="{dx:LWBrush 'Brush.Foreground.Secondary'}"
                  Opacity="0"
                  Style="{StaticResource UIShape.Left}"/>
            <Path x:Name="Forward"
                  Fill="{dx:LWBrush 'Brush.Foreground.Secondary'}"
                  Opacity="0"
                  Style="{StaticResource UIShape.Right}"/>
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal"/>
                    <VisualState x:Name="MouseOver">
                        <Storyboard>
                            <ColorAnimation Duration="0"
                                            Storyboard.TargetName="IsDefault"
                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                            To="{dx:LWColor 'Color.Button.HoverBackground'}"/>
                            <ColorAnimation Duration="0"
                                            Storyboard.TargetName="IsDefault"
                                            Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                            To="{dx:LWColor 'Color.Button.SimpleButtonHoverBorder'}"/>
                            <ColorAnimation Duration="0"
                                            Storyboard.TargetName="Forward"
                                            Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)"
                                            To="{dx:LWColor 'Color.Foreground.Primary'}"/>
                            <ColorAnimation Duration="0"
                                            Storyboard.TargetName="Backward"
                                            Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)"
                                            To="{dx:LWColor 'Color.Foreground.Primary'}"/>
                        </Storyboard>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <Storyboard>
                            <ColorAnimation Duration="0"
                                            Storyboard.TargetName="IsDefault"
                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                            To="{dx:LWColor 'Color.Button.PressedBackground'}"/>
                            <ColorAnimation Duration="0"
                                            Storyboard.TargetName="IsDefault"
                                            Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                            To="{dx:LWColor 'Color.Button.SimpleButtonPressedBorder'}"/>
                            <ColorAnimation Duration="0"
                                            Storyboard.TargetName="Forward"
                                            Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)"
                                            To="{dx:LWColor 'Color.Foreground.Muted'}"/>
                            <ColorAnimation Duration="0"
                                            Storyboard.TargetName="Backward"
                                            Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)"
                                            To="{dx:LWColor 'Color.Foreground.Muted'}"/>
                        </Storyboard>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <Storyboard>
                            <DoubleAnimation Duration="0"
                                             Storyboard.TargetName="Root"
                                             Storyboard.TargetProperty="Opacity"
                                             To="0"/>
                        </Storyboard>
                    </VisualState>
                </VisualStateGroup>
                <VisualStateGroup x:Name="DirectionStates">
                    <VisualState x:Name="Backward">
                        <Storyboard>
                            <DoubleAnimation Duration="0"
                                             Storyboard.TargetName="Backward"
                                             Storyboard.TargetProperty="Opacity"
                                             To="1"/>
                        </Storyboard>
                    </VisualState>
                    <VisualState x:Name="Forward">
                        <Storyboard>
                            <DoubleAnimation Duration="0"
                                             Storyboard.TargetName="Forward"
                                             Storyboard.TargetProperty="Opacity"
                                             To="1"/>
                        </Storyboard>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </Grid>
    </ControlTemplate>
    <Style x:Key="{dxscht:LWKey SchedulerView.MoreButtonDownStyle}" TargetType="{x:Type Button}">
        <Setter Property="Height" Value="22"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContentControl}">
                    <Grid x:Name="Root">
                        <dx:DXBorder x:Name="IsDefault"
                                Background="{dx:LWBrush 'Brush.Button.SimpleButtonBackground'}"
                                BorderBrush="{dx:LWBrush 'Brush.Button.SimpleButtonBorder'}"
                                BorderThickness="1"
                                CornerRadius="32">
                            <dx:DXBorder.Effect>
                                <DropShadowEffect BlurRadius="15"
                                                  Color="{dx:LWColor '#05F0F0F0, Win11Dark=#0D000000'}"
                                                  ShadowDepth="0">
                                </DropShadowEffect>
                            </dx:DXBorder.Effect>
                        </dx:DXBorder>
                        <Path x:Name="Glyph"
                              Fill="{dx:LWBrush 'Brush.Foreground.Secondary'}"
                              Style="{StaticResource UIShape.Down}"/>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <ColorAnimation Duration="0"
                                                        Storyboard.TargetName="IsDefault"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="{dx:LWColor 'Color.Button.HoverBackground'}"/>
                                        <ColorAnimation Duration="0"
                                                        Storyboard.TargetName="IsDefault"
                                                        Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                        To="{dx:LWColor 'Color.Button.HoverBorder'}"/>
                                        <ColorAnimation Duration="0"
                                                        Storyboard.TargetName="Glyph"
                                                        Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)"
                                                        To="{dx:LWColor 'Color.Foreground.Primary'}"/>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ColorAnimation Duration="0"
                                                        Storyboard.TargetName="IsDefault"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="{dx:LWColor 'Color.Button.PressedBackground'}"/>
                                        <ColorAnimation Duration="0"
                                                        Storyboard.TargetName="IsDefault"
                                                        Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                        To="{dx:LWColor 'Color.Button.PressedBorder'}"/>
                                        <ColorAnimation Duration="0"
                                                        Storyboard.TargetName="Glyph"
                                                        Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)"
                                                        To="{dx:LWColor 'Color.Foreground.Muted'}"/>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <DoubleAnimation Duration="0"
                                                         Storyboard.TargetName="Root"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0.38"/>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Width" Value="22"/>
        <Style.Triggers>
            <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
                <Setter Property="Margin" Value="14"/>
                <Setter Property="Margin" Value="44"/>
                <Setter Property="Margin" Value="44"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="{dxscht:LWKey SchedulerView.MoreButtonUpStyle}" TargetType="{x:Type Button}">
        <Setter Property="Height" Value="22"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContentControl}">
                    <Grid x:Name="Root">
                        <dx:DXBorder x:Name="IsDefault"
                                Background="{dx:LWBrush 'Brush.Button.SimpleButtonBackground'}"
                                BorderBrush="{dx:LWBrush 'Brush.Button.SimpleButtonBorder'}"
                                BorderThickness="1"
                                CornerRadius="32">
                            <dx:DXBorder.Effect>
                                <DropShadowEffect BlurRadius="15"
                                                  Color="{dx:LWColor '#05F0F0F0, Win11Dark=#0D000000'}"
                                                  ShadowDepth="0">
                                </DropShadowEffect>
                            </dx:DXBorder.Effect>
                        </dx:DXBorder>
                        <Path x:Name="Glyph"
                              Fill="{dx:LWBrush 'Brush.Foreground.Alt'}"
                              Style="{StaticResource UIShape.Up}"/>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <ColorAnimation Duration="0"
                                                        Storyboard.TargetName="IsDefault"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="{dx:LWColor 'Color.Button.HoverBackground'}"/>
                                        <ColorAnimation Duration="0"
                                                        Storyboard.TargetName="IsDefault"
                                                        Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                        To="{dx:LWColor 'Color.Button.SimpleButtonHoverBorder'}"/>
                                        <ColorAnimation Duration="0"
                                                        Storyboard.TargetName="Glyph"
                                                        Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)"
                                                        To="{dx:LWColor 'Color.Foreground.Primary'}"/>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ColorAnimation Duration="0"
                                                        Storyboard.TargetName="IsDefault"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="{dx:LWColor 'Color.Button.PressedBackground'}"/>
                                        <ColorAnimation Duration="0"
                                                        Storyboard.TargetName="IsDefault"
                                                        Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                        To="{dx:LWColor 'Color.Button.SimpleButtonPressedBorder'}"/>
                                        <ColorAnimation Duration="0"
                                                        Storyboard.TargetName="Glyph"
                                                        Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)"
                                                        To="{dx:LWColor 'Color.Foreground.Muted'}"/>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <DoubleAnimation Duration="0"
                                                         Storyboard.TargetName="Root"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0.38"/>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Width" Value="22"/>
        <Style.Triggers>
            <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
                <Setter Property="Margin" Value="14"/>
                <Setter Property="Margin" Value="44"/>
                <Setter Property="Margin" Value="44"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>