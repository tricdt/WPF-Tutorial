<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars" xmlns:dxcn="clr-namespace:DevExpress.Xpf.Core.Native;assembly=DevExpress.Xpf.Core.v24.1" xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" xmlns:dxpg="http://schemas.devexpress.com/winfx/2008/xaml/pivotgrid" xmlns:dxpgi="http://schemas.devexpress.com/winfx/2008/xaml/pivotgrid/internal" xmlns:dxdst="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys">
  <dx:BooleanToVisibilityConverter x:Key="boolToVisibilityConverter" />
  <dx:BoolToVisibilityInverseConverter x:Key="inverseBoolToVisibilityConverter" />
  <Style x:Key="{dxpgi:PivotGridThemeKey ResourceKey=ErrorPanelStyle}" TargetType="dxe:FlyoutControl">
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="HorizontalAlignment" Value="Center" />
    <Setter Property="Settings">
      <Setter.Value>
        <dxe:FlyInSettings />
      </Setter.Value>
    </Setter>
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <Grid Width="247" Margin="5,5,5,18">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Image x:Name="icon" Stretch="None" VerticalAlignment="Top" HorizontalAlignment="Left">
              <Image.Source>
                <Binding Source="DevExpress.Xpf.Core.Core.Images.ErrorIcon.png" Converter="{dx:EmbeddedResourceImageConverter}" />
              </Image.Source>
            </Image>
            <TextBlock Padding="9,3,0,0" Text="{Binding}" Grid.Column="1" TextWrapping="Wrap" TextAlignment="Left" />
          </Grid>
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="ResizerStyle" TargetType="{x:Type dx:DXThumb}">
    <Setter Property="MinWidth" Value="{StaticResource {x:Static SystemParameters.ResizeFrameVerticalBorderWidthKey}}" />
    <Setter Property="MinHeight" Value="4" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type dx:DXThumb}">
          <Border Background="Transparent" dx:DragManager.IsStartDragPlace="False" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.PivotGrid.FieldValueElement}" TargetType="{x:Type dxpg:FieldValueElement}">
    <Setter Property="BorderBrush" Value="{DynamicResource {dxpgi:AppearanceThemeKey ResourceKey=FieldValueBorderBrush}}" />
    <Setter Property="FontWeight" Value="Normal" />
    <Setter Property="dxpg:PivotGridPopupMenu.GridMenuType" Value="FieldValue" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="dxpg:FieldValueElement">
          <dx:SimplePanel x:Name="panel">
            <Decorator Margin="{TemplateBinding BorderThickness}">
              <dx:XPFContentControl x:Name="grid" Template="{TemplateBinding ContentTemplate}" Margin="{TemplateBinding Padding}" />
            </Decorator>
            <dx:DXThumb Name="PART_VerticalGripper" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Style="{StaticResource ResizerStyle}" Cursor="SizeNS" Visibility="{TemplateBinding GripperYVisibility}" />
            <dx:DXThumb Name="PART_HorizontalGripper" HorizontalAlignment="Right" VerticalAlignment="Stretch" Style="{StaticResource ResizerStyle}" Cursor="SizeWE" Visibility="{TemplateBinding GripperXVisibility}" />
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup Name="IsTotal">
                <VisualState x:Name="Normal" />
                <VisualState x:Name="Total">
                  <Storyboard>
                    <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="VerticalAlignment" Storyboard.TargetName="grid">
                      <DiscreteObjectKeyFrame KeyTime="0">
                        <DiscreteObjectKeyFrame.Value>
                          <VerticalAlignment>Center</VerticalAlignment>
                        </DiscreteObjectKeyFrame.Value>
                      </DiscreteObjectKeyFrame>
                    </ObjectAnimationUsingKeyFrames>
                  </Storyboard>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
          </dx:SimplePanel>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="False">
              <Setter TargetName="PART_HorizontalGripper" Property="Visibility" Value="Collapsed" />
              <Setter TargetName="PART_VerticalGripper" Property="Visibility" Value="Collapsed" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <DataTrigger Binding="{Binding ValueItem.IsOthersRow, RelativeSource={RelativeSource Self}}" Value="True">
        <Setter Property="FontWeight" Value="Bold" />
      </DataTrigger>
    </Style.Triggers>
  </Style>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.PivotGrid.LightweightFieldValueElement}" TargetType="{x:Type dxpg:LightweightFieldValueElement}" BasedOn="{StaticResource {dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.PivotGrid.FieldValueElement}}">
    <Setter Property="MinHeight" Value="10" />
    <Style.Triggers>
      <Trigger Property="IsLightWeightMode" Value="True">
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type dxpg:LightweightFieldValueElement}">
              <dx:SimplePanel DataContext="{x:Null}">
                <Decorator Name="root" Margin="{Binding ValueItem.BorderThickness, RelativeSource={RelativeSource TemplatedParent}}">
                  <Grid Name="grid" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Margin="{TemplateBinding Padding}">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <dxpgi:FieldValueExpandButton x:Name="PART_ExpandButton" Grid.Column="0" VerticalAlignment="Center" />
                    <dxpgi:LightWeightFieldValueEditor x:Name="PART_Editor" Grid.Column="1" VerticalAlignment="Center" />
                    <dxpgi:SortBySummaryButton x:Name="SortedByIndicator" Grid.Column="2" VerticalAlignment="Center" Margin="0,1,3,0" />
                  </Grid>
                </Decorator>
                <dx:DXThumb Name="PART_VerticalGripper" DataContext="{x:Null}" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Style="{StaticResource ResizerStyle}" Cursor="SizeNS" Visibility="{TemplateBinding GripperYVisibility}" />
                <dx:DXThumb Name="PART_HorizontalGripper" DataContext="{x:Null}" HorizontalAlignment="Right" VerticalAlignment="Stretch" Style="{StaticResource ResizerStyle}" Cursor="SizeWE" Visibility="{TemplateBinding GripperXVisibility}" />
                <VisualStateManager.VisualStateGroups>
                  <VisualStateGroup Name="IsTotal">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Total">
                      <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="VerticalAlignment" Storyboard.TargetName="root">
                          <DiscreteObjectKeyFrame KeyTime="0">
                            <DiscreteObjectKeyFrame.Value>
                              <VerticalAlignment>Center</VerticalAlignment>
                            </DiscreteObjectKeyFrame.Value>
                          </DiscreteObjectKeyFrame>
                        </ObjectAnimationUsingKeyFrames>
                      </Storyboard>
                    </VisualState>
                  </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>
              </dx:SimplePanel>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="False">
                  <Setter TargetName="PART_HorizontalGripper" Property="Visibility" Value="Collapsed" />
                  <Setter TargetName="PART_VerticalGripper" Property="Visibility" Value="Collapsed" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Trigger>
    </Style.Triggers>
  </Style>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.PivotGrid.Internal.FieldValueExpandButton}" TargetType="{x:Type dxpgi:FieldValueExpandButton}">
    <Setter Property="Margin" Value="{DynamicResource {dxpgi:ScrollableAreaThemeKey ResourceKey=ExpandButtonStyleRowMargin}}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type dxpgi:FieldValueExpandButton}">
          <dx:ToggleStateButton ClickMode="Press" IsChecked="{TemplateBinding IsChecked}" Command="{TemplateBinding Command}" CommandParameter="{TemplateBinding CommandParameter}" Style="{DynamicResource {dxpgi:ScrollableAreaThemeKey ResourceKey=ExpandButtonStyle}}" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="IsColumnButton" Value="True">
        <Setter Property="Margin" Value="{DynamicResource {dxpgi:ScrollableAreaThemeKey ResourceKey=ExpandButtonStyleColumnMargin}}" />
      </Trigger>
    </Style.Triggers>
  </Style>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.PivotGrid.Internal.SortBySummaryButton}" TargetType="{x:Type dxpgi:SortBySummaryButton}">
    <Setter Property="Template" Value="{DynamicResource {dxpgi:ScrollableAreaThemeKey ResourceKey=SortedByIndicatorTemplate}}" />
  </Style>
  <Geometry x:Key="{dxpgi:FieldHeaderThemeKey ResourceKey=GroupButtonCollapseItemPathData}">F1 M 645,375L 641,379L 645,383L 645,375 Z</Geometry>
  <Geometry x:Key="{dxpgi:FieldHeaderThemeKey ResourceKey=GroupButtonExpandItemPathData}">F1 M 653,383L 657,379L 653,375L 653,383 Z</Geometry>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.PivotGrid.Internal.GroupCollapseButton}" TargetType="{x:Type dxpgi:GroupCollapseButton}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type dxpgi:GroupCollapseButton}">
          <dx:SimplePanel Background="Transparent" IsHitTestVisible="True" Name="panel">
            <Path x:Name="collapseItem" Visibility="Visible" Style="{DynamicResource {dxpgi:FieldHeaderThemeKey ResourceKey=GroupButtonCollapseItemStyle}}" Data="{DynamicResource {dxpgi:FieldHeaderThemeKey ResourceKey=GroupButtonCollapseItemPathData}}" />
            <Path x:Name="expandItem" Visibility="Collapsed" Style="{DynamicResource {dxpgi:FieldHeaderThemeKey ResourceKey=GroupButtonExpandItemStyle}}" Data="{DynamicResource {dxpgi:FieldHeaderThemeKey ResourceKey=GroupButtonExpandItemPathData}}" />
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="Buttons">
                <VisualState x:Name="CollapseButton" />
                <VisualState x:Name="ExpandButton">
                  <Storyboard>
                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="collapseItem" Storyboard.TargetProperty="(UIElement.Visibility)" dxcn:ValueSetter.Visibility="Collapsed" />
                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="expandItem" Storyboard.TargetProperty="(UIElement.Visibility)" dxcn:ValueSetter.Visibility="Visible" />
                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="panel" Storyboard.TargetProperty="Margin" dxcn:ValueSetter.Thickness="2,0,-1,0" />
                  </Storyboard>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
          </dx:SimplePanel>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="MinWidth" Value="8" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Margin" Value="-1,0,1,0" />
  </Style>
  <DataTemplate x:Key="{dxpgi:PivotGridThemeKey ResourceKey=FilterCheckedTreeViewItemHeaderTemplate}">
    <dxe:CheckEdit Focusable="False" Content="{Binding Text, Mode=OneTime}" IsChecked="{Binding IsChecked, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=dxpgi:FilterCheckedTreeViewItem}}" IsThreeState="{Binding Path=IsThreeState, RelativeSource={RelativeSource AncestorType=dxpgi:FilterCheckedTreeViewItem}}" />
  </DataTemplate>
  <ControlTemplate x:Key="{dxpgi:PivotGridThemeKey ResourceKey=FilterCheckedTreeViewPopupTemplate}" TargetType="{x:Type Control}">
    <Grid Width="Auto" Height="Auto" SnapsToDevicePixels="True">
      <dxpgi:FilterCheckedTreeView x:Name="tree" BorderThickness="0" ItemsHeaderTemplate="{DynamicResource {dxpgi:PivotGridThemeKey ResourceKey=FilterCheckedTreeViewItemHeaderTemplate}}" IsEnabled="{Binding IsItemsLoaded, RelativeSource={RelativeSource Self}}" VirtualizingStackPanel.VirtualizationMode="Recycling" dxpg:PivotGridPopupMenu.GridMenuType="FilterPopup">
        <!--BEGIN WPF-->
        <VirtualizingStackPanel.IsVirtualizing>True</VirtualizingStackPanel.IsVirtualizing>
        <!--END WPF-->
      </dxpgi:FilterCheckedTreeView>
      <dx:ColumnWaitIndicator Visibility="{Binding Path=IsItemsLoaded, ElementName=tree, Converter={StaticResource inverseBoolToVisibilityConverter}}" VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="32" MaxHeight="32" />
    </Grid>
  </ControlTemplate>
  <ControlTemplate x:Key="{dxpgi:PivotGridThemeKey ResourceKey=FilterWaitIndicatorTemplate}" TargetType="{x:Type Control}">
    <Grid Width="Auto" Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=ItemsPresenter}}" SnapsToDevicePixels="True">
      <dx:ColumnWaitIndicator VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="32" MaxHeight="32" />
    </Grid>
  </ControlTemplate>
</ResourceDictionary>