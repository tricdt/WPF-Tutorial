<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxgcore="http://schemas.devexpress.com/winfx/2008/xaml/grid" xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid" xmlns:dxght="http://schemas.devexpress.com/winfx/2008/xaml/grid/internal" xmlns:sys="clr-namespace:System;assembly=mscorlib" xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys" xmlns:dxei="http://schemas.devexpress.com/winfx/2008/xaml/editors/internal" xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars" xmlns:dxet="http://schemas.devexpress.com/winfx/2008/xaml/editors/themekeys" xmlns:dxgi="http://schemas.devexpress.com/winfx/2008/xaml/grid/internal" xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys" xmlns:dxdst="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys">
  <dx:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter" />
  <dx:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
  <dxg:GridColumnToGridColumnDataConverter x:Key="GridColumnToGridColumnDataConverter" />
  <!--BEGIN WPF-->
  <dxg:DefaultDataTemplate x:Key="{dxgt:GridColumnHeaderThemeKey ResourceKey=ContentTemplate}">
    <!--END WPF-->
    <!--ONLY SL
    <DataTemplate x:Key="{dxgt:GridColumnHeaderThemeKey ResourceKey=ContentTemplate}">
    ONLY SL-->
    <ContentPresenter Name="textBlock" Content="{Binding}" dxg:GridColumnHeader.GridColumn="{Binding Path=(dxg:GridColumnHeader.GridColumn), RelativeSource={RelativeSource TemplatedParent}}">
      <!--BEGIN WPF-->
      <ContentPresenter.Resources>
        <Style TargetType="{x:Type TextBlock}">
          <Setter Property="TextTrimming" Value="CharacterEllipsis" />
          <Setter Property="dxei:TextBlockService.AllowIsTextTrimmed" Value="True" />
          <Style.Triggers>
            <MultiDataTrigger>
              <MultiDataTrigger.Conditions>
                <Condition Binding="{Binding Path=(dxg:GridColumnHeader.GridColumn).HeaderToolTip, RelativeSource={RelativeSource TemplatedParent}}" Value="{x:Null}" />
                <Condition Binding="{Binding Path=(dxei:TextBlockService.IsTextTrimmed), RelativeSource={RelativeSource Self}}" Value="True" />
              </MultiDataTrigger.Conditions>
              <Setter Property="ToolTipService.ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=Text}" />
            </MultiDataTrigger>
          </Style.Triggers>
        </Style>
      </ContentPresenter.Resources>
      <!--END WPF-->
    </ContentPresenter>
    <!--BEGIN WPF-->
  </dxg:DefaultDataTemplate>
  <!--END WPF-->
  <!--ONLY SL
    </DataTemplate>
    ONLY SL-->
  <!--BEGIN WPF-->
  <dxg:DefaultDataTemplate x:Key="{dxgt:GridColumnHeaderThemeKey ResourceKey=CustomizationAreaTemplate}">
    <!--END WPF-->
    <!--ONLY SL
    <DataTemplate x:Key="{dxgt:GridColumnHeaderThemeKey ResourceKey=CustomizationAreaTemplate}">
    ONLY SL-->
    <!--BEGIN WPF-->
  </dxg:DefaultDataTemplate>
  <!--END WPF-->
  <!--ONLY SL
    </DataTemplate>
    ONLY SL-->
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.Grid.GridThumb}" TargetType="{x:Type dxg:GridThumb}">
    <!--BEGIN WPF-->
    <Setter Property="MinWidth" Value="{DynamicResource {x:Static SystemParameters.ResizeFrameVerticalBorderWidthKey}}" />
    <Setter Property="MinHeight" Value="{DynamicResource {x:Static SystemParameters.ResizeFrameHorizontalBorderHeightKey}}" />
    <!--END WPF-->
    <!--ONLY SL
        <Setter Property="MinWidth" Value="8" />
        ONLY SL-->
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type dxg:GridThumb}">
          <Grid Background="Transparent" dx:DragManager.IsStartDragPlace="False" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="dxg:GridViewHitInfoBase.HitTestAcceptor">
      <Setter.Value>
        <dxgi:ColumnEdgeTableViewHitTestAcceptor />
      </Setter.Value>
    </Setter>
  </Style>
  <ControlTemplate x:Key="{dxgt:GridColumnHeaderThemeKey ResourceKey=FilterAreaTemplate}" TargetType="{x:Type dxe:PopupBaseEdit}">
    <ToggleButton Template="{DynamicResource {dxgt:GridColumnHeaderThemeKey ResourceKey=ColumnFilterTemplate}}" AutomationProperties.Name="FilterButton" AutomationProperties.AutomationId="FilterButton" IsChecked="{Binding Path=IsPopupOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" HorizontalAlignment="Left" VerticalAlignment="Top" Focusable="False" IsTabStop="False" Foreground="{Binding Path=Parent.(TextElement.Foreground), RelativeSource={RelativeSource TemplatedParent}}" />
  </ControlTemplate>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.Grid.DesignTimeSelectionControl}" TargetType="{x:Type dxg:DesignTimeSelectionControl}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate>
          <Border Background="Red" IsHitTestVisible="false" Opacity="0.3" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <!--BEGIN WPF-->
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.Grid.SortIndicatorControl}" TargetType="dxg:SortIndicatorControl">
    <Setter Property="HorizontalAlignment" Value="Right" />
    <Setter Property="Width" Value="{DynamicResource {dxgt:GridColumnHeaderThemeKey ResourceKey=SortIndicatorWidth}}" />
    <Setter Property="Margin" Value="{DynamicResource {dxgt:GridColumnHeaderThemeKey ResourceKey=SortIndicatorUpMargin}}" />
    <Setter Property="Template" Value="{DynamicResource {dxgt:GridColumnHeaderThemeKey ResourceKey=SortArrowUpTemplate}}" />
    <Style.Triggers>
      <Trigger Property="SortOrder" Value="Descending">
        <Setter Property="Template" Value="{DynamicResource {dxgt:GridColumnHeaderThemeKey ResourceKey=SortArrowDownTemplate}}" />
        <Setter Property="Margin" Value="{DynamicResource {dxgt:GridColumnHeaderThemeKey ResourceKey=SortIndicatorDownMargin}}" />
      </Trigger>
    </Style.Triggers>
  </Style>
  <!--END WPF-->
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.Grid.HeaderFilterDisplayControl}" TargetType="dxg:HeaderFilterDisplayControl">
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type dxg:HeaderFilterDisplayControl}">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="PART_SearchToggle">
              <ToggleButton.Template>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                  <Border VerticalAlignment="Center" Background="#00FF0000">
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                      </Grid.ColumnDefinitions>
                      <Border x:Name="PART_SearchIconBorder" VerticalAlignment="Center" Background="#00000000">
                        <Path x:Name="PART_SearchIcon" Visibility="Collapsed" Style="{DynamicResource ResourceKey={dxgt:GridColumnHeaderThemeKey ResourceKey=HeaderFilterSearchIconStyle}}" Margin="2,0,2,0" Fill="{DynamicResource ResourceKey={dxgt:GridColumnHeaderThemeKey ResourceKey=HeaderFilterDisplayForeground}}" />
                      </Border>
                      <TextBlock Name="PART_Textblock" Grid.Column="1" Text="{TemplateBinding Content}">
                        <TextBlock.Style>
                          <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=TextBlock}" TargetType="TextBlock">
                            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                            <Setter Property="dxei:TextBlockService.AllowIsTextTrimmed" Value="True" />
                            <Setter Property="Foreground" Value="{DynamicResource ResourceKey={dxgt:GridColumnHeaderThemeKey ResourceKey=HeaderFilterDisplayForeground}}" />
                            <Style.Triggers>
                              <Trigger Property="dxei:TextBlockService.IsTextTrimmed" Value="True">
                                <Setter Property="ToolTipService.ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=Text}" />
                              </Trigger>
                            </Style.Triggers>
                          </Style>
                        </TextBlock.Style>
                      </TextBlock>
                    </Grid>
                  </Border>
                  <ControlTemplate.Triggers>
                    <MultiDataTrigger>
                      <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type dxg:GridColumnHeader}}}" Value="True" />
                        <Condition Binding="{Binding AutoFilterValue, Converter={dxgi:StringToBoolConverter}}" Value="False" />
                      </MultiDataTrigger.Conditions>
                      <Setter TargetName="PART_SearchIcon" Property="Visibility" Value="Visible" />
                    </MultiDataTrigger>
                    <MultiDataTrigger>
                      <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type dxg:GridColumnHeader}}}" Value="True" />
                      </MultiDataTrigger.Conditions>
                      <Setter TargetName="PART_Textblock" Property="Foreground" Value="{DynamicResource ResourceKey={dxgt:GridColumnHeaderThemeKey ResourceKey=HeaderFilterDisplayForegroundHeaderHover}}" />
                      <Setter TargetName="PART_SearchIcon" Property="Fill" Value="{DynamicResource ResourceKey={dxgt:GridColumnHeaderThemeKey ResourceKey=HeaderFilterDisplayForegroundHeaderHover}}" />
                    </MultiDataTrigger>
                    <MultiDataTrigger>
                      <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding IsPressed, RelativeSource={RelativeSource AncestorType={x:Type dxg:GridColumnHeader}}}" Value="True" />
                      </MultiDataTrigger.Conditions>
                      <Setter TargetName="PART_Textblock" Property="Foreground" Value="{DynamicResource ResourceKey={dxgt:GridColumnHeaderThemeKey ResourceKey=HeaderFilterDisplayForegroundHeaderPressed}}" />
                      <Setter TargetName="PART_SearchIcon" Property="Fill" Value="{DynamicResource ResourceKey={dxgt:GridColumnHeaderThemeKey ResourceKey=HeaderFilterDisplayForegroundHeaderPressed}}" />
                    </MultiDataTrigger>
                    <Trigger SourceName="PART_SearchIconBorder" Property="IsMouseOver" Value="True">
                      <Setter TargetName="PART_SearchIcon" Property="Fill" Value="{DynamicResource ResourceKey={dxgt:GridColumnHeaderThemeKey ResourceKey=HeaderFilterDisplayForegroundHover}}" />
                    </Trigger>
                    <Trigger SourceName="PART_Textblock" Property="IsMouseOver" Value="True">
                      <Setter TargetName="PART_Textblock" Property="Foreground" Value="{DynamicResource ResourceKey={dxgt:GridColumnHeaderThemeKey ResourceKey=HeaderFilterDisplayForegroundHover}}" />
                    </Trigger>
                  </ControlTemplate.Triggers>
                </ControlTemplate>
              </ToggleButton.Template>
            </ToggleButton>
            <Button Grid.Column="1" Name="PART_ClearButton" dx:FocusHelper2.Focusable="False" Visibility="Collapsed" Command="{Binding Path=ClearCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=dxg:HeaderFilterDisplayControl}}" Template="{DynamicResource {dxgt:GridColumnHeaderThemeKey ResourceKey=HeaderFilterDisplayClearButtonTemplate}}" />
          </Grid>
          <ControlTemplate.Triggers>
            <MultiDataTrigger>
              <MultiDataTrigger.Conditions>
                <Condition Binding="{Binding AutoFilterValue, Converter={dxgi:StringToBoolConverter}}" Value="True" />
                <Condition Binding="{Binding View.AutoFilterRowClearButtonMode}" Value="Always" />
              </MultiDataTrigger.Conditions>
              <Setter TargetName="PART_ClearButton" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
            <MultiDataTrigger>
              <MultiDataTrigger.Conditions>
                <Condition Binding="{Binding AutoFilterValue, Converter={dxgi:StringToBoolConverter}}" Value="True" />
                <Condition Binding="{Binding View.AutoFilterRowClearButtonMode}" Value="Display" />
              </MultiDataTrigger.Conditions>
              <Setter TargetName="PART_ClearButton" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.Grid.HeaderFilterEditControl}" TargetType="{x:Type dxg:HeaderFilterEditControl}">
    <Setter Property="Background" Value="{DynamicResource {dxgt:GridColumnHeaderThemeKey ResourceKey=HeaderFilterContainerBackground}}" />
    <Setter Property="Margin" Value="{DynamicResource {dxgt:GridColumnHeaderThemeKey ResourceKey=HeaderFilterContainerMargin}}" />
    <Setter Property="Visibility" Value="Collapsed" />
    <Setter Property="dx:WpfSvgPalette.Palette" Value="{DynamicResource {dxt:BrushesThemeKey ResourceKey=ContrastSvgPalette}}" />
    <Setter Property="Foreground" Value="{DynamicResource {dxgt:GridColumnHeaderThemeKey ResourceKey=HeaderFilterForeground}}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type dxg:HeaderFilterEditControl}">
          <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
              </Grid.ColumnDefinitions>
              <dxg:FilterCriteriaControl DataContext="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=dxg:HeaderFilterEditControl}}" Grid.Column="0">
                <dxg:FilterCriteriaControl.Style>
                  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.Grid.FilterCriteriaControl}" TargetType="dxg:FilterCriteriaControl">
                    <Setter Property="Column" Value="{Binding Column, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=dxg:HeaderFilterEditControl}}" />
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                      <DataTrigger Binding="{Binding Column.ActualShowCriteriaInAutoFilterRow, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=dxg:HeaderFilterEditControl}}" Value="True">
                        <Setter Property="Visibility" Value="Visible" />
                      </DataTrigger>
                    </Style.Triggers>
                  </Style>
                </dxg:FilterCriteriaControl.Style>
              </dxg:FilterCriteriaControl>
              <ContentPresenter Grid.Column="1" Content="{TemplateBinding Editor}" />
              <Button x:Name="PART_ClearButton" Grid.Column="2" Visibility="Collapsed" Command="{Binding Path=ClearCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=dxg:HeaderFilterEditControl}}" Background="#00000000" Margin="0,0,2,0" dx:FocusHelper2.Focusable="False" Template="{DynamicResource {dxgt:GridColumnHeaderThemeKey ResourceKey=HeaderFilterEditClearButtonTemplate}}" />
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <MultiDataTrigger>
              <MultiDataTrigger.Conditions>
                <Condition Binding="{Binding ActualAllowAutoFilterClearButton}" Value="True" />
                <Condition Binding="{Binding AutoFilterValue, Converter={dxgi:StringToBoolConverter}}" Value="True" />
                <Condition Binding="{Binding View.AutoFilterRowClearButtonMode}" Value="Always" />
              </MultiDataTrigger.Conditions>
              <Setter TargetName="PART_ClearButton" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
            <MultiDataTrigger>
              <MultiDataTrigger.Conditions>
                <Condition Binding="{Binding ActualAllowAutoFilterClearButton}" Value="True" />
                <Condition Binding="{Binding AutoFilterValue, Converter={dxgi:StringToBoolConverter}}" Value="True" />
                <Condition Binding="{Binding View.AutoFilterRowClearButtonMode}" Value="Edit" />
              </MultiDataTrigger.Conditions>
              <Setter TargetName="PART_ClearButton" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
        <Setter Property="Margin" Value="{DynamicResource {dxgt:GridColumnHeaderThemeKey ResourceKey=HeaderFilterContainerMarginTouch}}" />
      </Trigger>
    </Style.Triggers>
  </Style>
  <DataTemplate x:Key="{dxgt:GridColumnHeaderThemeKey ResourceKey=CheckBoxSelectorColumnHeaderTemplate}">
    <Border Padding="0,0,2,0" Margin="{DynamicResource {dxgt:GridColumnHeaderThemeKey ResourceKey=CheckBoxSelectorMargin}}">
      <dxe:CheckEdit x:Name="PART_ColumnSelectionCheckBox" IsHitTestVisible="False" EditValue="{Binding Path=GridView.AllItemsSelected, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type dxg:GridColumnHeader}}, Mode=OneWay}" IsEnabled="{Binding Path=GridView.EnabledCheckBoxSelector, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type dxg:GridColumnHeader}}, Mode=OneWay}" />
    </Border>
  </DataTemplate>
  <ControlTemplate x:Key="{dxgt:GridColumnHeaderThemeKey ResourceKey=Layout}" TargetType="{x:Type Control}">
    <dxg:ColumnHeaderDockPanel x:Name="PART_LayoutPanel" ContentMargin="{TemplateBinding Padding}" />
  </ControlTemplate>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.Grid.ColumnHeaderLayout}" TargetType="{x:Type dxg:ColumnHeaderLayout}">
    <Setter Property="Focusable" Value="False" />
    <Setter Property="Template" Value="{DynamicResource {dxgt:GridColumnHeaderThemeKey ResourceKey=Layout}}" />
  </Style>
  <Style x:Key="{dxgt:GridColumnHeaderThemeKey ResourceKey=ImageStyle}" TargetType="Image">
    <Setter Property="Stretch" Value="None" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="dxb:BarItem.UseAsBarItemGlyph" Value="True" />
    <Setter Property="Margin" Value="0,0,4,0" />
    <Style.Triggers>
      <DataTrigger Binding="{Binding Path=(dxg:GridColumnHeader.GridColumn).ImageAlignment, RelativeSource={RelativeSource Self}}" Value="Center">
        <Setter Property="Margin" Value="0,0,0,0" />
      </DataTrigger>
      <DataTrigger Binding="{Binding Path=(dxg:GridColumnHeader.GridColumn).ImageAlignment, RelativeSource={RelativeSource Self}}" Value="Far">
        <Setter Property="Margin" Value="4,0,0,0" />
      </DataTrigger>
    </Style.Triggers>
  </Style>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.Grid.HeaderImageControl}" TargetType="{x:Type dxg:HeaderImageControl}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type dxg:HeaderImageControl}">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Image Source="{TemplateBinding Image}" Style="{TemplateBinding ImageStyle}" />
            <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}" Grid.Column="1" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="ImageStyle" Value="{DynamicResource {dxgt:GridColumnHeaderThemeKey ResourceKey=ImageStyle}}" />
  </Style>
</ResourceDictionary>