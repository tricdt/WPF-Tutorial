<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="http://schemas.devexpress.com/winfx/2008/xaml/treemap" xmlns:dxdst="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys">
  <ResourceDictionary.MergedDictionaries>
    <dxdst:ResourceDictionaryEx Source="ToolTip.xaml" />
  </ResourceDictionary.MergedDictionaries>
  <local:BoolToVisibilityConverter x:Key="boolToVisibilityConverter" />
  <local:BoolToBrushConverter x:Key="boolToBrushConverter" />
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.TreeMap.SunburstControl}" TargetType="{x:Type local:SunburstControl}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type local:SunburstControl}">
          <Grid Background="Transparent">
            <local:HierarchicalLayoutControl x:Name="PART_OuterItemsContainer" Titles="{TemplateBinding Titles}" Legend="{TemplateBinding Legend}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
              <local:HierarchicalLayoutControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <local:HierarchicalLayoutControlPanel />
                </ItemsPanelTemplate>
              </local:HierarchicalLayoutControl.ItemsPanel>
              <local:HierarchicalLayoutControl.MasterElement>
                <Grid>
                  <local:SunburstSegmentItemsControl x:Name="PART_SunburstSegmentItemsControl" SectorStyle="{Binding SectorStyle, RelativeSource={RelativeSource Mode=TemplatedParent}}">
                    <local:SunburstSegmentItemsControl.ItemsPanel>
                      <ItemsPanelTemplate>
                        <local:SunburstPanel HoleRadiusPercent="{Binding Path=(local:SunburstControl.Sunburst).HoleRadiusPercent, RelativeSource={RelativeSource Self}}" MinLabelSize="{Binding Path=(local:SunburstControl.Sunburst).MinLabelSize, RelativeSource={RelativeSource Self}}" LabelDisplayMode="{Binding Path=(local:SunburstControl.Sunburst).LabelDisplayMode, RelativeSource={RelativeSource Self}}" LevelCount="{Binding Path=(local:SunburstControl.Sunburst).LevelCount, RelativeSource={RelativeSource Self}}" SectorViewInfos="{Binding Path=(local:SunburstControl.Sunburst).SectorViewInfos, RelativeSource={RelativeSource Self}}" />
                      </ItemsPanelTemplate>
                    </local:SunburstSegmentItemsControl.ItemsPanel>
                  </local:SunburstSegmentItemsControl>
                  <local:SunburstHoleContentControl HoleRadiusPercent="{Binding Path=(local:SunburstControl.Sunburst).HoleRadiusPercent, RelativeSource={RelativeSource Self}}" Content="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:SunburstControl}}, Path=HoleContent}" />
                </Grid>
              </local:HierarchicalLayoutControl.MasterElement>
            </local:HierarchicalLayoutControl>
            <local:ToolTipPanel x:Name="PART_ToolTipPanel" Position="{Binding Path=ToolTipInfo.ToolTipPosition, RelativeSource={RelativeSource TemplatedParent}}" Visibility="{Binding Path=ToolTipInfo.Visible, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource boolToVisibilityConverter}}">
              <Popup IsOpen="{Binding Path=ToolTipInfo.Visible, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}" AllowsTransparency="True" Placement="Absolute">
                <ContentPresenter Content="{Binding Path=ToolTipInfo, RelativeSource={RelativeSource TemplatedParent}}" ContentTemplate="{TemplateBinding ToolTipTemplate}" />
              </Popup>
            </local:ToolTipPanel>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="ToolTipTemplate" Value="{StaticResource SunburstToolTipTemplate}" />
    <Setter Property="ToolTipContentTemplate" Value="{StaticResource defaultSunburstToolTipContentTemplate}" />
    <Setter Property="ToolTipGroupContentTemplate" Value="{StaticResource defaultSunburstToolTipContentTemplate}" />
  </Style>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.TreeMap.SunburstSectorPresentation}" TargetType="{x:Type local:SunburstSectorPresentation}">
    <Setter Property="Stroke" Value="{Binding Stroke}" />
    <Setter Property="Fill">
      <Setter.Value>
        <MultiBinding Converter="{StaticResource boolToBrushConverter}">
          <Binding Path="IsSelected" />
          <Binding Path="IsHighlighted" />
          <Binding Path="Fill" />
        </MultiBinding>
      </Setter.Value>
    </Setter>
    <Setter Property="OpacityMask">
      <Setter.Value>
        <MultiBinding Converter="{local:OpacityMaskConverter}">
          <Binding Path="IsSelected" />
          <Binding Path="IsHighlighted" />
          <Binding Path="Fill" />
        </MultiBinding>
      </Setter.Value>
    </Setter>
    <Setter Property="StrokeThickness" Value="{Binding StrokeThickness}" />
    <Setter Property="Geometry" Value="{Binding Geometry}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type local:SunburstSectorPresentation}">
          <Grid>
            <Path Stroke="{TemplateBinding Stroke}" Fill="{TemplateBinding Fill}" StrokeThickness="{TemplateBinding StrokeThickness}" Data="{TemplateBinding Geometry}" />
            <local:SunburstLabelPresentation LabelBounds="{Binding LabelBounds}" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.TreeMap.SunburstLabelPresentation}" TargetType="{x:Type local:SunburstLabelPresentation}">
    <Setter Property="Foreground" Value="{Binding Foreground}" />
    <Setter Property="FontSize" Value="{Binding FontSize}" />
    <Setter Property="FontStyle" Value="{Binding FontStyle}" />
    <Setter Property="FontWeight" Value="{Binding FontWeight}" />
    <Setter Property="FontFamily" Value="{Binding FontFamily}" />
    <Setter Property="HorizontalAlignment" Value="Center" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Text" Value="{Binding TextObject}" />
    <Setter Property="Visibility" Value="{Binding LabelVisible, Converter={StaticResource boolToVisibilityConverter}}" />
    <Setter Property="LabelEffect" Value="{Binding Shadow}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="local:SunburstLabelPresentation">
          <Grid x:Name="labelGrid" RenderTransformOrigin="0.5, 0.5" Visibility="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:SunburstControl}, Mode=FindAncestor}, Path=LabelDisplayMode, Converter={local:LabelDisplayModeToVisibilityConverter}}" RenderTransform="{Binding LabelTransform}">
            <Border x:Name="labelBorder" HorizontalAlignment="Center" VerticalAlignment="Center" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
              <TextBlock x:Name="labelTextBlock" Text="{TemplateBinding Text}" HorizontalAlignment="{TemplateBinding HorizontalAlignment}" VerticalAlignment="{TemplateBinding VerticalAlignment}" TextTrimming="{TemplateBinding TextTrimming}" TextWrapping="{TemplateBinding TextWrapping}" Foreground="{TemplateBinding Foreground}" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" FontStretch="{TemplateBinding FontStretch}" FontStyle="{TemplateBinding FontStyle}" FontWeight="{TemplateBinding FontWeight}" Effect="{TemplateBinding LabelEffect}" />
            </Border>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>