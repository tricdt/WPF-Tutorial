<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                    xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/docking/themekeys"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    xmlns:ve="http://schemas.devexpress.com/winfx/2008/xaml/docking/visualelements"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <dx:BoolToVisibilityConverter x:Key="boolToVisibilityConverter"/>

    <SolidColorBrush x:Key="{dxt:BrushElementsThemeKey ResourceKey=PanelForeground}" Color="#FF000000"/>

    <SolidColorBrush x:Key="{dxt:BrushElementsThemeKey ResourceKey=PanelBackground}" Color="#FFE3EFFF"/>

    <SolidColorBrush x:Key="{dxt:BrushElementsThemeKey ResourceKey=CaptionActive}" Color="Red"/>

    <SolidColorBrush x:Key="{dxt:BrushElementsThemeKey ResourceKey=CaptionInactive}" Color="Red"/>

    <SolidColorBrush x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=BorderBrush}" Color="#FF8896AB"/>

    <SolidColorBrush x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=FloatingActiveBorderBrush}" Color="#FF8896AB"/>

    <LinearGradientBrush x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=CaptionActiveBackground}"
                         EndPoint="0.5,1"
                         StartPoint="0.5,0">
        <GradientStop Color="#FFBAD2E9" Offset="0"/>
        <GradientStop Color="#FFA5C4E2" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=CaptionNormalBackground}"
                         EndPoint="0.5,1"
                         StartPoint="0.5,0">
        <GradientStop Color="#FFD6E3EE" Offset="0"/>
        <GradientStop Color="#FFCDDCEA" Offset="1"/>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=CaptionActiveForeground}" Color="#FF000000"/>

    <SolidColorBrush x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=CaptionNormalForeground}" Color="#FF434E54"/>

    <SolidColorBrush x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=Background}" Color="#FFFFFFFF"/>

    <SolidColorBrush x:Key="{dxt:BrushElementsThemeKey ResourceKey=MoveTargetBackground}" Color="#FFCD0000"/>

    <CornerRadius x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=CaptionCornerRadius}">2,2,0,0</CornerRadius>

    <CornerRadius x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=FloatingCaptionCornerRadius}">2,2,0,0</CornerRadius>

    <Thickness x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=BorderThickness}">1,1,1,1</Thickness>

    <Thickness x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=BorderMargin}">0</Thickness>

    <Thickness x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=BorderPadding}">0,0,0,1</Thickness>

    <Thickness x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=BarContainerMargin}">1,0,1,0</Thickness>

    <Thickness x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=ContentMargin}">0</Thickness>

    <Thickness x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=HeaderMargin}">0</Thickness>

    <Thickness x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=LightViewHeaderMargin}"/>

    <sys:Boolean x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=IsDarkTheme}">False</sys:Boolean>

    <ControlTemplate x:Key="{dxt:DockElementsThemeKey ResourceKey=SplitterTemplate}" TargetType="ve:Splitter">
        <Grid Background="Transparent">
            <dx:DXBorder x:Name="PART_SplitThumb"
                         Background="{TemplateBinding Background}"
                         EnableDPICorrection="False"
                         Margin="{TemplateBinding Padding}"/>
            <Border Background="{DynamicResource {dxt:BrushElementsThemeKey ResourceKey=MoveTargetBackground}}"
                    Opacity="0.15"
                    Visibility="{Binding Path=IsDragDropOver, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource boolToVisibilityConverter}}"/>
        </Grid>
    </ControlTemplate>

    <DataTemplate x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=Header}">
        <Border Background="#FFF7FAFD" DataContext="{Binding LayoutItem}">
            <Border x:Name="PART_CaptionBackground"
                    CornerRadius="1,1,0,0"
                    Margin="1,1,1,0"
                    ve:DockPane.HitTestType="Header">
                <Border.Background>
                    <LinearGradientBrush EndPoint="1,0.5" StartPoint="0,0.5">
                        <GradientStop Color="#FFBAD2E9" Offset="0"/>
                        <GradientStop Color="#FFA5C4E2" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid Margin="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ve:TemplatedCaptionControl x:Name="PART_CaptionControlPresenter"
                                                LayoutItem="{Binding}"
                                                Padding="3,0"/>
                    <ve:PanelControlBoxControl x:Name="PART_ControlBox"
                                               ButtonHeight="17"
                                               ButtonStyle="{DynamicResource {dxt:DockPaneElementsThemeKey ResourceKey=DockPaneButtonStyle}}"
                                               ButtonWidth="17"
                                               CloseButtonTemplate="{DynamicResource {dxt:DockPaneElementsThemeKey ResourceKey=CloseButton}}"
                                               CollapseButtonTemplate="{DynamicResource {dxt:DockPaneElementsThemeKey ResourceKey=CollapseButton}}"
                                               ExpandButtonTemplate="{DynamicResource {dxt:DockPaneElementsThemeKey ResourceKey=ExpandButton}}"
                                               Grid.Column="1"
                                               HideButtonTemplate="{DynamicResource {dxt:DockPaneElementsThemeKey ResourceKey=HideButton}}"
                                               LayoutItem="{Binding}"
                                               MaximizeButtonTemplate="{DynamicResource {dxt:DockPaneElementsThemeKey ResourceKey=MaximizeButton}}"
                                               MinimizeButtonTemplate="{DynamicResource {dxt:DockPaneElementsThemeKey ResourceKey=MinimizeButton}}"
                                               PinButtonTemplate="{DynamicResource {dxt:DockPaneElementsThemeKey ResourceKey=PinButton}}"
                                               RestoreButtonTemplate="{DynamicResource {dxt:DockPaneElementsThemeKey ResourceKey=RestoreButton}}"/>
                </Grid>
            </Border>
        </Border>
    </DataTemplate>

    <ControlTemplate x:Key="{dxt:DockPaneElementsThemeKey ResourceKey=Template}" TargetType="ve:DockPane">
        <Grid Background="Transparent"
              Margin="{Binding Path=LayoutItem.ActualMargin, RelativeSource={RelativeSource TemplatedParent}}"
              ve:DockPane.HitTestType="Bounds">
            <Border BorderBrush="{TemplateBinding ActualBorderBrush}"
                    BorderThickness="{TemplateBinding ActualBorderThickness}"
                    CornerRadius="{TemplateBinding ActualCaptionCornerRadius}"
                    Margin="{TemplateBinding ActualBorderMargin}"/>
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding ActualBorderBrush}"
                    BorderThickness="{TemplateBinding ActualBorderThickness}"
                    CornerRadius="{TemplateBinding ActualCaptionCornerRadius}"
                    Margin="{TemplateBinding ActualBorderMargin}"
                    Padding="{TemplateBinding ActualBorderPadding}"
                    SnapsToDevicePixels="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <ve:FloatingDragWidget Background="Transparent">
                        <ve:DockPaneHeaderPresenter x:Name="PART_Header"
                                                    Content="{TemplateBinding Header}"
                                                    Margin="{DynamicResource {dxt:DockPaneElementsThemeKey ResourceKey=HeaderMargin}}"/>
                    </ve:FloatingDragWidget>
                    <ve:DockPaneContentPresenter x:Name="PART_Content"
                                                 Content="{TemplateBinding Content}"
                                                 Grid.Row="1"
                                                 Margin="{Binding Path=LayoutItem.ActualPadding, RelativeSource={RelativeSource TemplatedParent}}"/>
                </Grid>
            </Border>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="ViewStyle" Value="Light">
                <Setter Property="Margin"
                        TargetName="PART_Header"
                        Value="{DynamicResource {dxt:DockPaneElementsThemeKey ResourceKey=LightViewHeaderMargin}}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

</ResourceDictionary>