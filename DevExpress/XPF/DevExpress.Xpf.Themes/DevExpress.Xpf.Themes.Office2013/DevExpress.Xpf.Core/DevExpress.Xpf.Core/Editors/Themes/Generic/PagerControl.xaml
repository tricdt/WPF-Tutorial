<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:dxtEx="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxet="http://schemas.devexpress.com/winfx/2008/xaml/editors/themekeys" xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" xmlns:sys="clr-namespace:System;assembly=mscorlib" xmlns:dxdst="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys">
  <dx:BoolToVisibilityConverter x:Key="boolToVisibilityConverter" />
  <dx:BoolToVisibilityInverseConverter x:Key="boolToVisiblityConverterInverse" />
  <dxe:ActualPageIndexConveter x:Key="pageIndexConverter" />
  <ControlTemplate x:Key="{dxet:PagerControlThemeKey ResourceKey=Template}" TargetType="dxe:PagerControl">
    <Grid>
      <ContentControl VerticalAlignment="Center" Template="{DynamicResource {dxet:PagerControlThemeKey ResourceKey=BorderTemplate}}">
        <DockPanel>
          <TextBlock Text="{TemplateBinding SummaryText}" VerticalAlignment="Center" Visibility="{TemplateBinding ShowSummaryText, Converter={StaticResource boolToVisibilityConverter}}" DockPanel.Dock="Left" Margin="12,0,12,0" />
          <Grid DockPanel.Dock="Right" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="0,0,12,0">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <dxe:PagerControlNavigationButton Grid.Column="0" Command="{Binding Path=FirstPageCommand, RelativeSource={RelativeSource TemplatedParent}}" ContentTemplate="{DynamicResource {dxet:PagerControlThemeKey ResourceKey=NavigationButtonFirstPageGlyph}}" Visibility="{TemplateBinding ActualShowFirstPageButton, Converter={StaticResource boolToVisibilityConverter}}" ToolTip="{dxe:EditorStringId FirstPage}" />
            <dxe:PagerControlNavigationButton Grid.Column="1" Command="{Binding Path=PreviousPageCommand, RelativeSource={RelativeSource TemplatedParent}}" ContentTemplate="{DynamicResource {dxet:PagerControlThemeKey ResourceKey=NavigationButtonPreviousPageGlyph}}" Visibility="{TemplateBinding ActualShowPrevPageButton, Converter={StaticResource boolToVisibilityConverter}}" ToolTip="{dxe:EditorStringId PrevPage}" />
            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Hidden">
              <ItemsControl Name="PART_NumberItemsControl" ItemTemplate="{DynamicResource {dxet:PagerControlThemeKey ResourceKey=NumericItemTemplate}}" Visibility="{TemplateBinding ShowNumericButtons, Converter={StaticResource boolToVisibilityConverter}}">
                <ItemsControl.ItemsPanel>
                  <ItemsPanelTemplate>
                    <dx:StackVisibleIndexPanel Orientation="Horizontal" />
                  </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
              </ItemsControl>
            </ScrollViewer>
            <dxe:PagerControlNavigationButton Grid.Column="3" Command="{Binding Path=NextPageCommand, RelativeSource={RelativeSource TemplatedParent}}" ContentTemplate="{DynamicResource {dxet:PagerControlThemeKey ResourceKey=NavigationButtonNextPageGlyph}}" Visibility="{TemplateBinding ActualShowNextPageButton, Converter={StaticResource boolToVisibilityConverter}}" ToolTip="{dxe:EditorStringId NextPage}" />
            <dxe:PagerControlNavigationButton Grid.Column="4" Command="{Binding Path=LastPageCommand, RelativeSource={RelativeSource TemplatedParent}}" ContentTemplate="{DynamicResource {dxet:PagerControlThemeKey ResourceKey=NavigationButtonLastPageGlyph}}" Visibility="{TemplateBinding ActualShowLastPageButton, Converter={StaticResource boolToVisibilityConverter}}" ToolTip="{dxe:EditorStringId LastPage}" />
            <dxe:PagerControlNavigationButton Grid.Column="5" Command="{Binding Path=RefreshPageCommand, RelativeSource={RelativeSource TemplatedParent}}" ContentTemplate="{DynamicResource {dxet:PagerControlThemeKey ResourceKey=RefreshPageGlyph}}" Visibility="{TemplateBinding ShowRefreshPageButton, Converter={StaticResource boolToVisibilityConverter}}" Margin="8,0,0,0" ToolTip="{dxe:EditorStringId RefreshPage}" />
          </Grid>
        </DockPanel>
      </ContentControl>
    </Grid>
  </ControlTemplate>
  <DataTemplate x:Key="{dxet:PagerControlThemeKey ResourceKey=NumericItemTemplate}">
    <dxe:PagerControlNumericButton ShowEllipsis="{Binding ShowEllipsis}" Number="{Binding Number}" Command="{Binding Path=(dxe:PagerControl.PagerControl).MoveToPageCommand, RelativeSource={RelativeSource TemplatedParent}}" CommandParameter="{Binding Number}" />
  </DataTemplate>
</ResourceDictionary>