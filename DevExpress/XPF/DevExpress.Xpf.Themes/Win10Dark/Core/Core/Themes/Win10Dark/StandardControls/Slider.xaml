<ResourceDictionary mc:Ignorable="cs" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:cs="colorscheme_ignorable_uri" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxi="http://schemas.devexpress.com/winfx/2008/xaml/core/internal" xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:sys="clr-namespace:System;assembly=mscorlib" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ResourceDictionary.MergedDictionaries>
    <dxt:ResourceDictionaryEx Source="/DevExpress.Xpf.Themes.Win10Dark.v24.1;component/Core/Core/Themes/Win10Dark/StandardControls/Slider_Base.xaml" />
  </ResourceDictionary.MergedDictionaries>
  <!-- Thumb -->
  <Style x:Key="VerticalSliderThumbStyle" TargetType="{x:Type Thumb}">
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Grid x:Name="Root" Background="Transparent" cs:id="Grid_0000">
            <Border x:Name="IsDefault" Background="#FFA6A6A6" BorderBrush="#FF191919" BorderThickness="1" CornerRadius="4" Height="7" Width="18" cs:id="Border_0001" x:Uid="@Background=AltPressed;BorderBrush=Window.Background" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" TargetName="IsDefault" Value="#FFF2F2F2" cs:id="Setter_0000" x:Uid="@Value=Foreground" />
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
              <Setter Property="Background" TargetName="IsDefault" Value="#FF0078D7" cs:id="Setter_0001" x:Uid="@Value=Focused" />
            </Trigger>
            <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
              <Setter Property="Margin" TargetName="Root" Value="-22" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="HorizontalSliderThumbStyle" TargetType="{x:Type Thumb}">
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Grid x:Name="Root" Background="Transparent" cs:id="Grid_0000">
            <Border x:Name="IsDefault" Background="#FFA6A6A6" BorderBrush="#FF191919" BorderThickness="1" CornerRadius="4" Height="18" Width="7" cs:id="Border_0001" x:Uid="@Background=AltPressed;BorderBrush=Window.Background" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" TargetName="IsDefault" Value="#FFF2F2F2" cs:id="Setter_0000" x:Uid="@Value=Foreground" />
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
              <Setter Property="Background" TargetName="IsDefault" Value="#FF0078D7" cs:id="Setter_0001" x:Uid="@Value=Focused" />
            </Trigger>
            <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
              <Setter Property="Margin" TargetName="Root" Value="-22" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="HorizontalSliderUpThumbStyle" BasedOn="{StaticResource HorizontalSliderThumbStyle}" TargetType="{x:Type Thumb}" />
  <Style x:Key="HorizontalSliderDownThumbStyle" BasedOn="{StaticResource HorizontalSliderThumbStyle}" TargetType="{x:Type Thumb}" />
  <Style x:Key="VerticalSliderLeftThumbStyle" BasedOn="{StaticResource VerticalSliderThumbStyle}" TargetType="{x:Type Thumb}" />
  <Style x:Key="VerticalSliderRightThumbStyle" BasedOn="{StaticResource VerticalSliderThumbStyle}" TargetType="{x:Type Thumb}" />
  <!-- Slider -->
  <Style x:Key="{dxt:DefaultStyleThemeKey FullName=System.Windows.Controls.Slider}" TargetType="{x:Type Slider}">
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Foreground" Value="#FFF2F2F2" cs:id="Setter_0000" x:Uid="@Value=Foreground" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Stylus.IsPressAndHoldEnabled" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Slider}">
          <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Width="{TemplateBinding Width}">
            <Grid x:Name="Root" MinHeight="22">
              <TickBar x:Name="TopTick" Fill="#FFF2F2F2" Height="4" Placement="Top" VerticalAlignment="Top" Visibility="Collapsed" cs:id="TickBar_0000" x:Uid="@Fill=Foreground" />
              <Grid Margin="1,0,1,0" VerticalAlignment="Center">
                <Border x:Name="TrackBackground" Background="#FF808080" Height="2" VerticalAlignment="Center" cs:id="Border_0000" x:Uid="@Background=Delimiter" />
                <Border x:Name="FocusState" Background="#FF808080" Visibility="Collapsed" cs:id="Border_0001" x:Uid="@Background=Delimiter" />
                <Canvas>
                  <Rectangle x:Name="PART_SelectionRange" Fill="#FFF2F2F2" Height="2" Opacity="0" cs:id="Rectangle_0000" x:Uid="@Fill=Foreground" />
                </Canvas>
              </Grid>
              <Track x:Name="PART_Track" VerticalAlignment="Center">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}" />
                </Track.DecreaseRepeatButton>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}" />
                </Track.IncreaseRepeatButton>
                <Track.Thumb>
                  <Thumb x:Name="Thumb" Background="#FF2B2B2B" Height="{TemplateBinding Height}" Style="{StaticResource HorizontalSliderThumbStyle}" cs:id="Thumb_0000" x:Uid="@Background=Border" />
                </Track.Thumb>
              </Track>
              <TickBar x:Name="BottomTick" Fill="#FFF2F2F2" Height="4" Placement="Bottom" VerticalAlignment="Bottom" Visibility="Collapsed" cs:id="TickBar_0001" x:Uid="@Fill=Foreground" />
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="TickPlacement" Value="TopLeft">
              <Setter Property="Style" TargetName="Thumb" Value="{StaticResource HorizontalSliderUpThumbStyle}" />
              <Setter Property="Visibility" TargetName="TopTick" Value="Visible" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="BottomRight">
              <Setter Property="Visibility" TargetName="BottomTick" Value="Visible" />
              <Setter Property="Style" TargetName="Thumb" Value="{StaticResource HorizontalSliderDownThumbStyle}" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="Both">
              <Setter Property="Visibility" TargetName="BottomTick" Value="Visible" />
              <Setter Property="Visibility" TargetName="TopTick" Value="Visible" />
            </Trigger>
            <Trigger Property="IsSelectionRangeEnabled" Value="True">
              <Setter Property="Opacity" TargetName="PART_SelectionRange" Value="1" />
            </Trigger>
            <Trigger Property="IsFocused" Value="True">
              <Setter Property="Visibility" TargetName="FocusState" Value="Visible" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" TargetName="Root" Value="0.35" />
            </Trigger>
            <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
              <Setter Property="MinHeight" TargetName="Root" Value="44" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="Orientation" Value="Vertical">
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type Slider}">
              <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Height="{TemplateBinding Height}">
                <Grid x:Name="Root" MinWidth="22">
                  <TickBar x:Name="LeftTick" Fill="#FFF2F2F2" HorizontalAlignment="Left" Placement="Left" Visibility="Collapsed" Width="4" cs:id="TickBar_0002" x:Uid="@Fill=Foreground" />
                  <Grid HorizontalAlignment="Center" Margin="0,1,0,1">
                    <Border Name="TrackBackground" Background="#FF808080" Width="2" cs:id="Border_0002" x:Uid="@Background=Delimiter" />
                    <Border x:Name="FocusState" Background="#FF808080" Visibility="Collapsed" cs:id="Border_0003" x:Uid="@Background=Delimiter" />
                    <Canvas>
                      <Rectangle x:Name="PART_SelectionRange" Fill="#FFF2F2F2" Opacity="0" Width="2" cs:id="Rectangle_0001" x:Uid="@Fill=Foreground" />
                    </Canvas>
                  </Grid>
                  <Track x:Name="PART_Track">
                    <Track.DecreaseRepeatButton>
                      <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}" />
                    </Track.DecreaseRepeatButton>
                    <Track.IncreaseRepeatButton>
                      <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}" />
                    </Track.IncreaseRepeatButton>
                    <Track.Thumb>
                      <Thumb x:Name="Thumb" Background="#FF2B2B2B" Style="{StaticResource VerticalSliderThumbStyle}" Width="{TemplateBinding Width}" cs:id="Thumb_0001" x:Uid="@Background=Border" />
                    </Track.Thumb>
                  </Track>
                  <TickBar x:Name="RightTick" Fill="#FFF2F2F2" HorizontalAlignment="Right" Placement="Right" Visibility="Collapsed" Width="4" cs:id="TickBar_0003" x:Uid="@Fill=Foreground" />
                </Grid>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="TickPlacement" Value="TopLeft">
                  <Setter Property="Visibility" TargetName="LeftTick" Value="Visible" />
                  <Setter Property="Style" TargetName="Thumb" Value="{StaticResource VerticalSliderLeftThumbStyle}" />
                </Trigger>
                <Trigger Property="TickPlacement" Value="BottomRight">
                  <Setter Property="Visibility" TargetName="RightTick" Value="Visible" />
                  <Setter Property="Style" TargetName="Thumb" Value="{StaticResource VerticalSliderRightThumbStyle}" />
                </Trigger>
                <Trigger Property="TickPlacement" Value="Both">
                  <Setter Property="Visibility" TargetName="LeftTick" Value="Visible" />
                  <Setter Property="Visibility" TargetName="RightTick" Value="Visible" />
                </Trigger>
                <Trigger Property="IsSelectionRangeEnabled" Value="True">
                  <Setter Property="Opacity" TargetName="PART_SelectionRange" Value="1" />
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                  <Setter Property="Visibility" TargetName="FocusState" Value="Visible" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter Property="Opacity" TargetName="Root" Value="0.35" />
                </Trigger>
                <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
                  <Setter Property="MinWidth" TargetName="Root" Value="44" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Trigger>
    </Style.Triggers>
  </Style>
</ResourceDictionary>