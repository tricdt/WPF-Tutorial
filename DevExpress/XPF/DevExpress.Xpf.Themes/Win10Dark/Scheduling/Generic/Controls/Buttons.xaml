<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys" xmlns:dxsch="http://schemas.devexpress.com/winfx/2008/xaml/scheduling" xmlns:dxschv="http://schemas.devexpress.com/winfx/2008/xaml/scheduling/visual" xmlns:dxscht="http://schemas.devexpress.com/winfx/2008/xaml/scheduling/themekeys" xmlns:dxschc="http://schemas.devexpress.com/winfx/2008/xaml/scheduling" xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" xmlns:dxdst="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys">
  <ResourceDictionary.MergedDictionaries>
    <dxdst:ResourceDictionaryEx Source="{dx:SchedulingResource UICore.xaml, ShortPaths=True}" />
    <dxdst:ResourceDictionaryEx Source="{dx:SchedulingResource UIPreset.xaml, ShortPaths=True}" />
  </ResourceDictionary.MergedDictionaries>
  <Style x:Key="TextButton.Style" TargetType="dxschv:SchedulerButton">
    <Setter Property="Template" Value="{DynamicResource {dxscht:SchedulerCommonThemeKey ResourceKey=ButtonTemplate}}" />
    <Setter Property="Padding" Value="9,5,9,5" />
    <Setter Property="HorizontalContentAlignment" Value="Center" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Foreground" Value="{StaticResource DateNavigationPanelButton.Normal.Foreground}" />
    <Setter Property="Background" Value="{StaticResource DateNavigationPanelButton.NormalBackground}" />
    <Setter Property="BorderBrush" Value="{StaticResource DateNavigationPanelButton.NormalBorderBrush}" />
    <Setter Property="BorderThickness" Value="{StaticResource DateNavigationPanelButton.BorderThickness}" />
    <Setter Property="CornerRadius" Value="{StaticResource DateNavigationPanelButton.CornerRadius}" />
    <Setter Property="HoverForeground" Value="{StaticResource DateNavigationPanelButton.Hover.Foreground}" />
    <Setter Property="HoverBackground" Value="{StaticResource DateNavigationPanelButton.HoverBackground}" />
    <Setter Property="HoverBorderBrush" Value="{StaticResource DateNavigationPanelButton.HoverBorderBrush}" />
    <Setter Property="HoverBorderThickness" Value="{StaticResource DateNavigationPanelButton.BorderThickness}" />
    <Setter Property="HoverCornerRadius" Value="{StaticResource DateNavigationPanelButton.CornerRadius}" />
    <Setter Property="PressedForeground" Value="{StaticResource DateNavigationPanelButton.Pressed.Foreground}" />
    <Setter Property="PressedBackground" Value="{StaticResource DateNavigationPanelButton.PressedBackground}" />
    <Setter Property="PressedBorderBrush" Value="{StaticResource DateNavigationPanelButton.PressedBorderBrush}" />
    <Setter Property="PressedBorderThickness" Value="{StaticResource DateNavigationPanelButton.BorderThickness}" />
    <Setter Property="PressedCornerRadius" Value="{StaticResource DateNavigationPanelButton.CornerRadius}" />
    <Setter Property="DisabledBackground" Value="{StaticResource DateNavigationPanelButton.DisabledBackground}" />
    <Setter Property="DisabledBorderBrush" Value="{StaticResource DateNavigationPanelButton.DisabledBorderBrush}" />
  </Style>
  <Style x:Key="MoreButton.BaseStyle" TargetType="dxschv:SchedulerButton">
    <Setter Property="Template" Value="{DynamicResource {dxscht:SchedulerCommonThemeKey ResourceKey=ButtonTemplate}}" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="Width" Value="{StaticResource MoreButton.Width}" />
    <Setter Property="Height" Value="{StaticResource MoreButton.Height}" />
    <Setter Property="Margin" Value="{StaticResource MoreButton.Margin}" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="VerticalContentAlignment" Value="Stretch" />
    <Setter Property="dxsch:HitTestBehavior.HitTestType" Value="MoreButton" />
    <Setter Property="Foreground" Value="{StaticResource MoreButton.Normal.Foreground}" />
    <Setter Property="Background" Value="{StaticResource MoreButton.NormalBackground}" />
    <Setter Property="BorderBrush" Value="{StaticResource MoreButton.NormalBorderBrush}" />
    <Setter Property="BorderThickness" Value="{StaticResource MoreButton.BorderThickness}" />
    <Setter Property="CornerRadius" Value="{StaticResource MoreButton.CornerRadius}" />
    <Setter Property="HoverForeground" Value="{StaticResource MoreButton.Hover.Foreground}" />
    <Setter Property="HoverBackground" Value="{StaticResource MoreButton.HoverBackground}" />
    <Setter Property="HoverBorderBrush" Value="{StaticResource MoreButton.HoverBorderBrush}" />
    <Setter Property="HoverBorderThickness" Value="{StaticResource MoreButton.BorderThickness}" />
    <Setter Property="HoverCornerRadius" Value="{StaticResource MoreButton.CornerRadius}" />
    <Setter Property="PressedForeground" Value="{StaticResource MoreButton.Pressed.Foreground}" />
    <Setter Property="PressedBackground" Value="{StaticResource MoreButton.PressedBackground}" />
    <Setter Property="PressedBorderBrush" Value="{StaticResource MoreButton.PressedBorderBrush}" />
    <Setter Property="PressedBorderThickness" Value="{StaticResource MoreButton.BorderThickness}" />
    <Setter Property="PressedCornerRadius" Value="{StaticResource MoreButton.CornerRadius}" />
    <Setter Property="DisabledBackground" Value="{StaticResource MoreButton.DisabledBackground}" />
    <Setter Property="DisabledBorderBrush" Value="{StaticResource MoreButton.DisabledBorderBrush}" />
  </Style>
  <Style x:Key="NavigationButton.BaseStyle" TargetType="dxschv:SchedulerButton">
    <Setter Property="Template" Value="{DynamicResource {dxscht:SchedulerCommonThemeKey ResourceKey=ButtonTemplate}}" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="FontSize" Value="{StaticResource NavigationButton.FontSize}" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="VerticalContentAlignment" Value="Stretch" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="dxsch:HitTestBehavior.HitTestType" Value="NavigationButton" />
    <Setter Property="Width" Value="{StaticResource NavigationButton.Width}" />
    <Setter Property="Height" Value="{StaticResource NavigationButton.Height}" />
    <Setter Property="BorderThickness" Value="{StaticResource NavigationButton.BorderThickness}" />
    <Setter Property="HoverBorderThickness" Value="{StaticResource NavigationButton.BorderThickness}" />
    <Setter Property="PressedBorderThickness" Value="{StaticResource NavigationButton.BorderThickness}" />
    <Setter Property="dxmvvm:Interaction.BehaviorsTemplate">
      <Setter.Value>
        <DataTemplate>
          <ContentControl>
            <dxschc:SchedulerNavigationButtonColorizationBehavior />
          </ContentControl>
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="ResourceNavigatorButton.BaseStyle" TargetType="dxschv:SchedulerButton">
    <Setter Property="Template" Value="{DynamicResource {dxscht:SchedulerCommonThemeKey ResourceKey=ButtonTemplate}}" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="Width" Value="{StaticResource ResourceNavigatorButton.Width}" />
    <Setter Property="Height" Value="{StaticResource ResourceNavigatorButton.Height}" />
    <Setter Property="HorizontalContentAlignment" Value="Center" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Foreground" Value="{StaticResource ResourceNavigatorButton.Normal.Foreground}" />
    <Setter Property="Background" Value="{StaticResource ResourceNavigatorButton.NormalBackground}" />
    <Setter Property="BorderBrush" Value="{StaticResource ResourceNavigatorButton.NormalBorderBrush}" />
    <Setter Property="BorderThickness" Value="{StaticResource ResourceNavigatorButton.BorderThickness}" />
    <Setter Property="CornerRadius" Value="{StaticResource ResourceNavigatorButton.CornerRadius}" />
    <Setter Property="HoverForeground" Value="{StaticResource ResourceNavigatorButton.Hover.Foreground}" />
    <Setter Property="HoverBackground" Value="{StaticResource ResourceNavigatorButton.HoverBackground}" />
    <Setter Property="HoverBorderBrush" Value="{StaticResource ResourceNavigatorButton.HoverBorderBrush}" />
    <Setter Property="HoverBorderThickness" Value="{StaticResource ResourceNavigatorButton.BorderThickness}" />
    <Setter Property="HoverCornerRadius" Value="{StaticResource ResourceNavigatorButton.CornerRadius}" />
    <Setter Property="PressedForeground" Value="{StaticResource ResourceNavigatorButton.Pressed.Foreground}" />
    <Setter Property="PressedBackground" Value="{StaticResource ResourceNavigatorButton.PressedBackground}" />
    <Setter Property="PressedBorderBrush" Value="{StaticResource ResourceNavigatorButton.PressedBorderBrush}" />
    <Setter Property="PressedBorderThickness" Value="{StaticResource ResourceNavigatorButton.BorderThickness}" />
    <Setter Property="PressedCornerRadius" Value="{StaticResource ResourceNavigatorButton.CornerRadius}" />
    <Setter Property="DisabledBackground" Value="{StaticResource ResourceNavigatorButton.DisabledBackground}" />
    <Setter Property="DisabledBorderBrush" Value="{StaticResource ResourceNavigatorButton.DisabledBorderBrush}" />
  </Style>
  <Style x:Key="DateNavigationPanelButton.BaseStyle" TargetType="dxschv:SchedulerButton">
    <Setter Property="Template" Value="{DynamicResource {dxscht:SchedulerCommonThemeKey ResourceKey=ButtonTemplate}}" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="Width" Value="{StaticResource DateNavigationPanelButton.Width}" />
    <Setter Property="Height" Value="{StaticResource DateNavigationPanelButton.Height}" />
    <Setter Property="Margin" Value="{StaticResource DateNavigationPanelButton.Margin}" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="VerticalContentAlignment" Value="Stretch" />
    <Setter Property="Foreground" Value="{StaticResource DateNavigationPanelButton.Normal.Foreground}" />
    <Setter Property="Background" Value="{StaticResource DateNavigationPanelButton.NormalBackground}" />
    <Setter Property="BorderBrush" Value="{StaticResource DateNavigationPanelButton.NormalBorderBrush}" />
    <Setter Property="BorderThickness" Value="{StaticResource DateNavigationPanelButton.BorderThickness}" />
    <Setter Property="CornerRadius" Value="{StaticResource DateNavigationPanelButton.CornerRadius}" />
    <Setter Property="HoverForeground" Value="{StaticResource DateNavigationPanelButton.Hover.Foreground}" />
    <Setter Property="HoverBackground" Value="{StaticResource DateNavigationPanelButton.HoverBackground}" />
    <Setter Property="HoverBorderBrush" Value="{StaticResource DateNavigationPanelButton.HoverBorderBrush}" />
    <Setter Property="HoverBorderThickness" Value="{StaticResource DateNavigationPanelButton.BorderThickness}" />
    <Setter Property="HoverCornerRadius" Value="{StaticResource DateNavigationPanelButton.CornerRadius}" />
    <Setter Property="PressedForeground" Value="{StaticResource DateNavigationPanelButton.Pressed.Foreground}" />
    <Setter Property="PressedBackground" Value="{StaticResource DateNavigationPanelButton.PressedBackground}" />
    <Setter Property="PressedBorderBrush" Value="{StaticResource DateNavigationPanelButton.PressedBorderBrush}" />
    <Setter Property="PressedBorderThickness" Value="{StaticResource DateNavigationPanelButton.BorderThickness}" />
    <Setter Property="PressedCornerRadius" Value="{StaticResource DateNavigationPanelButton.CornerRadius}" />
    <Setter Property="DisabledBackground" Value="{StaticResource DateNavigationPanelButton.DisabledBackground}" />
    <Setter Property="DisabledBorderBrush" Value="{StaticResource DateNavigationPanelButton.DisabledBorderBrush}" />
    <Style.Triggers>
      <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
        <Setter Property="Width" Value="{StaticResource Touch.DateNavigationPanelButton.Width}" />
        <Setter Property="Height" Value="{StaticResource Touch.DateNavigationPanelButton.Height}" />
        <Setter Property="Margin" Value="{StaticResource Touch.DateNavigationPanelButton.Margin}" />
      </Trigger>
    </Style.Triggers>
  </Style>
  <Style x:Key="{dxscht:SchedulerButtonThemeKey ResourceKey=MoreButton_UpStyle}" TargetType="dxschv:SchedulerButton" BasedOn="{StaticResource MoreButton.BaseStyle}">
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <Path Style="{StaticResource UIShape.Up}" Fill="{Binding '(TextBlock.Foreground)', RelativeSource={RelativeSource Self}}" />
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxscht:SchedulerButtonThemeKey ResourceKey=MoreButton_DownStyle}" TargetType="dxschv:SchedulerButton" BasedOn="{StaticResource MoreButton.BaseStyle}">
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <Path Style="{StaticResource UIShape.Down}" Fill="{Binding '(TextBlock.Foreground)', RelativeSource={RelativeSource Self}}" />
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxscht:SchedulerButtonThemeKey ResourceKey=NavigationButton_BackwardStyle}" TargetType="dxschv:SchedulerButton" BasedOn="{StaticResource NavigationButton.BaseStyle}">
    <Setter Property="Margin" Value="{StaticResource NavigationButton.Backward.Margin}" />
    <Setter Property="CornerRadius" Value="{StaticResource NavigationButton.Backward.CornerRadius}" />
    <Setter Property="HoverCornerRadius" Value="{StaticResource NavigationButton.Backward.CornerRadius}" />
    <Setter Property="PressedCornerRadius" Value="{StaticResource NavigationButton.Backward.CornerRadius}" />
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <dxschv:NavigationButtonPanel>
            <Path Style="{StaticResource UIShape.NavigationButton.Prev}" Margin="4,12,4,12" Fill="{Binding '(TextBlock.Foreground)', RelativeSource={RelativeSource Self}}" />
            <TextBlock Text="{dxschc:SchedulerLocalizer StringId=Caption_PrevAppointment}" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" Margin="4,0,4,24" Width="118">
              <TextBlock.LayoutTransform>
                <RotateTransform Angle="-90" />
              </TextBlock.LayoutTransform>
            </TextBlock>
          </dxschv:NavigationButtonPanel>
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxscht:SchedulerButtonThemeKey ResourceKey=NavigationButton_ForwardStyle}" TargetType="dxschv:SchedulerButton" BasedOn="{StaticResource NavigationButton.BaseStyle}">
    <Setter Property="Margin" Value="{StaticResource NavigationButton.Forward.Margin}" />
    <Setter Property="CornerRadius" Value="{StaticResource NavigationButton.Forward.CornerRadius}" />
    <Setter Property="HoverCornerRadius" Value="{StaticResource NavigationButton.Forward.CornerRadius}" />
    <Setter Property="PressedCornerRadius" Value="{StaticResource NavigationButton.Forward.CornerRadius}" />
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <dxschv:NavigationButtonPanel>
            <Path Style="{StaticResource UIShape.NavigationButton.Next}" Margin="4,12,4,12" Fill="{Binding '(TextBlock.Foreground)', RelativeSource={RelativeSource Self}}" />
            <TextBlock Text="{dxschc:SchedulerLocalizer StringId=Caption_NextAppointment}" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" Margin="4,0,4,24" Width="118">
              <TextBlock.LayoutTransform>
                <RotateTransform Angle="-90" />
              </TextBlock.LayoutTransform>
            </TextBlock>
          </dxschv:NavigationButtonPanel>
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_FirstStyle}" TargetType="dxschv:SchedulerButton" BasedOn="{StaticResource ResourceNavigatorButton.BaseStyle}">
    <Setter Property="ToolTip" Value="{dxschc:SchedulerLocalizer StringId=Caption_FirstVisibleResources}" />
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <Path Style="{StaticResource UIShape.Navigation.First}" Fill="{Binding '(TextBlock.Foreground)', RelativeSource={RelativeSource Self}}" />
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_PrevPageStyle}" TargetType="dxschv:SchedulerButton" BasedOn="{StaticResource ResourceNavigatorButton.BaseStyle}">
    <Setter Property="ToolTip" Value="{dxschc:SchedulerLocalizer StringId=Caption_PrevVisibleResourcesPage}" />
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <Path Style="{StaticResource UIShape.Navigation.PrevPage}" Fill="{Binding '(TextBlock.Foreground)', RelativeSource={RelativeSource Self}}" />
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_PrevItemStyle}" TargetType="dxschv:SchedulerButton" BasedOn="{StaticResource ResourceNavigatorButton.BaseStyle}">
    <Setter Property="ToolTip" Value="{dxschc:SchedulerLocalizer StringId=Caption_PrevVisibleResources}" />
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <Path Style="{StaticResource UIShape.Navigation.PrevItem}" Fill="{Binding '(TextBlock.Foreground)', RelativeSource={RelativeSource Self}}" />
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_NextItemStyle}" TargetType="dxschv:SchedulerButton" BasedOn="{StaticResource ResourceNavigatorButton.BaseStyle}">
    <Setter Property="ToolTip" Value="{dxschc:SchedulerLocalizer StringId=Caption_NextVisibleResources}" />
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <Path Style="{StaticResource UIShape.Navigation.NextItem}" Fill="{Binding '(TextBlock.Foreground)', RelativeSource={RelativeSource Self}}" />
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_NextPageStyle}" TargetType="dxschv:SchedulerButton" BasedOn="{StaticResource ResourceNavigatorButton.BaseStyle}">
    <Setter Property="ToolTip" Value="{dxschc:SchedulerLocalizer StringId=Caption_NextVisibleResourcesPage}" />
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <Path Style="{StaticResource UIShape.Navigation.NextPage}" Fill="{Binding '(TextBlock.Foreground)', RelativeSource={RelativeSource Self}}" />
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_LastStyle}" TargetType="dxschv:SchedulerButton" BasedOn="{StaticResource ResourceNavigatorButton.BaseStyle}">
    <Setter Property="ToolTip" Value="{dxschc:SchedulerLocalizer StringId=Caption_LastVisibleResources}" />
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <Path Style="{StaticResource UIShape.Navigation.Last}" Fill="{Binding '(TextBlock.Foreground)', RelativeSource={RelativeSource Self}}" />
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_PlusStyle}" TargetType="dxschv:SchedulerButton" BasedOn="{StaticResource ResourceNavigatorButton.BaseStyle}">
    <Setter Property="ToolTip" Value="{dxschc:SchedulerLocalizer StringId=Caption_IncreaseVisibleResourcesCount}" />
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <Path Style="{StaticResource UIShape.Navigation.Plus}" Fill="{Binding '(TextBlock.Foreground)', RelativeSource={RelativeSource Self}}" />
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_MinusStyle}" TargetType="dxschv:SchedulerButton" BasedOn="{StaticResource ResourceNavigatorButton.BaseStyle}">
    <Setter Property="ToolTip" Value="{dxschc:SchedulerLocalizer StringId=Caption_DecreaseVisibleResourcesCount}" />
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <Path Style="{StaticResource UIShape.Navigation.Minus}" Fill="{Binding '(TextBlock.Foreground)', RelativeSource={RelativeSource Self}}" />
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxscht:SchedulerButtonThemeKey ResourceKey=DateNavigationPanelButton_BackStyle}" TargetType="dxschv:SchedulerButton" BasedOn="{StaticResource DateNavigationPanelButton.BaseStyle}">
    <Setter Property="ToolTip" Value="{dxschc:SchedulerLocalizer StringId=MenuCmd_NavigateBackward}" />
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <Path Style="{StaticResource UIShape.Left}" Fill="{Binding '(TextBlock.Foreground)', RelativeSource={RelativeSource Self}}" />
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxscht:SchedulerButtonThemeKey ResourceKey=DateNavigationPanelButton_NextStyle}" TargetType="dxschv:SchedulerButton" BasedOn="{StaticResource DateNavigationPanelButton.BaseStyle}">
    <Setter Property="ToolTip" Value="{dxschc:SchedulerLocalizer StringId=MenuCmd_NavigateForward}" />
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <Path Style="{StaticResource UIShape.Right}" Fill="{Binding '(TextBlock.Foreground)', RelativeSource={RelativeSource Self}}" />
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxscht:SchedulerButtonThemeKey ResourceKey=DateNavigationPanelButton_CalendarButtonStyle}" TargetType="dxschv:SchedulerButton" BasedOn="{StaticResource DateNavigationPanelButton.BaseStyle}">
    <Setter Property="Margin" Value="6,0" />
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <Path Style="{StaticResource UIShape.Down}" Fill="{Binding '(TextBlock.Foreground)', RelativeSource={RelativeSource Self}}" />
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="SchedulerButton.DefaultStyle" TargetType="dxschv:SchedulerButton">
    <Setter Property="StyleTextButton" Value="{StaticResource TextButton.Style}" />
    <Setter Property="StyleMoreButtonUp" Value="{DynamicResource {dxscht:SchedulerButtonThemeKey ResourceKey=MoreButton_UpStyle}}" />
    <Setter Property="StyleMoreButtonDown" Value="{DynamicResource {dxscht:SchedulerButtonThemeKey ResourceKey=MoreButton_DownStyle}}" />
    <Setter Property="StyleNavigationButtonBackward" Value="{DynamicResource {dxscht:SchedulerButtonThemeKey ResourceKey=NavigationButton_BackwardStyle}}" />
    <Setter Property="StyleNavigationButtonForward" Value="{DynamicResource {dxscht:SchedulerButtonThemeKey ResourceKey=NavigationButton_ForwardStyle}}" />
    <Setter Property="StyleResourceNavigatorButtonFirst" Value="{DynamicResource {dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_FirstStyle}}" />
    <Setter Property="StyleResourceNavigatorButtonPrevPage" Value="{DynamicResource {dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_PrevPageStyle}}" />
    <Setter Property="StyleResourceNavigatorButtonPrevItem" Value="{DynamicResource {dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_PrevItemStyle}}" />
    <Setter Property="StyleResourceNavigatorButtonNextItem" Value="{DynamicResource {dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_NextItemStyle}}" />
    <Setter Property="StyleResourceNavigatorButtonNextPage" Value="{DynamicResource {dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_NextPageStyle}}" />
    <Setter Property="StyleResourceNavigatorButtonLast" Value="{DynamicResource {dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_LastStyle}}" />
    <Setter Property="StyleResourceNavigatorButtonPlus" Value="{DynamicResource {dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_PlusStyle}}" />
    <Setter Property="StyleResourceNavigatorButtonMinus" Value="{DynamicResource {dxscht:SchedulerButtonThemeKey ResourceKey=ResourceNavigatorButton_MinusStyle}}" />
    <Setter Property="StyleDateNavigationPanelButtonBack" Value="{DynamicResource {dxscht:SchedulerButtonThemeKey ResourceKey=DateNavigationPanelButton_BackStyle}}" />
    <Setter Property="StyleDateNavigationPanelButtonNext" Value="{DynamicResource {dxscht:SchedulerButtonThemeKey ResourceKey=DateNavigationPanelButton_NextStyle}}" />
    <Setter Property="StyleDateNavigationPanelButtonCalendar" Value="{DynamicResource {dxscht:SchedulerButtonThemeKey ResourceKey=DateNavigationPanelButton_CalendarButtonStyle}}" />
  </Style>
</ResourceDictionary>