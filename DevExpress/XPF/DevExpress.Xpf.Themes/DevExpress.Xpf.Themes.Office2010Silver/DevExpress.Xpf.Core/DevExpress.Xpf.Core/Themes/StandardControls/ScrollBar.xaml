<ResourceDictionary mc:Ignorable="cs" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:cs="colorscheme_ignorable_uri" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxdst="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys" xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ResourceDictionary.MergedDictionaries>
    <dxdst:ResourceDictionaryEx Source="/DevExpress.Xpf.Themes.Office2010Silver.v24.1;component/DevExpress.Xpf.Core/DevExpress.Xpf.Core/Themes/StandardControls/Brushes.xaml" />
    <dxdst:ResourceDictionaryEx Source="/DevExpress.Xpf.Themes.Office2010Silver.v24.1;component/DevExpress.Xpf.Core/DevExpress.Xpf.Core/Themes/StandardControls/SharedResources.xaml" />
  </ResourceDictionary.MergedDictionaries>
  <dx:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
  <dx:EnumToObjectConverter x:Key="ScrollBarModeToScrollBackVisibilityConverter" DefaultValue="Visible">
    <dx:EnumToObjectConverter.Values>
      <dx:EnumObjectProvider x:Key="TouchOverlap" Value="Collapsed" />
    </dx:EnumToObjectConverter.Values>
  </dx:EnumToObjectConverter>
  <dx:EnumToObjectConverter x:Key="ScrollBarModeToHardWidthScrollConverter" DefaultValue="Collapsed">
    <dx:EnumToObjectConverter.Values>
      <dx:EnumObjectProvider x:Key="Standard" Value="Visible" />
    </dx:EnumToObjectConverter.Values>
  </dx:EnumToObjectConverter>
  <Style x:Key="HorizontalScrollBarPageButtonStyle" TargetType="{x:Type RepeatButton}">
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="MinHeight" Value="0" />
    <Setter Property="MinWidth" Value="0" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type RepeatButton}">
          <Rectangle x:Name="PageButtonBackground" Fill="Transparent" />
          <ControlTemplate.Triggers>
            <Trigger Property="IsPressed" Value="True">
              <Setter Property="Fill" TargetName="PageButtonBackground" Value="{StaticResource ScrollBar.Horizontal.PageButton.Background}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="VerticalScrollBarPageButtonStyle" TargetType="{x:Type RepeatButton}">
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="MinWidth" Value="0" />
    <Setter Property="MinHeight" Value="0" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type RepeatButton}">
          <Rectangle x:Name="PageButtonBackground" Fill="Transparent" />
          <ControlTemplate.Triggers>
            <Trigger Property="IsPressed" Value="True">
              <Setter Property="Fill" TargetName="PageButtonBackground" Value="{StaticResource ScrollBar.Vertical.PageButton.Background}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="ScrollBarHorizontalButtonStyle" TargetType="{x:Type RepeatButton}">
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="Transparent" />
    <Setter Property="BorderBrush" Value="Transparent" />
    <Setter Property="BorderThickness" Value="2" />
    <Setter Property="MinWidth" Value="0" />
    <Setter Property="MinHeight" Value="0" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type RepeatButton}">
          <Grid x:Name="Root" SnapsToDevicePixels="True">
            <Grid x:Name="DefaultState">
              <Border Background="{StaticResource ScrollBar.Horizontal.DefaultState.Border1}" CornerRadius="2" Padding="1">
                <Border Background="{StaticResource ScrollBar.Horizontal.DefaultState.Border2}" CornerRadius="1" />
              </Border>
            </Grid>
            <Grid x:Name="HoverState" Opacity="0">
              <Border Background="{StaticResource ScrollBar.Horizontal.HoverState.Border1}" CornerRadius="2" Padding="1">
                <Border Background="{StaticResource ScrollBar.Horizontal.HoverState.Border2}" CornerRadius="1" />
              </Border>
            </Grid>
            <Grid x:Name="PressedState" Opacity="0">
              <Border Background="{StaticResource ScrollBar.Horizontal.DraggingState.Border1}" CornerRadius="2" Padding="1">
                <Border Background="{StaticResource ScrollBar.Horizontal.DraggingState.Border2}" CornerRadius="1" />
              </Border>
            </Grid>
            <Grid Height="9" HorizontalAlignment="Center" SnapsToDevicePixels="True" VerticalAlignment="Center" Width="5">
              <Path x:Name="Path" Data="{TemplateBinding Content}" Fill="{StaticResource ScrollBar.Path.Background}" HorizontalAlignment="Center" Stretch="Fill" VerticalAlignment="Center" />
            </Grid>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Opacity" TargetName="DefaultState" Value="0" />
              <Setter Property="Opacity" TargetName="HoverState" Value="1" />
              <Setter Property="Opacity" TargetName="PressedState" Value="0" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter Property="Opacity" TargetName="DefaultState" Value="0" />
              <Setter Property="Opacity" TargetName="HoverState" Value="0" />
              <Setter Property="Opacity" TargetName="PressedState" Value="1" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" TargetName="DefaultState" Value="0" />
              <Setter Property="Opacity" TargetName="HoverState" Value="0" />
              <Setter Property="Opacity" TargetName="PressedState" Value="0" />
              <Setter Property="Opacity" TargetName="Root" Value="0.5" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="ScrollBarVerticalButtonStyle" TargetType="{x:Type RepeatButton}">
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="Transparent" />
    <Setter Property="BorderBrush" Value="Transparent" />
    <Setter Property="BorderThickness" Value="2" />
    <Setter Property="MinWidth" Value="0" />
    <Setter Property="MinHeight" Value="0" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type RepeatButton}">
          <Grid x:Name="Root" SnapsToDevicePixels="True">
            <Grid x:Name="DefaultState">
              <Border Background="{StaticResource ScrollBar.Vertical.DefaultState.Border1}" CornerRadius="2" Padding="1">
                <Border Background="{StaticResource ScrollBar.Vertical.DefaultState.Border2}" CornerRadius="1" />
              </Border>
            </Grid>
            <Grid x:Name="HoverState" Opacity="0">
              <Border Background="{StaticResource ScrollBar.Vertical.HoverState.Border1}" CornerRadius="2" Padding="1">
                <Border Background="{StaticResource ScrollBar.Vertical.HoverState.Border2}" CornerRadius="1" />
              </Border>
            </Grid>
            <Grid x:Name="PressedState" Opacity="0">
              <Border Background="{StaticResource ScrollBar.Vertical.DraggingState.Border1}" CornerRadius="2" Padding="1">
                <Border Background="{StaticResource ScrollBar.Vertical.DraggingState.Border2}" CornerRadius="1" />
              </Border>
            </Grid>
            <Grid Height="5" HorizontalAlignment="Center" SnapsToDevicePixels="True" VerticalAlignment="Center" Width="9">
              <Path x:Name="Path" Data="{TemplateBinding Content}" Fill="{StaticResource ScrollBar.Path.Background}" HorizontalAlignment="Center" Stretch="Fill" VerticalAlignment="Center" />
            </Grid>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Opacity" TargetName="DefaultState" Value="0" />
              <Setter Property="Opacity" TargetName="HoverState" Value="1" />
              <Setter Property="Opacity" TargetName="PressedState" Value="0" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter Property="Opacity" TargetName="DefaultState" Value="0" />
              <Setter Property="Opacity" TargetName="HoverState" Value="0" />
              <Setter Property="Opacity" TargetName="PressedState" Value="1" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" TargetName="DefaultState" Value="0" />
              <Setter Property="Opacity" TargetName="HoverState" Value="0" />
              <Setter Property="Opacity" TargetName="PressedState" Value="0" />
              <Setter Property="Opacity" TargetName="Root" Value="0.5" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="ScrollBarThumbHorizontal" TargetType="{x:Type Thumb}">
    <Setter Property="MinWidth" Value="4" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Grid x:Name="Root" SnapsToDevicePixels="True">
            <Grid x:Name="DefaultState">
              <Border Background="{StaticResource ScrollBar.Horizontal.DefaultState.Border1}" CornerRadius="2" Padding="1">
                <Border Background="{StaticResource ScrollBar.Horizontal.DefaultState.Border2}" CornerRadius="1" />
              </Border>
            </Grid>
            <Grid x:Name="HoverState" Opacity="0">
              <Border Background="{StaticResource ScrollBar.Horizontal.HoverState.Border1}" CornerRadius="2" Padding="1">
                <Border Background="{StaticResource ScrollBar.Horizontal.HoverState.Border2}" CornerRadius="1" />
              </Border>
            </Grid>
            <Grid x:Name="DraggingState" Opacity="0">
              <Border Background="{StaticResource ScrollBar.Horizontal.DraggingState.Border1}" CornerRadius="2" Padding="1">
                <Border Background="{StaticResource ScrollBar.Horizontal.DraggingState.Border2}" CornerRadius="1" />
              </Border>
            </Grid>
            <dx:MinSizeArranger Margin="2" MinChildWidth="12">
              <Grid Height="7" HorizontalAlignment="Center" VerticalAlignment="Center" Width="8">
                <Rectangle x:Name="BackPath" Fill="{StaticResource ScrollBar.Path.BorderBrush}" Stretch="Fill" />
                <Rectangle x:Name="Path1" Fill="{StaticResource ScrollBar.Thumb.Path.Background}" HorizontalAlignment="Left" Stretch="Fill" Width="1" />
                <Rectangle x:Name="Path2" Fill="{StaticResource ScrollBar.Thumb.Path.Background}" HorizontalAlignment="Left" Margin="2,0,0,0" Stretch="Fill" Width="1" />
                <Rectangle x:Name="Path3" Fill="{StaticResource ScrollBar.Thumb.Path.Background}" HorizontalAlignment="Left" Margin="4,0,0,0" Stretch="Fill" Width="1" />
                <Rectangle x:Name="Path4" Fill="{StaticResource ScrollBar.Thumb.Path.Background}" HorizontalAlignment="Left" Margin="6,0,0,0" Stretch="Fill" Width="1" />
              </Grid>
            </dx:MinSizeArranger>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Opacity" TargetName="DefaultState" Value="0" />
              <Setter Property="Opacity" TargetName="HoverState" Value="1" />
              <Setter Property="Opacity" TargetName="DraggingState" Value="0" />
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
              <Setter Property="Opacity" TargetName="DefaultState" Value="0" />
              <Setter Property="Opacity" TargetName="HoverState" Value="0" />
              <Setter Property="Opacity" TargetName="DraggingState" Value="1" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" TargetName="DefaultState" Value="0" />
              <Setter Property="Opacity" TargetName="HoverState" Value="0" />
              <Setter Property="Opacity" TargetName="DraggingState" Value="0" />
              <Setter Property="Opacity" TargetName="Root" Value="0.5" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="ScrollBarThumbVertical" TargetType="{x:Type Thumb}">
    <Setter Property="MinHeight" Value="4" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Grid x:Name="Root" SnapsToDevicePixels="True">
            <Grid>
              <Grid x:Name="DefaultState">
                <Border Background="{StaticResource ScrollBar.Vertical.DefaultState.Border1}" CornerRadius="2" Padding="1">
                  <Border Background="{StaticResource ScrollBar.Vertical.DefaultState.Border2}" CornerRadius="1" />
                </Border>
              </Grid>
              <Grid x:Name="HoverState" Opacity="0">
                <Border Background="{StaticResource ScrollBar.Vertical.HoverState.Border1}" CornerRadius="2" Padding="1">
                  <Border Background="{StaticResource ScrollBar.Vertical.HoverState.Border2}" CornerRadius="1" />
                </Border>
              </Grid>
              <Grid x:Name="DraggingState" Opacity="0">
                <Border Background="{StaticResource ScrollBar.Vertical.DraggingState.Border1}" CornerRadius="2" Padding="1">
                  <Border Background="{StaticResource ScrollBar.Vertical.DraggingState.Border2}" CornerRadius="1" />
                </Border>
              </Grid>
              <dx:MinSizeArranger Margin="2" MinChildHeight="12">
                <Grid Height="8" HorizontalAlignment="Center" VerticalAlignment="Center" Width="7">
                  <Rectangle x:Name="BackPath" Fill="{StaticResource ScrollBar.Path.BorderBrush}" Stretch="Fill" />
                  <Rectangle x:Name="Path1" Fill="{StaticResource ScrollBar.Thumb.Path.Background}" Height="1" Stretch="Fill" VerticalAlignment="Top" />
                  <Rectangle x:Name="Path2" Fill="{StaticResource ScrollBar.Thumb.Path.Background}" Height="1" Margin="0,2,0,0" Stretch="Fill" VerticalAlignment="Top" />
                  <Rectangle x:Name="Path3" Fill="{StaticResource ScrollBar.Thumb.Path.Background}" Height="1" Margin="0,4,0,0" Stretch="Fill" VerticalAlignment="Top" />
                  <Rectangle x:Name="Path4" Fill="{StaticResource ScrollBar.Thumb.Path.Background}" Height="1" Margin="0,6,0,0" Stretch="Fill" VerticalAlignment="Top" />
                </Grid>
              </dx:MinSizeArranger>
            </Grid>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Opacity" TargetName="DefaultState" Value="0" />
              <Setter Property="Opacity" TargetName="HoverState" Value="1" />
              <Setter Property="Opacity" TargetName="DraggingState" Value="0" />
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
              <Setter Property="Opacity" TargetName="DefaultState" Value="0" />
              <Setter Property="Opacity" TargetName="HoverState" Value="0" />
              <Setter Property="Opacity" TargetName="DraggingState" Value="1" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" TargetName="DefaultState" Value="0" />
              <Setter Property="Opacity" TargetName="HoverState" Value="0" />
              <Setter Property="Opacity" TargetName="DraggingState" Value="0" />
              <Setter Property="Opacity" TargetName="Root" Value="0.5" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxdst:ScrollBarThemeKey ResourceKey=ScrollingCornerStyle}" TargetType="{x:Type Border}">
    <Setter Property="Background" Value="#FFF0F0F0" />
  </Style>
  <!-- TOUCH SCROLLBAR -->
  <Style x:Key="{dxdst:ScrollBarThemeKey ResourceKey=PageButtonTouchStyle}" TargetType="{x:Type RepeatButton}">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type RepeatButton}">
          <Rectangle Fill="{TemplateBinding Background}" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <!-- Thumb -->
  <Style x:Key="{dxdst:ScrollBarThemeKey ResourceKey=ScrollThumbHorzTouchStyle}" TargetType="{x:Type Thumb}">
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Rectangle Fill="{TemplateBinding Background}" Height="{TemplateBinding Height}" Margin="1" Opacity="0.5" Width="{TemplateBinding Width}" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxdst:ScrollBarThemeKey ResourceKey=ScrollThumbVertTouchStyle}" TargetType="{x:Type Thumb}">
    <Setter Property="OverridesDefaultStyle" Value="true" />
    <Setter Property="IsTabStop" Value="false" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Rectangle Fill="{TemplateBinding Background}" Height="{TemplateBinding Height}" Margin="1" Opacity="0.5" Width="{TemplateBinding Width}" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <!-- Scroll -->
  <ControlTemplate x:Key="{dxdst:ScrollBarThemeKey ResourceKey=ScrollBarVertTouchTemplate}" TargetType="{x:Type ScrollBar}">
    <Grid x:Name="Root" HorizontalAlignment="Right" SnapsToDevicePixels="True" Width="6">
      <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Visibility="{Binding Path=(dx:ScrollBarExtensions.ScrollBarMode), RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ScrollBarModeToScrollBackVisibilityConverter}}" />
      <Border x:Name="ScrollBarBackground" Background="{TemplateBinding Foreground}" Opacity="0.1">
        <Border.Visibility>
          <Binding ElementName="Root" Path="Width">
            <Binding.Converter>
              <dxmvvm:ObjectToObjectConverter DefaultTarget="Visible">
                <dxmvvm:MapItem Source="6" Target="Collapsed" />
              </dxmvvm:ObjectToObjectConverter>
            </Binding.Converter>
          </Binding>
        </Border.Visibility>
      </Border>
      <Track x:Name="PART_Track" HorizontalAlignment="Center" IsDirectionReversed="True" Visibility="{Binding Path=(dx:ScrollBarExtensions.ScrollBarMode), RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ScrollBarModeToHardWidthScrollConverter}}" Width="6">
        <Track.DecreaseRepeatButton>
          <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Style="{DynamicResource {dxdst:ScrollBarThemeKey ResourceKey=PageButtonTouchStyle}}" />
        </Track.DecreaseRepeatButton>
        <Track.IncreaseRepeatButton>
          <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Style="{DynamicResource {dxdst:ScrollBarThemeKey ResourceKey=PageButtonTouchStyle}}" />
        </Track.IncreaseRepeatButton>
        <Track.Thumb>
          <Thumb x:Name="thumb" Background="{TemplateBinding Foreground}" Style="{DynamicResource {dxdst:ScrollBarThemeKey ResourceKey=ScrollThumbVertTouchStyle}}" />
        </Track.Thumb>
      </Track>
    </Grid>
    <ControlTemplate.Triggers>
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="dx:ScrollBarExtensions.IsTouchScrollBarMode" Value="True" />
          <Condition Property="dx:ScrollBarExtensions.ScrollViewerMouseMoved" Value="True" />
          <Condition Property="IsMouseOver" Value="False" />
        </MultiTrigger.Conditions>
        <MultiTrigger.EnterActions>
          <BeginStoryboard>
            <Storyboard>
              <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="(UIElement.Visibility)">
                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}" />
              </ObjectAnimationUsingKeyFrames>
            </Storyboard>
          </BeginStoryboard>
        </MultiTrigger.EnterActions>
        <MultiTrigger.ExitActions>
          <BeginStoryboard>
            <Storyboard>
              <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="(UIElement.Visibility)">
                <DiscreteObjectKeyFrame KeyTime="0:0:01.5" Value="{x:Static Visibility.Collapsed}" />
              </ObjectAnimationUsingKeyFrames>
            </Storyboard>
          </BeginStoryboard>
        </MultiTrigger.ExitActions>
      </MultiTrigger>
      <Trigger Property="IsMouseOver" Value="True">
        <Trigger.EnterActions>
          <BeginStoryboard x:Name="sb">
            <Storyboard>
              <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="(UIElement.Visibility)">
                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}" />
              </ObjectAnimationUsingKeyFrames>
            </Storyboard>
          </BeginStoryboard>
          <BeginStoryboard>
            <Storyboard>
              <DoubleAnimation Duration="0:0:0.15" Storyboard.TargetName="Root" Storyboard.TargetProperty="(FrameworkElement.Width)" To="14" />
              <DoubleAnimation Duration="0:0:0.15" Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="(FrameworkElement.Width)" To="12" />
            </Storyboard>
          </BeginStoryboard>
        </Trigger.EnterActions>
        <Trigger.ExitActions>
          <RemoveStoryboard BeginStoryboardName="sb" />
          <BeginStoryboard>
            <Storyboard>
              <DoubleAnimation BeginTime="0:0:0.5" Duration="0:0:0.35" Storyboard.TargetName="Root" Storyboard.TargetProperty="(FrameworkElement.Width)" To="6" />
              <DoubleAnimation BeginTime="0:0:0.5" Duration="0:0:0.35" Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="(FrameworkElement.Width)" To="6" />
            </Storyboard>
          </BeginStoryboard>
        </Trigger.ExitActions>
      </Trigger>
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="dx:ScrollBarExtensions.ScrollViewerSizeChanged" Value="True" />
          <Condition Property="IsMouseOver" Value="False" />
        </MultiTrigger.Conditions>
        <MultiTrigger.EnterActions>
          <BeginStoryboard x:Name="sbSizeChanged">
            <Storyboard>
              <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="(UIElement.Visibility)">
                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Collapsed}" />
              </ObjectAnimationUsingKeyFrames>
            </Storyboard>
          </BeginStoryboard>
        </MultiTrigger.EnterActions>
        <MultiTrigger.ExitActions>
          <RemoveStoryboard BeginStoryboardName="sbSizeChanged" />
        </MultiTrigger.ExitActions>
      </MultiTrigger>
      <Trigger Property="IsEnabled" Value="False">
        <Setter Property="Opacity" TargetName="thumb" Value="0" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <ControlTemplate x:Key="{dxdst:ScrollBarThemeKey ResourceKey=ScrollBarHorzTouchTemplate}" TargetType="{x:Type ScrollBar}">
    <Grid x:Name="Root" Height="6" SnapsToDevicePixels="True" VerticalAlignment="Bottom">
      <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Visibility="{Binding Path=(dx:ScrollBarExtensions.ScrollBarMode), RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ScrollBarModeToScrollBackVisibilityConverter}}" />
      <Border x:Name="ScrollBarBackground" Background="{TemplateBinding Foreground}" Opacity="0.1">
        <Border.Visibility>
          <Binding ElementName="Root" Path="Height">
            <Binding.Converter>
              <dxmvvm:ObjectToObjectConverter DefaultTarget="Visible">
                <dxmvvm:MapItem Source="6" Target="Collapsed" />
              </dxmvvm:ObjectToObjectConverter>
            </Binding.Converter>
          </Binding>
        </Border.Visibility>
      </Border>
      <Track x:Name="PART_Track" Height="6" IsEnabled="{TemplateBinding IsMouseOver}" VerticalAlignment="Center" Visibility="{Binding Path=(dx:ScrollBarExtensions.ScrollBarMode), RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ScrollBarModeToHardWidthScrollConverter}}">
        <Track.DecreaseRepeatButton>
          <RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}" Style="{DynamicResource {dxdst:ScrollBarThemeKey ResourceKey=PageButtonTouchStyle}}" />
        </Track.DecreaseRepeatButton>
        <Track.IncreaseRepeatButton>
          <RepeatButton Command="{x:Static ScrollBar.PageRightCommand}" Style="{DynamicResource {dxdst:ScrollBarThemeKey ResourceKey=PageButtonTouchStyle}}" />
        </Track.IncreaseRepeatButton>
        <Track.Thumb>
          <Thumb Background="{TemplateBinding Foreground}" Style="{DynamicResource {dxdst:ScrollBarThemeKey ResourceKey=ScrollThumbHorzTouchStyle}}" />
        </Track.Thumb>
      </Track>
    </Grid>
    <ControlTemplate.Triggers>
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="dx:ScrollBarExtensions.IsTouchScrollBarMode" Value="True" />
          <Condition Property="dx:ScrollBarExtensions.ScrollViewerMouseMoved" Value="True" />
          <Condition Property="IsMouseOver" Value="False" />
        </MultiTrigger.Conditions>
        <MultiTrigger.EnterActions>
          <BeginStoryboard>
            <Storyboard>
              <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="(UIElement.Visibility)">
                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}" />
              </ObjectAnimationUsingKeyFrames>
            </Storyboard>
          </BeginStoryboard>
        </MultiTrigger.EnterActions>
        <MultiTrigger.ExitActions>
          <BeginStoryboard>
            <Storyboard>
              <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="(UIElement.Visibility)">
                <DiscreteObjectKeyFrame KeyTime="0:0:01.5" Value="{x:Static Visibility.Collapsed}" />
              </ObjectAnimationUsingKeyFrames>
            </Storyboard>
          </BeginStoryboard>
        </MultiTrigger.ExitActions>
      </MultiTrigger>
      <Trigger Property="IsMouseOver" Value="True">
        <Trigger.EnterActions>
          <BeginStoryboard x:Name="sbMouseOver">
            <Storyboard>
              <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="(UIElement.Visibility)">
                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}" />
              </ObjectAnimationUsingKeyFrames>
            </Storyboard>
          </BeginStoryboard>
          <BeginStoryboard>
            <Storyboard>
              <DoubleAnimation Duration="0:0:0.15" Storyboard.TargetName="Root" Storyboard.TargetProperty="(FrameworkElement.Height)" To="14" />
              <DoubleAnimation Duration="0:0:0.15" Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="(FrameworkElement.Height)" To="12" />
            </Storyboard>
          </BeginStoryboard>
        </Trigger.EnterActions>
        <Trigger.ExitActions>
          <RemoveStoryboard BeginStoryboardName="sbMouseOver" />
          <BeginStoryboard>
            <Storyboard>
              <DoubleAnimation BeginTime="0:0:0.5" Duration="0:0:0.5" Storyboard.TargetName="Root" Storyboard.TargetProperty="(FrameworkElement.Height)" To="6" />
              <DoubleAnimation BeginTime="0:0:0.5" Duration="0:0:0.5" Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="(FrameworkElement.Height)" To="6" />
            </Storyboard>
          </BeginStoryboard>
        </Trigger.ExitActions>
      </Trigger>
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="dx:ScrollBarExtensions.ScrollViewerSizeChanged" Value="True" />
          <Condition Property="IsMouseOver" Value="False" />
        </MultiTrigger.Conditions>
        <MultiTrigger.EnterActions>
          <BeginStoryboard x:Name="sbSizeChanged">
            <Storyboard>
              <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="(UIElement.Visibility)">
                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Collapsed}" />
              </ObjectAnimationUsingKeyFrames>
            </Storyboard>
          </BeginStoryboard>
        </MultiTrigger.EnterActions>
        <MultiTrigger.ExitActions>
          <RemoveStoryboard BeginStoryboardName="sbSizeChanged" />
        </MultiTrigger.ExitActions>
      </MultiTrigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=System.Windows.Controls.Primitives.ScrollBar}" TargetType="{x:Type ScrollBar}">
    <Setter Property="dx:ScrollBarExtensions.ListeningScrollBarThumbDragDelta" Value="True" />
    <Setter Property="dx:ScrollBarExtensions.ScrollViewerMouseMoved" Value="{Binding Path=(dx:ScrollBarExtensions.ScrollViewerMouseMoved), RelativeSource={RelativeSource TemplatedParent}}" />
    <Setter Property="dx:ScrollBarExtensions.ScrollViewerSizeChanged" Value="{Binding Path=(dx:ScrollBarExtensions.ScrollViewerSizeChanged), RelativeSource={RelativeSource TemplatedParent}}" />
    <Setter Property="dx:ScrollBarExtensions.IsTouchScrollBarMode">
      <Setter.Value>
        <Binding Path="(dx:ScrollBarExtensions.ScrollBarMode)" RelativeSource="{RelativeSource Self}">
          <Binding.Converter>
            <dx:EnumToObjectConverter DefaultValue="True">
              <dx:EnumToObjectConverter.Values>
                <dx:EnumObjectProvider x:Key="Standard" Value="False" />
              </dx:EnumToObjectConverter.Values>
            </dx:EnumToObjectConverter>
          </Binding.Converter>
        </Binding>
      </Setter.Value>
    </Setter>
    <Setter Property="Background" Value="{StaticResource ScrollBar.Vertical.Background}" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Width" Value="17" />
    <Setter Property="MinWidth" Value="13" />
    <Setter Property="Stylus.IsPressAndHoldEnabled" Value="False" />
    <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ScrollBar}">
          <Grid Background="{TemplateBinding Background}">
            <Grid.RowDefinitions>
              <RowDefinition MaxHeight="{DynamicResource {x:Static SystemParameters.VerticalScrollBarButtonHeightKey}}" />
              <RowDefinition Height="0.00001*" />
              <RowDefinition MaxHeight="{DynamicResource {x:Static SystemParameters.VerticalScrollBarButtonHeightKey}}" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="1">
              <RepeatButton x:Name="PART_LineUpButton" Command="{x:Static ScrollBar.LineUpCommand}" Content="{StaticResource UpArrowGeometry}" Opacity="0" Style="{StaticResource ScrollBarVerticalButtonStyle}" />
              <Grid x:Name="UpArrow" Grid.Row="0" Height="5" HorizontalAlignment="Center" SnapsToDevicePixels="True" VerticalAlignment="Center" Width="9">
                <Path Data="{StaticResource UpArrowGeometry}" Fill="{StaticResource ScrollBar.Path.Background}" HorizontalAlignment="Center" Stretch="Fill" VerticalAlignment="Center" />
              </Grid>
            </Grid>
            <Track x:Name="PART_Track" Grid.Row="1" IsDirectionReversed="True">
              <Track.DecreaseRepeatButton>
                <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Style="{StaticResource VerticalScrollBarPageButtonStyle}" />
              </Track.DecreaseRepeatButton>
              <Track.IncreaseRepeatButton>
                <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Style="{StaticResource VerticalScrollBarPageButtonStyle}" />
              </Track.IncreaseRepeatButton>
              <Track.Thumb>
                <Thumb Margin="1,0" Style="{StaticResource ScrollBarThumbVertical}" />
              </Track.Thumb>
            </Track>
            <Grid Grid.Row="2" Margin="1">
              <RepeatButton x:Name="PART_LineDownButton" Command="{x:Static ScrollBar.LineDownCommand}" Content="{StaticResource DownArrowGeometry}" Opacity="0" Style="{StaticResource ScrollBarVerticalButtonStyle}" />
              <Grid x:Name="DownArrow" Grid.Row="2" Height="5" HorizontalAlignment="Center" SnapsToDevicePixels="True" VerticalAlignment="Center" Width="9">
                <Path Data="{StaticResource DownArrowGeometry}" Fill="{StaticResource ScrollBar.Path.Background}" HorizontalAlignment="Center" Stretch="Fill" VerticalAlignment="Center" />
              </Grid>
            </Grid>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Opacity" TargetName="PART_LineUpButton" Value="1" />
              <Setter Property="Opacity" TargetName="PART_LineDownButton" Value="1" />
              <Setter Property="Visibility" TargetName="UpArrow" Value="Collapsed" />
              <Setter Property="Visibility" TargetName="DownArrow" Value="Collapsed" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="Orientation" Value="Vertical" />
          <Condition Property="dx:ScrollBarExtensions.IsTouchScrollBarMode" Value="True" />
        </MultiTrigger.Conditions>
        <MultiTrigger.Setters>
          <Setter Property="Width" Value="16" />
          <Setter Property="MinWidth" Value="16" />
          <Setter Property="Template" Value="{DynamicResource {dxdst:ScrollBarThemeKey ResourceKey=ScrollBarVertTouchTemplate}}" />
        </MultiTrigger.Setters>
      </MultiTrigger>
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="Orientation" Value="Horizontal" />
          <Condition Property="dx:ScrollBarExtensions.IsTouchScrollBarMode" Value="False" />
        </MultiTrigger.Conditions>
        <MultiTrigger.Setters>
          <Setter Property="Background" Value="{StaticResource ScrollBar.Horizontal.Background}" />
          <Setter Property="Width" Value="Auto" />
          <Setter Property="MinWidth" Value="0" />
          <Setter Property="Height" Value="17" />
          <Setter Property="MinHeight" Value="13" />
          <Setter Property="Template">
            <Setter.Value>
              <ControlTemplate TargetType="{x:Type ScrollBar}">
                <Grid Background="{TemplateBinding Background}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition MaxWidth="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarButtonWidthKey}}" />
                    <ColumnDefinition Width="0.00001*" />
                    <ColumnDefinition MaxWidth="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarButtonWidthKey}}" />
                  </Grid.ColumnDefinitions>
                  <Grid Grid.Column="0" Margin="1">
                    <RepeatButton x:Name="PART_LineLeftButton" Command="{x:Static ScrollBar.LineLeftCommand}" Content="{StaticResource LeftArrowGeometry}" Opacity="0" Style="{StaticResource ScrollBarHorizontalButtonStyle}" />
                    <Grid x:Name="LeftArrow" Grid.Column="0" Height="9" HorizontalAlignment="Center" SnapsToDevicePixels="True" VerticalAlignment="Center" Width="5">
                      <Path Data="{StaticResource LeftArrowGeometry}" Fill="{StaticResource ScrollBar.Path.Background}" HorizontalAlignment="Center" Stretch="Fill" VerticalAlignment="Center" />
                    </Grid>
                  </Grid>
                  <Track x:Name="PART_Track" Grid.Column="1">
                    <Track.DecreaseRepeatButton>
                      <RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}" Style="{StaticResource HorizontalScrollBarPageButtonStyle}" />
                    </Track.DecreaseRepeatButton>
                    <Track.IncreaseRepeatButton>
                      <RepeatButton Command="{x:Static ScrollBar.PageRightCommand}" Style="{StaticResource HorizontalScrollBarPageButtonStyle}" />
                    </Track.IncreaseRepeatButton>
                    <Track.Thumb>
                      <Thumb Margin="0,1,0,1" Style="{StaticResource ScrollBarThumbHorizontal}" />
                    </Track.Thumb>
                  </Track>
                  <Grid Grid.Column="2" Margin="1">
                    <RepeatButton x:Name="PART_LineRightButton" Command="{x:Static ScrollBar.LineRightCommand}" Content="{StaticResource RightArrowGeometry}" Opacity="0" Style="{StaticResource ScrollBarHorizontalButtonStyle}" />
                    <Grid x:Name="RightArrow" Height="9" HorizontalAlignment="Center" SnapsToDevicePixels="True" VerticalAlignment="Center" Width="5">
                      <Path Data="{StaticResource RightArrowGeometry}" Fill="{StaticResource ScrollBar.Path.Background}" HorizontalAlignment="Center" Stretch="Fill" VerticalAlignment="Center" />
                    </Grid>
                  </Grid>
                </Grid>
                <ControlTemplate.Triggers>
                  <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" TargetName="PART_LineLeftButton" Value="1" />
                    <Setter Property="Opacity" TargetName="PART_LineRightButton" Value="1" />
                    <Setter Property="Visibility" TargetName="RightArrow" Value="Collapsed" />
                    <Setter Property="Visibility" TargetName="LeftArrow" Value="Collapsed" />
                  </Trigger>
                </ControlTemplate.Triggers>
              </ControlTemplate>
            </Setter.Value>
          </Setter>
        </MultiTrigger.Setters>
      </MultiTrigger>
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="Orientation" Value="Horizontal" />
          <Condition Property="dx:ScrollBarExtensions.IsTouchScrollBarMode" Value="True" />
        </MultiTrigger.Conditions>
        <MultiTrigger.Setters>
          <Setter Property="Background" Value="{StaticResource ScrollBar.Horizontal.Background}" />
          <Setter Property="Width" Value="Auto" />
          <Setter Property="MinWidth" Value="0" />
          <Setter Property="Height" Value="16" />
          <Setter Property="MinHeight" Value="16" />
          <Setter Property="Template" Value="{DynamicResource {dxdst:ScrollBarThemeKey ResourceKey=ScrollBarHorzTouchTemplate}}" />
        </MultiTrigger.Setters>
      </MultiTrigger>
    </Style.Triggers>
  </Style>
</ResourceDictionary>