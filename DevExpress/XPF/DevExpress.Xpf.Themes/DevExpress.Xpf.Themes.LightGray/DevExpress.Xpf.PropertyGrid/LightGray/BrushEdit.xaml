<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                    xmlns:dxcn="clr-namespace:DevExpress.Xpf.Core.Native;assembly=DevExpress.Xpf.Core.v24.1"
                    xmlns:dxprgt="http://schemas.devexpress.com/winfx/2008/xaml/propertygrid/themekeys"
                    xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/DevExpress.Xpf.Themes.LightGray.v24.1;component/DevExpress.Xpf.PropertyGrid/Generic/BrushEdit.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <ControlTemplate x:Key="{dxprgt:PopupBrushEditThemeKey ResourceKey=BrushTypeButtonTemplate}" TargetType="{x:Type ToggleButton}">
        <Grid x:Name="Root" Margin="0,0,-2,-1">
            <Border x:Name="DefaultState"
                    Background="#FFB9BABF"
                    CornerRadius="2,2,0,0"
                    Margin="0,0,1,0">
                <Border x:Name="HoverStateGradientTop"
                        Background="#FFD5D8E0"
                        CornerRadius="1,1,0,0"
                        Margin="1,1,1,0"/>
            </Border>
            <Border x:Name="HoverState"
                    Background="#FFB9BABF"
                    CornerRadius="2,2,0,0"
                    Margin="0,0,1,0"
                    Opacity="0">
                <Border x:Name="GradientTop"
                        Background="#FFC2DBF7"
                        CornerRadius="1,1,0,0"
                        Margin="1,1,1,0"/>
            </Border>
            <Border x:Name="PressedState"
                    Background="#FFB9BABF"
                    CornerRadius="2,2,0,0"
                    Margin="0,-2,1,0"
                    Opacity="0">
                <Border Background="#FFFFFFFF"
                        CornerRadius="1,1,0,0"
                        Margin="1,1,1,-1">
                </Border>
            </Border>
            <ContentPresenter HorizontalAlignment="Center"
                              Margin="3"
                              VerticalAlignment="Center"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Opacity"
                        TargetName="HoverState"
                        Value="1"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Opacity"
                        TargetName="PressedState"
                        Value="1"/>
            </Trigger>
            <Trigger Property="ToggleButton.IsChecked" Value="True">
                <Setter Property="Opacity"
                        TargetName="PressedState"
                        Value="1"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity"
                        TargetName="Root"
                        Value="0.38"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

</ResourceDictionary> 