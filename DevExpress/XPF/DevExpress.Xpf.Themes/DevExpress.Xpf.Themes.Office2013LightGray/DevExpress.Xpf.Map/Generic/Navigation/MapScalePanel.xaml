<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/map/themekeys" xmlns:local="http://schemas.devexpress.com/winfx/2008/xaml/map" xmlns:dxdst="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys">
  <local:BoolToVisibilityConverter x:Key="VisibilityConverter" />
  <local:NavigationElementHorizontalAlignmentToHorizontalAlignmentConverter x:Key="HorizontalAlignmentConverter" />
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.Map.MapScalePanelLayoutControl}" TargetType="local:MapScalePanelLayoutControl">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="local:MapScalePanelLayoutControl">
          <local:MapScalePanel x:Name="PART_ScalePanel" ShowKilometersScale="{Binding Path=Options.ShowKilometersScale}" ShowMilesScale="{Binding Path=Options.ShowMilesScale}" KilometersScale="{Binding Path=KilometersScale}" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.Map.MapScalePanel}" TargetType="local:MapScalePanel">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="local:MapScalePanel">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid x:Name="PART_KilometersScale" Grid.Row="0" MinHeight="29" Margin="2" HorizontalAlignment="{Binding Path=Options.HorizontalAlignment, Converter={StaticResource HorizontalAlignmentConverter}}" Visibility="{Binding ShowKilometersScale, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource VisibilityConverter}}">
              <Border Background="{DynamicResource {dxt:MapBrushesThemeKey ResourceKey=OverlayBackground}}" CornerRadius="{DynamicResource {dxt:MapThemeKey ResourceKey=OverlayRadius}}" />
              <StackPanel Orientation="Vertical" Margin="16,6" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock TextWrapping="Wrap" Text="{TemplateBinding KilometersScaleText}" Foreground="{DynamicResource {dxt:MapBrushesThemeKey ResourceKey=OverlayElementForeground}}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-3,0,-2" />
                <Border BorderBrush="{DynamicResource {dxt:MapBrushesThemeKey ResourceKey=OverlayElementForeground}}" BorderThickness="1,0,1,2" Height="6" Width="{TemplateBinding KilometersScaleBarWidth}" HorizontalAlignment="Center" VerticalAlignment="Center" />
              </StackPanel>
            </Grid>
            <Grid x:Name="PART_MilesScale" Grid.Row="1" MinHeight="29" Margin="2" HorizontalAlignment="{Binding Path=Options.HorizontalAlignment, Converter={StaticResource HorizontalAlignmentConverter}}" Visibility="{Binding ShowMilesScale, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource VisibilityConverter}}">
              <Border Background="{DynamicResource {dxt:MapBrushesThemeKey ResourceKey=OverlayBackground}}" CornerRadius="{DynamicResource {dxt:MapThemeKey ResourceKey=OverlayRadius}}" />
              <StackPanel Orientation="Vertical" Margin="16,6" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock TextWrapping="Wrap" Text="{TemplateBinding MilesScaleText}" Foreground="{DynamicResource {dxt:MapBrushesThemeKey ResourceKey=OverlayElementForeground}}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-3,0,-2" />
                <Border BorderBrush="{DynamicResource {dxt:MapBrushesThemeKey ResourceKey=OverlayElementForeground}}" BorderThickness="1,0,1,2" Height="6" Width="{TemplateBinding MilesScaleBarWidth}" HorizontalAlignment="Center" VerticalAlignment="Center" />
              </StackPanel>
            </Grid>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>