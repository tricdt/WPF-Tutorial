<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:dxtEx="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" xmlns:dxec="clr-namespace:DevExpress.Xpf.Editors.Controls;assembly=DevExpress.Xpf.Core.v24.1" xmlns:dxedn="http://schemas.devexpress.com/winfx/2008/xaml/editors" xmlns:dxednc="http://schemas.devexpress.com/winfx/2008/xaml/editors/internal" xmlns:dxei="http://schemas.devexpress.com/winfx/2008/xaml/editors/internal" xmlns:dxet="http://schemas.devexpress.com/winfx/2008/xaml/editors/themekeys" xmlns:interactivity="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" xmlns:ui="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" xmlns:dxdst="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys">
  <dx:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
  <dx:EditorStringIdConverter x:Key="EditorStringIdConverter" />
  <CornerRadius x:Key="{dxet:DateNavigatorThemeKey ResourceKey=CellButtonCornerRadius}">0</CornerRadius>
  <DataTemplate x:Key="{dxet:DateNavigatorThemeKey ResourceKey=MonthCellTemplate}">
    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Path=Text}" />
  </DataTemplate>
  <DataTemplate x:Key="{dxet:DateNavigatorThemeKey ResourceKey=YearCellTemplate}">
    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Path=Text}" />
  </DataTemplate>
  <DataTemplate x:Key="{dxet:DateNavigatorThemeKey ResourceKey=YearsCellTemplate}">
    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Path=Text}" />
  </DataTemplate>
  <DataTemplate x:Key="{dxet:DateNavigatorThemeKey ResourceKey=YearsRangeCellTemplate}">
    <TextBlock TextWrapping="WrapWithOverflow" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Path=Text}" />
  </DataTemplate>
  <dx:EnumToBoolConverter x:Key="EnumToBoolConverter" />
  <ControlTemplate x:Key="{dxet:DateNavigatorThemeKey ResourceKey=CalendarContentControlTemplate}" TargetType="{x:Type dxednc:DateNavigatorCalendarContentControl}">
    <Border Margin="{TemplateBinding Padding}">
      <ItemsPresenter x:Name="PART_ItemsPresenter" />
    </Border>
  </ControlTemplate>
  <ControlTemplate x:Key="{dxet:DateNavigatorThemeKey ResourceKey=CalendarContentControlMonthTemplate}" TargetType="{x:Type dxednc:DateNavigatorCalendarContentControl}">
    <Border Margin="{TemplateBinding Padding}">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ItemsControl ItemsSource="{TemplateBinding DayNames}" x:Name="PART_WeekdayAbbreviationItemsControl" HorizontalAlignment="Right" Grid.Row="0" Grid.Column="2">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <dxei:DateNavigatorCalendarMonthLabelsPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <TextBlock Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=WeekdayAbbreviationStyle}}" Text="{Binding}" />
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <ContentControl Template="{DynamicResource {dxet:DateNavigatorThemeKey IsVisibleInBlend=True, ResourceKey=MonthInfoDelimeter}}" Grid.Column="2" Grid.Row="1" Margin="0,0,0,5" dx:FocusHelper2.Focusable="False" />
        <ItemsControl ItemsSource="{TemplateBinding WeekNumbers}" x:Name="PART_WeekNumbersItemsControl" Visibility="{Binding Path=(dxedn:DateNavigator.Navigator).ShowWeekNumbers, RelativeSource={RelativeSource Self}, Converter={StaticResource BoolToVisibilityConverter}}" Grid.Row="2" Grid.Column="0">
          <ui:Interaction.Behaviors>
            <ui:EventToCommand Event="MouseLeftButtonDown" Command="{Binding Path=WeekNumberClickCommand, RelativeSource={RelativeSource TemplatedParent}}" PassEventArgsToCommand="True" />
          </ui:Interaction.Behaviors>
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <dxei:DateNavigatorCalendarMonthLabelsPanel Orientation="Vertical" />
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <TextBlock Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=WeekNumberStyle}}" Text="{Binding}" />
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <ContentControl Template="{DynamicResource {dxet:DateNavigatorThemeKey IsVisibleInBlend=True, ResourceKey=WeekNumberDelimeter}}" Visibility="{Binding Path=(dxedn:DateNavigator.Navigator).ShowWeekNumbers, RelativeSource={RelativeSource Self}, Converter={StaticResource BoolToVisibilityConverter}}" Grid.Column="1" Grid.Row="2" dx:FocusHelper2.Focusable="False" />
        <ItemsPresenter HorizontalAlignment="Right" Grid.Row="2" Grid.Column="2" x:Name="PART_ItemsPresenter" />
      </Grid>
    </Border>
  </ControlTemplate>
  <ControlTemplate x:Key="{dxet:DateNavigatorThemeKey ResourceKey=CalendarControlTemplate}" TargetType="{x:Type dxednc:DateNavigatorCalendarControl}">
    <Grid Margin="{TemplateBinding Padding}">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <Border Grid.Row="0" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=OutlookHeaderStyle}}" x:Name="PART_HeaderBorder" Margin="0,0,0,4">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <RepeatButton x:Name="PART_PrevButton" Command="{Binding Path=ArrowLeftCommand, RelativeSource={RelativeSource TemplatedParent}}" Grid.Row="0" Grid.Column="0" Visibility="Collapsed" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=LeftArrowStyle}}">&lt;</RepeatButton>
          <Grid x:Name="PART_Header" Grid.Row="0" Grid.Column="1">
            <TextBlock x:Name="PART_HeaderText" Style="{DynamicResource {dxet:DateNavigatorThemeKey IsVisibleInBlend=True, ResourceKey=CalendarCurrentDateStyle}}" Text="{TemplateBinding CurrentDateText}" />
            <Button x:Name="PART_HeaderButton" Visibility="Collapsed" Command="{Binding Path=HeaderClickCommand, RelativeSource={RelativeSource TemplatedParent}}" Content="{TemplateBinding CurrentDateText}" dx:FocusHelper2.Focusable="False" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=CurrentDateButtonStyle}}" />
          </Grid>
          <RepeatButton x:Name="PART_NextButton" Command="{Binding Path=ArrowRightCommand, RelativeSource={RelativeSource TemplatedParent}}" Grid.Row="0" Grid.Column="2" Visibility="Collapsed" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=RightArrowStyle}}">&gt;</RepeatButton>
        </Grid>
      </Border>
      <Border Grid.Row="1" Background="{TemplateBinding Background}">
        <dxei:DateNavigatorCalendarContentControl x:Name="PART_CalendarContent" FirstItemOffset="{TemplateBinding SkipItems}" FirstDayOfWeek="{TemplateBinding FirstDayOfWeek}" ItemsSource="{TemplateBinding Cells}" State="{TemplateBinding State}" DateTime="{TemplateBinding DateTime}" />
      </Border>
    </Grid>
    <ControlTemplate.Triggers>
      <MultiDataTrigger>
        <MultiDataTrigger.Conditions>
          <Condition Binding="{Binding Path=HeaderType, RelativeSource={RelativeSource Self}}" Value="First" />
          <Condition Binding="{Binding Path=(dxedn:DateNavigator.Navigator).DisplayMode, RelativeSource={RelativeSource Self}}" Value="Outlook" />
        </MultiDataTrigger.Conditions>
        <Setter TargetName="PART_PrevButton" Property="Visibility" Value="Visible" />
      </MultiDataTrigger>
      <MultiDataTrigger>
        <MultiDataTrigger.Conditions>
          <Condition Binding="{Binding Path=HeaderType, RelativeSource={RelativeSource Self}}" Value="Both" />
          <Condition Binding="{Binding Path=(dxedn:DateNavigator.Navigator).DisplayMode, RelativeSource={RelativeSource Self}}" Value="Outlook" />
        </MultiDataTrigger.Conditions>
        <Setter TargetName="PART_PrevButton" Property="Visibility" Value="Visible" />
        <Setter TargetName="PART_NextButton" Property="Visibility" Value="Visible" />
      </MultiDataTrigger>
      <MultiDataTrigger>
        <MultiDataTrigger.Conditions>
          <Condition Binding="{Binding Path=HeaderType, RelativeSource={RelativeSource Self}}" Value="Last" />
          <Condition Binding="{Binding Path=(dxedn:DateNavigator.Navigator).DisplayMode, RelativeSource={RelativeSource Self}}" Value="Outlook" />
        </MultiDataTrigger.Conditions>
        <Setter TargetName="PART_NextButton" Property="Visibility" Value="Visible" />
      </MultiDataTrigger>
      <DataTrigger Binding="{Binding Path=(dxedn:DateNavigator.Navigator).DisplayMode, RelativeSource={RelativeSource Self}}" Value="Outlook">
        <Setter TargetName="PART_HeaderText" Property="Visibility" Value="Collapsed" />
        <Setter TargetName="PART_HeaderButton" Property="Visibility" Value="Visible" />
      </DataTrigger>
      <DataTrigger Binding="{Binding Path=(dxedn:DateNavigator.Navigator).DisplayMode, RelativeSource={RelativeSource Self}}" Value="Classic">
        <Setter TargetName="PART_HeaderBorder" Property="Background" Value="Transparent" />
      </DataTrigger>
      <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
        <Setter TargetName="PART_Header" Property="Margin" Value="0,5" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <dxei:DateNavigatorCellButtonStateConverter x:Key="DateNavigatorCellButtonStateConverter" />
  <ControlTemplate x:Key="{dxet:DateNavigatorThemeKey ResourceKey=CalendarCellButtonTemplate}" TargetType="{x:Type dxednc:DateNavigatorCellButton}">
    <Grid Opacity="{Binding Path=Appearance.Opacity, RelativeSource={RelativeSource TemplatedParent}}">
      <Border x:Name="PART_Border" CornerRadius="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=CellButtonCornerRadius}}" BorderThickness="{Binding Path=Appearance.BorderThickness, RelativeSource={RelativeSource TemplatedParent}}" BorderBrush="{Binding Path=Appearance.BorderBrush, RelativeSource={RelativeSource TemplatedParent}}" Background="{Binding Path=Appearance.Background, RelativeSource={RelativeSource TemplatedParent}}" />
      <ContentPresenter x:Name="PART_Content" Margin="{TemplateBinding Padding}" TextBlock.Foreground="{Binding Path=Appearance.Foreground, RelativeSource={RelativeSource TemplatedParent}}" TextBlock.FontWeight="{Binding Path=Appearance.FontWeight, RelativeSource={RelativeSource TemplatedParent}}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" RecognizesAccessKey="True" />
    </Grid>
  </ControlTemplate>
  <ControlTemplate x:Key="{dxet:DateNavigatorThemeKey ResourceKey=NewCalendarTemplate}" TargetType="{x:Type dxedn:DateNavigator}">
    <Grid Background="{TemplateBinding Background}">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <Border Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=HeaderStyle}}" x:Name="PART_Header">
        <Grid Grid.Row="0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <RepeatButton Grid.Column="0" HorizontalAlignment="Left" Content="&lt;" x:Name="PART_ArrowLeft" dx:FocusHelper2.Focusable="False" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=LeftArrowStyle}}" />
          <Button Grid.Column="1" Content="{TemplateBinding CurrentDateText}" x:Name="PART_CurrentDateButton" dx:FocusHelper2.Focusable="False" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=CurrentDateButtonStyle}}" />
          <RepeatButton Grid.Column="2" HorizontalAlignment="Right" Content="&gt;" x:Name="PART_ArrowRight" dx:FocusHelper2.Focusable="False" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=RightArrowStyle}}" />
        </Grid>
      </Border>
      <dxei:DateNavigatorPanel Tag="NewCalendar" Grid.Row="1" VerticalAlignment="Top">
        <Grid x:Name="PART_NavigatorContent">
          <!--<dxei:DateNavigatorPanel x:Name="PART_Panel" ColumnCount="{TemplateBinding ColumnCount}" RowCount="{TemplateBinding RowCount}" />-->
          <dxei:DateNavigatorContent x:Name="PART_Content" ColumnCount="{TemplateBinding ColumnCount}" RowCount="{TemplateBinding RowCount}" IsHitTestVisible="False" />
          <!--<dxednc:DateNavigatorContent Opacity="0" State="Year" x:Name="PART_ContentYear" ColumnCount="{TemplateBinding ColumnCount}" RowCount="{TemplateBinding RowCount}" IsHitTestVisible="False"/>
                    <dxednc:DateNavigatorContent Opacity="0" State="Years" x:Name="PART_ContentYears" ColumnCount="{TemplateBinding ColumnCount}" RowCount="{TemplateBinding RowCount}" IsHitTestVisible="False"/>
                    <dxednc:DateNavigatorContent Opacity="0" State="YearsRange" x:Name="PART_ContentYearsRange" ColumnCount="{TemplateBinding ColumnCount}" RowCount="{TemplateBinding RowCount}" IsHitTestVisible="False"/>-->
        </Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
          <Button x:Name="PART_TodayButton" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=TodayButtonStyle}}" />
          <Button x:Name="PART_ClearButton" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=ClearButtonStyle}}" Content="{Binding Path=IsTabStop, RelativeSource={RelativeSource Self}, ConverterParameter=Clear, Converter={StaticResource EditorStringIdConverter}}" />
        </StackPanel>
      </dxei:DateNavigatorPanel>
    </Grid>
    <ControlTemplate.Triggers>
      <Trigger Property="DisplayMode" Value="Outlook">
        <Setter TargetName="PART_Header" Property="Visibility" Value="Collapsed" />
      </Trigger>
      <Trigger Property="IsEnabled" Value="False">
        <Setter TargetName="PART_NavigatorContent" Property="Opacity" Value="0.38" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <ControlTemplate x:Key="{dxet:DateNavigatorThemeKey ResourceKey=Template}" TargetType="{x:Type dxedn:DateNavigator}">
    <Grid Background="{TemplateBinding Background}">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <Border Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=HeaderStyle}}" x:Name="PART_Header">
        <Grid Grid.Row="0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <RepeatButton Grid.Column="0" HorizontalAlignment="Left" Content="&lt;" x:Name="PART_ArrowLeft" dx:FocusHelper2.Focusable="False" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=LeftArrowStyle}}" />
          <Button Grid.Column="1" Content="{TemplateBinding CurrentDateText}" x:Name="PART_CurrentDateButton" dx:FocusHelper2.Focusable="False" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=CurrentDateButtonStyle}}" />
          <RepeatButton Grid.Column="2" HorizontalAlignment="Right" Content="&gt;" x:Name="PART_ArrowRight" dx:FocusHelper2.Focusable="False" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=RightArrowStyle}}" />
        </Grid>
      </Border>
      <dxei:DateNavigatorPanel Tag="OldCalendar" Grid.Row="1" VerticalAlignment="Top">
        <Grid x:Name="PART_NavigatorContent">
          <!--<dxei:DateNavigatorPanel x:Name="PART_Panel" ColumnCount="{TemplateBinding ColumnCount}" RowCount="{TemplateBinding RowCount}" />-->
          <dxei:DateNavigatorContent Opacity="0" State="Month" x:Name="PART_ContentMonth" ColumnCount="{TemplateBinding ColumnCount}" RowCount="{TemplateBinding RowCount}" IsHitTestVisible="False" />
          <dxei:DateNavigatorContent Opacity="0" State="Year" x:Name="PART_ContentYear" ColumnCount="{TemplateBinding ColumnCount}" RowCount="{TemplateBinding RowCount}" IsHitTestVisible="False" />
          <dxei:DateNavigatorContent Opacity="0" State="Years" x:Name="PART_ContentYears" ColumnCount="{TemplateBinding ColumnCount}" RowCount="{TemplateBinding RowCount}" IsHitTestVisible="False" />
          <dxei:DateNavigatorContent Opacity="0" State="YearsRange" x:Name="PART_ContentYearsRange" ColumnCount="{TemplateBinding ColumnCount}" RowCount="{TemplateBinding RowCount}" IsHitTestVisible="False" />
        </Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
          <Button x:Name="PART_TodayButton" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=TodayButtonStyle}}" />
          <Button x:Name="PART_ClearButton" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=ClearButtonStyle}}" Content="{Binding Path=IsTabStop, RelativeSource={RelativeSource Self}, ConverterParameter=Clear, Converter={StaticResource EditorStringIdConverter}}" />
        </StackPanel>
      </dxei:DateNavigatorPanel>
    </Grid>
    <ControlTemplate.Triggers>
      <Trigger Property="DisplayMode" Value="Outlook">
        <Setter TargetName="PART_Header" Property="Visibility" Value="Collapsed" />
      </Trigger>
      <Trigger Property="IsEnabled" Value="False">
        <Setter TargetName="PART_NavigatorContent" Property="Opacity" Value="0.38" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <ControlTemplate x:Key="{dxet:DateNavigatorContentThemeKey ResourceKey=Template}" TargetType="{x:Type dxednc:DateNavigatorContent}">
    <dxei:DateNavigatorContentPanel x:Name="PART_Panel" ColumnCount="{TemplateBinding ColumnCount}" RowCount="{TemplateBinding RowCount}" />
  </ControlTemplate>
  <ControlTemplate x:Key="{dxet:DateNavigatorThemeKey ResourceKey=CalendarTemplate}" TargetType="{x:Type dxednc:DateNavigatorCalendar}">
    <Grid Margin="{TemplateBinding Padding}">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <Border Grid.Row="0" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=OutlookHeaderStyle}}" x:Name="PART_HeaderBorder" Margin="0,0,0,4">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <RepeatButton x:Name="PART_PrevButton" Command="{Binding Path=ArrowLeftCommand, RelativeSource={RelativeSource TemplatedParent}}" Grid.Row="0" Grid.Column="0" Visibility="Collapsed" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=LeftArrowStyle}}">&lt;</RepeatButton>
          <Grid x:Name="PART_Header" Grid.Row="0" Grid.Column="1">
            <TextBlock x:Name="PART_HeaderText" Style="{DynamicResource {dxet:DateNavigatorThemeKey IsVisibleInBlend=True, ResourceKey=CalendarCurrentDateStyle}}" Text="{TemplateBinding CurrentDateText}" />
            <Button x:Name="PART_HeaderButton" Visibility="Collapsed" Command="{Binding Path=HeaderClickCommand, RelativeSource={RelativeSource TemplatedParent}}" Content="{TemplateBinding CurrentDateText}" dx:FocusHelper2.Focusable="False" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=CurrentDateButtonStyle}}" />
          </Grid>
          <RepeatButton x:Name="PART_NextButton" Command="{Binding Path=ArrowRightCommand, RelativeSource={RelativeSource TemplatedParent}}" Grid.Row="0" Grid.Column="2" Visibility="Collapsed" Style="{DynamicResource {dxet:DateNavigatorThemeKey ResourceKey=RightArrowStyle}}">&gt;</RepeatButton>
        </Grid>
      </Border>
      <Border Grid.Row="1" Background="{TemplateBinding Background}">
        <Grid>
          <Grid x:Name="PART_ContentPaddingPanel" />
          <dxei:DateNavigatorCalendarContent x:Name="PART_Content" dx:FocusHelper2.Focusable="False" />
        </Grid>
      </Border>
    </Grid>
    <ControlTemplate.Triggers>
      <MultiDataTrigger>
        <MultiDataTrigger.Conditions>
          <Condition Binding="{Binding Path=HeaderType, RelativeSource={RelativeSource Self}}" Value="First" />
          <Condition Binding="{Binding Path=(dxedn:DateNavigator.Navigator).DisplayMode, RelativeSource={RelativeSource Self}}" Value="Outlook" />
        </MultiDataTrigger.Conditions>
        <Setter TargetName="PART_PrevButton" Property="Visibility" Value="Visible" />
      </MultiDataTrigger>
      <MultiDataTrigger>
        <MultiDataTrigger.Conditions>
          <Condition Binding="{Binding Path=HeaderType, RelativeSource={RelativeSource Self}}" Value="Both" />
          <Condition Binding="{Binding Path=(dxedn:DateNavigator.Navigator).DisplayMode, RelativeSource={RelativeSource Self}}" Value="Outlook" />
        </MultiDataTrigger.Conditions>
        <Setter TargetName="PART_PrevButton" Property="Visibility" Value="Visible" />
        <Setter TargetName="PART_NextButton" Property="Visibility" Value="Visible" />
      </MultiDataTrigger>
      <MultiDataTrigger>
        <MultiDataTrigger.Conditions>
          <Condition Binding="{Binding Path=HeaderType, RelativeSource={RelativeSource Self}}" Value="Last" />
          <Condition Binding="{Binding Path=(dxedn:DateNavigator.Navigator).DisplayMode, RelativeSource={RelativeSource Self}}" Value="Outlook" />
        </MultiDataTrigger.Conditions>
        <Setter TargetName="PART_NextButton" Property="Visibility" Value="Visible" />
      </MultiDataTrigger>
      <DataTrigger Binding="{Binding Path=(dxedn:DateNavigator.Navigator).DisplayMode, RelativeSource={RelativeSource Self}}" Value="Outlook">
        <Setter TargetName="PART_HeaderText" Property="Visibility" Value="Collapsed" />
        <Setter TargetName="PART_HeaderButton" Property="Visibility" Value="Visible" />
      </DataTrigger>
      <DataTrigger Binding="{Binding Path=(dxedn:DateNavigator.Navigator).DisplayMode, RelativeSource={RelativeSource Self}}" Value="Classic">
        <Setter TargetName="PART_HeaderBorder" Property="Background" Value="Transparent" />
      </DataTrigger>
      <Trigger Property="dx:ThemeManager.IsTouchEnabled" Value="True">
        <Setter TargetName="PART_Header" Property="Margin" Value="0,5" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
</ResourceDictionary>