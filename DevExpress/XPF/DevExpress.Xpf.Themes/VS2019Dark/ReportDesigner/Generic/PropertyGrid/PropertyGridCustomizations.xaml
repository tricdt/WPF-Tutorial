<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" xmlns:dxprg="http://schemas.devexpress.com/winfx/2008/xaml/propertygrid" xmlns:dxprgt="http://schemas.devexpress.com/winfx/2008/xaml/propertygrid/themekeys" xmlns:designer="http://schemas.devexpress.com/winfx/2008/xaml/reports/userdesigner" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:localization="http://schemas.devexpress.com/winfx/2008/xaml/reports/userdesigner" xmlns:designernative="http://schemas.devexpress.com/winfx/2008/xaml/reports/userdesigner/internal" xmlns:propertygrid="http://schemas.devexpress.com/winfx/2008/xaml/reports/userdesigner/internal" xmlns:diagram="http://schemas.devexpress.com/winfx/2008/xaml/reports/userdesigner/internal" xmlns:sys="clr-namespace:System;assembly=mscorlib" xmlns:dxdst="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys">
  <Style x:Key="TrackBarEditItemStyle" TargetType="{x:Type dxe:TrackBarEdit}">
    <Setter Property="Minimum" Value="0" />
    <Setter Property="Maximum" Value="100" />
    <Setter Property="EditValueType" Value="{x:Type sys:Int32}" />
  </Style>
  <Style x:Key="ShapeBaseEditItem" TargetType="{x:Type dxe:SpinEdit}">
    <Setter Property="MinValue" Value="0" />
    <Setter Property="MaxValue" Value="100" />
    <Setter Property="IsFloatValue" Value="False" />
    <Setter Property="EditValueType" Value="{x:Type sys:Int32}" />
  </Style>
  <DataTemplate x:Key="simpleHeaderTemplate">
    <TextBlock Text="{Binding}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
  </DataTemplate>
  <DataTemplate x:Key="menuBarItemContentTemplate">
    <Border Margin="0,0,15,0">
      <TextBlock Text="{Binding}" FontWeight="{DXBinding '@a($dxb:BarItemLinkInfo).Item.($TextElement.FontWeight)'}" />
    </Border>
  </DataTemplate>
  <DataTemplate x:Key="expressionMenuButtonTemplate">
    <ContentControl>
      <dxb:BarButtonItem Content="{Binding Caption}" IsEnabled="{Binding IsDisabled, Converter={dxmvvm:BooleanNegationConverter}}" TextElement.FontWeight="{Binding HasValue, Converter={dxmvvm:BooleanToObjectConverter TrueValue={x:Static FontWeights.Bold}, FalseValue={x:Static FontWeights.Normal}}}" Command="{Binding Command}" CommandParameter="{Binding CommandParameter}" ContentTemplate="{StaticResource menuBarItemContentTemplate}" />
    </ContentControl>
  </DataTemplate>
  <DataTemplate x:Key="expressionMenuSplitButtonTemplate">
    <ContentControl>
      <dxb:BarSubItem Content="{Binding Caption}" ItemLinksSource="{Binding Commands}" ItemTemplate="{StaticResource expressionMenuButtonTemplate}" ContentTemplate="{StaticResource menuBarItemContentTemplate}" TextElement.FontWeight="{Binding HasValue, Converter={dxmvvm:BooleanToObjectConverter TrueValue={x:Static FontWeights.Bold}, FalseValue={x:Static FontWeights.Normal}}}" />
    </ContentControl>
  </DataTemplate>
  <DataTemplate x:Key="popupMenuItemContentTemplate">
    <TextBlock Margin="0,0,18,0" Text="{Binding}" VerticalAlignment="Center" />
  </DataTemplate>
  <dxmvvm:BooleanToObjectConverter x:Key="propertiesGlyphConverter" FalseValue="{dx:SvgImageSource Uri='/DevExpress.Xpf.ReportDesigner.v24.1;component/Images/SVG/PropertyGrid/Properties.svg'}" TrueValue="{dx:SvgImageSource Uri='/DevExpress.Xpf.ReportDesigner.v24.1;component/Images/SVG/PropertyGrid/FavoriteProperties.svg'}" />
  <DataTemplate x:Key="commonPanelTemplate">
    <dxb:ToolBarControl ShowBackground="False" ShowDragWidget="False" AllowCustomizationMenu="False" AllowQuickCustomization="False" AllowHide="False" Visibility="{Binding Path=(dxprg:PropertyGridHelper.PropertyGrid).ShowToolPanel, RelativeSource={RelativeSource Self}, Converter={dx:BooleanToVisibilityConverter}}">
      <dxb:BarCheckItem GroupIndex="3120" Hint="{localization:ReportDesignerStringId StringId=PropertyGrid_BarControl_BarItem_Categorized}" IsChecked="{Binding Path=(dxprg:PropertyGridHelper.PropertyGrid).ShowCategories, Converter={designernative:ShowCategoriesToIsCheckedConverter ShowCategories=Visible}, Mode=TwoWay, RelativeSource={RelativeSource Self}}">
        <dxb:BarCheckItem.GlyphTemplate>
          <DataTemplate>
            <Path x:Name="PART_GroupView" Stretch="Fill" Fill="{Binding Path=(TextElement.Foreground), RelativeSource={RelativeSource Self}}" Data="F1 M 2,2L 7,2L 7,7L 2,7L 2,2 Z M 2,9.00001L 6.99999,9.00001L 6.99999,14L 2,14L 2,9.00001 Z M 8,4.00001L 14,4.00001L 14,5.00001L 8,5.00001L 8,4.00001 Z M 8,11L 14,11L 14,12L 8,12L 8,11 Z M 4,3L 4,4L 3,4L 3,5L 4,5L 4,6L 5,6L 5,5L 6,5L 6,4L 5,4L 5,3L 4,3 Z M 4,10L 4,11L 3,11L 3,12L 4,12L 4,13L 5,13L 5,12L 6,12L 6,11L 5,11L 5,10L 4,10 Z " Width="12" Height="12" />
          </DataTemplate>
        </dxb:BarCheckItem.GlyphTemplate>
      </dxb:BarCheckItem>
      <dxb:BarCheckItem GroupIndex="3120" Hint="{localization:ReportDesignerStringId StringId=PropertyGrid_BarControl_BarItem_Alphabetical}" IsChecked="{Binding Path=(dxprg:PropertyGridHelper.PropertyGrid).ShowCategories, Converter={designernative:ShowCategoriesToIsCheckedConverter ShowCategories=Hidden}, Mode=TwoWay, RelativeSource={RelativeSource Self}}">
        <dxb:BarCheckItem.GlyphTemplate>
          <DataTemplate>
            <Path x:Name="PART_ListView" Stretch="Fill" Fill="{Binding Path=(TextElement.Foreground), RelativeSource={RelativeSource Self}}" Data="F1 M 2,3L 14,3L 14,4L 2,4L 2,3 Z M 2,6L 14,6L 14,7L 2,7L 2,6 Z M 2,9L 14,9L 14,10L 2,10L 2,9 Z M 2,12L 14,12L 14,13L 2,13L 2,12 Z " Width="12" Height="10" />
          </DataTemplate>
        </dxb:BarCheckItem.GlyphTemplate>
      </dxb:BarCheckItem>
      <dxb:BarItemSeparator />
      <dxb:BarSplitCheckItem Hint="Show Favorite Properties" Glyph="{dx:SvgImageSource Uri='/DevExpress.Xpf.ReportDesigner.v24.1;component/Images/SVG/PropertyGrid/FavoriteProperties.svg'}" IsChecked="{Binding Path=(dxprg:PropertyGridHelper.PropertyGrid).ShowFavoriteProperties, RelativeSource={RelativeSource Self}}" IsVisible="{Binding Path=(diagram:XtraReportDiagramItem.ReportModel).DataBindingMode, RelativeSource={RelativeSource Self}, Converter={designernative:BindingModeAndExpressionsToBooleanConverter Inverse=True}, FallbackValue=False}">
        <dxb:BarSplitCheckItem.PopupControl>
          <dxb:PopupMenu>
            <dxb:BarButtonItem Content="Edit Favorite Properties..." Command="{Binding Path=(designer:ReportDesigner.Designer).ActualCommands.EditFavoritePropertiesCommand, RelativeSource={RelativeSource Self}}" ContentTemplate="{StaticResource popupMenuItemContentTemplate}" />
          </dxb:PopupMenu>
        </dxb:BarSplitCheckItem.PopupControl>
      </dxb:BarSplitCheckItem>
      <dxb:BarSplitCheckItem Glyph="{Binding Path=(dxprg:PropertyGridHelper.PropertyGrid).ShowFavoriteProperties, RelativeSource={RelativeSource Self}, Converter={StaticResource propertiesGlyphConverter}}" GroupIndex="52416" Hint="{localization:ReportDesignerStringId StringId=Properties}" IsChecked="{Binding Path=(dxprg:PropertyGridHelper.PropertyGrid).ExpressionTabIsActive, RelativeSource={RelativeSource Self}, FallbackValue=False, Converter={dxmvvm:BooleanNegationConverter}}" IsVisible="{Binding Path=(diagram:XtraReportDiagramItem.ReportModel).DataBindingMode, RelativeSource={RelativeSource Self}, Converter={designernative:BindingModeAndExpressionsToBooleanConverter}}">
        <dxb:BarSplitCheckItem.PopupControl>
          <dxb:PopupMenu>
            <dxb:BarCheckItem Content="Show Favorite Properties" Glyph="{dx:SvgImageSource Uri='/DevExpress.Xpf.ReportDesigner.v24.1;component/Images/SVG/PropertyGrid/FavoriteProperties.svg'}" ContentTemplate="{StaticResource popupMenuItemContentTemplate}" IsChecked="{Binding Path=(dxprg:PropertyGridHelper.PropertyGrid).ShowFavoriteProperties, RelativeSource={RelativeSource Self}}" />
            <dxb:BarButtonItem Content="Edit Favorite Properties..." Command="{Binding Path=(designer:ReportDesigner.Designer).ActualCommands.EditFavoritePropertiesCommand, RelativeSource={RelativeSource Self}}" ContentTemplate="{StaticResource popupMenuItemContentTemplate}" />
          </dxb:PopupMenu>
        </dxb:BarSplitCheckItem.PopupControl>
      </dxb:BarSplitCheckItem>
      <dxb:BarCheckItem Hint="{localization:ReportDesignerStringId StringId=PropertyGrid_TabExpressions}" Glyph="{dx:SvgImageSource Uri='/DevExpress.Xpf.ReportDesigner.v24.1;component/Images/SVG/PropertyGrid/Expressions.svg'}" GroupIndex="52416">
        <dxb:BarCheckItem.IsVisible>
          <MultiBinding Converter="{designernative:BindingModeAndExpressionsToBooleanConverter}">
            <Binding Path="(diagram:XtraReportDiagramItem.ReportModel).DataBindingMode" RelativeSource="{RelativeSource Self}" />
            <Binding Path="(dxprg:PropertyGridHelper.PropertyGrid).ShowExpressionsInSeparateTab" RelativeSource="{RelativeSource Self}" />
          </MultiBinding>
        </dxb:BarCheckItem.IsVisible>
      </dxb:BarCheckItem>
      <dxb:BarItemSeparator />
    </dxb:ToolBarControl>
  </DataTemplate>
  <ControlTemplate x:Key="emptyButtonTemplate" TargetType="ToggleButton">
    <Grid>
      <Border Background="Gray" Opacity="0" Name="background" />
      <ContentPresenter VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Margin="{TemplateBinding Padding}" Name="glyphPresenter" Opacity="0.5" Content="{TemplateBinding Content}" />
    </Grid>
    <ControlTemplate.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter TargetName="glyphPresenter" Property="Opacity" Value="1" />
        <Setter TargetName="background" Property="Opacity" Value="0.1" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <DataTemplate x:Key="commonCategoryTab">
    <Grid ToolTip="{Binding Header}" MaxWidth="65">
      <dx:DXImage Source="{Binding Header, Converter={designernative:CategoryNameToIconConverter}}" Height="22" Width="22" Name="glyph" Visibility="{Binding Source, Converter={designernative:ObjectToVisibilityConverter}, RelativeSource={RelativeSource Self}}" />
      <TextBlock VerticalAlignment="Center" Text="{Binding Header}" TextTrimming="CharacterEllipsis" Visibility="{Binding Source, ElementName=glyph, Converter={designernative:ObjectToVisibilityConverter Inverse=True}}" />
    </Grid>
  </DataTemplate>
  <DataTemplate x:Key="favoritesCategoryTab">
    <StackPanel Orientation="Horizontal">
      <ContentControl Content="{Binding}" ContentTemplate="{StaticResource commonCategoryTab}" />
      <ToggleButton x:Name="PART_Item" BorderThickness="0" Padding="1,0" VerticalContentAlignment="Center" Template="{StaticResource emptyButtonTemplate}">
        <dxb:BarManager.DXContextMenu>
          <dxb:PopupMenu IsOpen="{Binding PlacementTarget.IsChecked, RelativeSource={RelativeSource Self}}" DataContext="{Binding Path=PlacementTarget.(designer:ReportDesigner.Designer).ActualCommands, RelativeSource={RelativeSource Self}}">
            <dxb:BarButtonItem Content="Edit Favorite Properties..." Command="{Binding EditFavoritePropertiesCommand}" />
          </dxb:PopupMenu>
        </dxb:BarManager.DXContextMenu>
        <Path Data="F1M0,0L8,0 4,4 0,0z" Fill="{Binding (TextElement.Foreground), RelativeSource={RelativeSource Self}}" />
      </ToggleButton>
    </StackPanel>
  </DataTemplate>
  <DataTemplate x:Key="categorizedPanelTemplate">
    <dxe:ListBoxEdit Name="PART_CategoriesSelector" Focusable="False" AllowCollectionView="True" ShowBorder="False" AllowItemHighlighting="True" IsSynchronizedWithCurrentItem="True" ItemsSource="{Binding Path =(dxprg:PropertyGridHelper.PropertyGrid).View.CategoriesSource, RelativeSource={RelativeSource TemplatedParent}}">
      <dxe:ListBoxEdit.ItemsPanel>
        <ItemsPanelTemplate>
          <StackPanel Orientation="Horizontal" />
        </ItemsPanelTemplate>
      </dxe:ListBoxEdit.ItemsPanel>
      <dxe:ListBoxEdit.ItemTemplate>
        <DataTemplate>
          <ContentControl Content="{Binding}" ContentTemplate="{Binding Header, Converter={designernative:StringEqualsToDataTemplateConverter ExpectedString={localization:ReportStringId StringId=UD_PropertyGrid_TabFavorites}, TrueValue={StaticResource favoritesCategoryTab}, FalseValue={StaticResource commonCategoryTab}}}" />
        </DataTemplate>
      </dxe:ListBoxEdit.ItemTemplate>
    </dxe:ListBoxEdit>
  </DataTemplate>
  <DataTemplate x:Key="PropertyGridTopPanelTemplate">
    <ContentControl Name="PART_SelectorContainer" Content="{Binding}" ContentTemplate="{StaticResource commonPanelTemplate}" />
    <DataTemplate.Triggers>
      <DataTrigger Binding="{Binding Path=(dxprg:PropertyGridHelper.PropertyGrid).ShowCategories, RelativeSource={RelativeSource TemplatedParent}}" Value="Tabbed">
        <Setter TargetName="PART_SelectorContainer" Property="ContentTemplate" Value="{StaticResource categorizedPanelTemplate}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding Path=(dxprg:PropertyGridHelper.PropertyGrid).UserFilterCriteria, Converter={designernative:CriteriaToBooleanConverter}, RelativeSource={RelativeSource TemplatedParent}}" Value="True">
        <Setter TargetName="PART_SelectorContainer" Property="IsEnabled" Value="False" />
        <Setter TargetName="PART_SelectorContainer" Property="Opacity" Value="0.4" />
      </DataTrigger>
    </DataTemplate.Triggers>
  </DataTemplate>
  <ControlTemplate TargetType="propertygrid:ReportDesignerPropertyGrid" x:Key="propertyGridTemplate">
    <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0,0,0,1" Padding="4,0,4,2">
          <ContentControl Grid.Row="0" x:Name="PART_TopPanel" Focusable="False">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <ContentControl Content="{x:Null}" ContentTemplate="{StaticResource PropertyGridTopPanelTemplate}" />
              <Border x:Name="PART_SelectorAndSearchSeparator" Grid.Column="1" IsHitTestVisible="False" Background="Transparent" Width="{DynamicResource {dxprgt:PropertyGridControlThemeKey ResourceKey=SelectorAndSearchSeparatorWidth}}" />
              <dxprg:FilterColumnProvider x:Name="PART_ColumnProvider" Owner="{Binding RelativeSource={RelativeSource TemplatedParent}}" Width="0" Height="0" IsHitTestVisible="False" Opacity="0" />
              <dxprg:PropertyGridSearchControl x:Name="PART_SearchControl" Grid.Row="0" Grid.Column="2" Focusable="False" VerticalAlignment="Center" KeyboardNavigation.ControlTabNavigation="Continue" KeyboardNavigation.DirectionalNavigation="Continue" KeyboardNavigation.TabNavigation="Continue" FilterCondition="Contains" FilterByColumnsMode="Default" PostMode="Delayed" FindMode="Always" ShowFindButton="True" ShowMRUButton="False" ImmediateMRUPopup="False" MRULength="10" SearchTextPostDelay="1000" Style="{DynamicResource ResourceKey={dxprgt:PropertyGridControlThemeKey ResourceKey=SearchControlStyle}}" Visibility="{Binding ShowSearchBox, RelativeSource={RelativeSource TemplatedParent}, Converter={dx:BooleanToVisibilityConverter}}" ColumnProvider="{Binding ElementName=PART_ColumnProvider}" />
            </Grid>
          </ContentControl>
        </Border>
        <ContentPresenter x:Name="PART_ViewPresenter" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" />
        <dxprg:PropertyDescriptionPresenterControl x:Name="PART_DescriptionPresenter" Focusable="False" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" SelectedRow="{Binding ElementName=PART_ViewPresenter, Path=Content.SelectedItem}" Style="{TemplateBinding ActualDescriptionContainerStyle}">
          <dxprg:PropertyDescriptionPresenterControl.Visibility>
            <Binding Path="ShowDescriptionIn" RelativeSource="{RelativeSource TemplatedParent}">
              <Binding.Converter>
                <dxprg:DescriptionLocationConverter>
                  <dxprg:DescriptionLocationConverter.TrueValue>
                    <Visibility>Visible</Visibility>
                  </dxprg:DescriptionLocationConverter.TrueValue>
                  <dxprg:DescriptionLocationConverter.FalseValue>
                    <Visibility>Collapsed</Visibility>
                  </dxprg:DescriptionLocationConverter.FalseValue>
                </dxprg:DescriptionLocationConverter>
              </Binding.Converter>
              <Binding.ConverterParameter>
                <dxprg:DescriptionLocation>Panel</dxprg:DescriptionLocation>
              </Binding.ConverterParameter>
            </Binding>
          </dxprg:PropertyDescriptionPresenterControl.Visibility>
        </dxprg:PropertyDescriptionPresenterControl>
      </Grid>
    </Border>
    <ControlTemplate.Triggers>
      <Trigger Property="ShowToolPanel" Value="False">
        <Setter TargetName="PART_SelectorAndSearchSeparator" Property="Visibility" Value="Collapsed" />
      </Trigger>
      <Trigger Property="ShowSearchBox" Value="False">
        <Setter TargetName="PART_SelectorAndSearchSeparator" Property="Visibility" Value="Collapsed" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <Style TargetType="propertygrid:ReportDesignerPropertyGrid" x:Key="propertyGridStyle">
    <Style.Triggers>
      <DataTrigger Binding="{Binding Path=(designer:ReportDesigner.Designer).PropertyGridOptions.UseOfficeInspiredPropertyGrid, RelativeSource={RelativeSource Self}}" Value="True">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="ShowGridLines" Value="False" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="RowPadding" Value="3" />
      </DataTrigger>
    </Style.Triggers>
    <Setter Property="Template" Value="{StaticResource propertyGridTemplate}" />
    <Setter Property="ShowSearchBox" Value="True" />
    <Setter Property="ShowToolPanel" Value="True" />
    <Setter Property="ShowMenuButtonInRows" Value="True" />
    <Setter Property="ShowMenu" Value="Always" />
    <Setter Property="HighlightingMode" Value="None" />
    <Setter Property="ExpandCategoriesWhenSelectedObjectChanged" Value="True" />
    <Setter Property="SortMode" Value="Ascending" />
    <Setter Property="UseCollectionEditor" Value="False" />
  </Style>
</ResourceDictionary>